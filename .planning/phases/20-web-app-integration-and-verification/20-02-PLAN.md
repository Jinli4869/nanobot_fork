---
phase: 20-web-app-integration-and-verification
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - tests/test_tui_p20_static.py
  - nanobot/tui/static.py
  - nanobot/tui/app.py
  - nanobot/tui/web/vite.config.ts
  - nanobot/tui/web/src/lib/api/client.ts
  - nanobot/tui/web/src/lib/api/client.test.ts
  - nanobot/tui/web/src/lib/chat-events.ts
  - nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx
  - nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx
  - nanobot/tui/web/src/features/workspace-routes.test.tsx
autonomous: true
requirements: [WEB-01, SHIP-01]
must_haves:
  truths:
    - "Built frontend assets can be served from the local FastAPI app when the frontend build is present."
    - "Development mode has a concrete frontend-to-backend bridge through a Vite proxy or explicit API base resolver, so the SPA can talk to FastAPI before packaged delivery work begins."
    - "Browser deep links such as `/chat/{session_id}` and `/operations?runId=...` fall back to the SPA shell while existing `/health`, `/chat`, `/runtime`, and `/tasks` routes keep their current API semantics."
  artifacts:
    - path: "tests/test_tui_p20_static.py"
      provides: "Static-serving and deep-link regression coverage for the Phase 20 web shell"
      min_lines: 80
    - path: "nanobot/tui/static.py"
      provides: "Frontend asset discovery and SPA fallback helper for built assets"
      contains: "importlib.resources"
    - path: "nanobot/tui/web/vite.config.ts"
      provides: "Development proxy or backend bridge configuration for the frontend shell"
      contains: "/api"
    - path: "nanobot/tui/web/src/lib/api/client.test.ts"
      provides: "Frontend proof that development and served API base resolution is explicit"
      contains: "resolveApiBase"
    - path: "nanobot/tui/web/src/features/workspace-routes.test.tsx"
      provides: "Route-level proof that chat and operations screens consume the same typed client contract"
      contains: "runId|sessionId"
  key_links:
    - from: "nanobot/tui/app.py"
      to: "nanobot/tui/static.py"
      via: "frontend-serving opt-in for runtime apps"
      pattern: "serve_frontend"
    - from: "nanobot/tui/web/vite.config.ts"
      to: "nanobot/tui/routes/chat.py and other FastAPI routes"
      via: "development proxy or equivalent backend bridge"
      pattern: "/api|proxy"
    - from: "nanobot/tui/web/src/lib/api/client.ts"
      to: "nanobot/tui/web/src/lib/chat-events.ts"
      via: "shared API-base and streaming transport contract"
      pattern: "resolveApiBase|EventSource"
---

<objective>
Wire the static-serving and development bridge for the Phase 20 web app so the React/Vite frontend can talk to FastAPI and the built SPA shell can load safely without reopening packaged-startup seams yet.

Purpose: Satisfy the FastAPI serving and frontend-bridge parts of `WEB-01` and `SHIP-01` by connecting the Phase 20 SPA shell to the existing route family, deep-link fallback behavior, and development workflow.
Output: Static-serving regressions, development proxy/base-url wiring, FastAPI SPA fallback wiring, and typed frontend API/SSE route integration.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md
@.planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md
@.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-02-SUMMARY.md
@nanobot/tui/app.py
@nanobot/tui/routes/chat.py
@nanobot/tui/routes/runtime.py
@nanobot/tui/routes/tasks.py
@nanobot/tui/routes/traces.py
@tests/test_tui_p17_runtime.py
@nanobot/tui/web/package.json
@nanobot/tui/web/vite.config.ts
@nanobot/tui/web/src/lib/api/client.ts
@nanobot/tui/web/src/lib/chat-events.ts
@nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx
@nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx

<interfaces>
Current app seam:
```python
def create_app(*, config: Config | None = None, include_runtime_routes: bool = False, serve_frontend: bool = False) -> FastAPI: ...
```

Development-mode requirement:
```text
npm --prefix nanobot/tui/web run dev
- frontend uses `/api/...` proxying or explicit API base resolution
- dev requests reach the existing FastAPI route family without inventing `/api` routes on the backend
```

Constraints to preserve:
```text
- existing API route paths stay unchanged
- the default `create_app()` path stays safe for health-only/import-safe tests
- packaging metadata and canonical startup seams stay in the later closeout plan
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add static-serving and development-bridge regression coverage</name>
  <files>tests/test_tui_p20_static.py, nanobot/tui/web/src/lib/api/client.test.ts</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, nanobot/tui/app.py, nanobot/tui/web/vite.config.ts, nanobot/tui/web/src/lib/api/client.ts, tests/test_tui_p17_runtime.py</read_first>
  <behavior>
    - Test 1: `GET /` serves built `index.html` when frontend build assets are present.
    - Test 2: deep links such as `/chat/demo-session` and `/operations?runId=run-123` return the SPA shell instead of a raw 404.
    - Test 3: `/health`, `/chat`, `/runtime`, and `/tasks` still bypass the SPA fallback and keep their API behavior.
    - Test 4: missing build assets return deterministic startup guidance rather than a misleading blank HTML response.
    - Test 5: frontend development mode has a concrete backend bridge through `/api` proxying or explicit API base resolution.
  </behavior>
  <action>Create `tests/test_tui_p20_static.py` and `nanobot/tui/web/src/lib/api/client.test.ts` first with failing coverage for the delivery paths promised by this plan. Reuse `create_app(...)` in tests instead of booting uvicorn. Keep the expected route vocabulary rooted in the existing backend: do not introduce `/api/*` aliases on the server, auth redirects, or remote-host assumptions. Lock the development bridge explicitly by testing a concrete `resolveApiBase()` helper in `nanobot/tui/web/src/lib/api/client.ts` that returns `/api` in dev mode and a served-safe base in built mode.</action>
  <acceptance_criteria>
    - `tests/test_tui_p20_static.py` contains explicit assertions for `/`, `/chat/demo-session`, `/operations?runId=run-123`, and at least one existing API route.
    - `nanobot/tui/web/src/lib/api/client.test.ts` exists and contains `resolveApiBase`.
    - `npm --prefix nanobot/tui/web run test -- --run` passes.
    - `uv run --extra dev pytest tests/test_tui_p20_static.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run test -- --run && uv run --extra dev pytest tests/test_tui_p20_static.py -q</automated>
  </verify>
  <done>Wave-0 coverage exists for static serving, deep links, and the frontend development bridge before delivery code changes begin.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SPA fallback, typed client wiring, and chat/operations route integration</name>
  <files>nanobot/tui/static.py, nanobot/tui/app.py, nanobot/tui/web/vite.config.ts, nanobot/tui/web/src/lib/api/client.ts, nanobot/tui/web/src/lib/api/client.test.ts, nanobot/tui/web/src/lib/chat-events.ts, nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx, nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx, nanobot/tui/web/src/features/workspace-routes.test.tsx</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, .planning/phases/20-web-app-integration-and-verification/20-01-PLAN.md, tests/test_tui_p20_static.py, nanobot/tui/app.py, nanobot/tui/routes/chat.py, nanobot/tui/routes/runtime.py, nanobot/tui/routes/tasks.py, nanobot/tui/web/vite.config.ts, nanobot/tui/web/src/lib/api/client.ts, nanobot/tui/web/src/lib/chat-events.ts, nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx, nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx</read_first>
  <action>Implement `nanobot/tui/static.py` as the single built-asset helper. Resolve `nanobot/tui/web/dist` through `importlib.resources`, expose helpers for `dist` existence and `index.html`, and install static-file plus SPA-fallback routes in a way FastAPI can call without guessing the current working directory. Update `nanobot/tui/app.py` so the default `create_app()` behavior remains health-only and non-frontend, but runtime-enabled apps can opt into frontend serving through an explicit flag such as `serve_frontend=True`. The SPA fallback must serve `index.html` for `/`, `/chat/...`, and `/operations...` only when built assets exist; existing API routes must continue to win exact path matching. In `nanobot/tui/web/vite.config.ts`, `nanobot/tui/web/src/lib/api/client.ts`, `nanobot/tui/web/src/lib/chat-events.ts`, `nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx`, `nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx`, and `nanobot/tui/web/src/features/workspace-routes.test.tsx`, wire the real frontend client and chat SSE transport to the chosen development bridge: proxy `/api` from Vite to the FastAPI backend and rewrite back to the existing route family, or use an explicit `VITE_NANOBOT_API_BASE` resolver with the same behavior. `chat-events.ts` must construct `EventSource` from the same resolved base or a guaranteed direct `/chat` proxy contract so streamed chat works in both dev mode and built mode. The route components and route-integration test must consume the updated client and SSE helper so development mode and built mode both hit the same typed API layer. When build assets are missing, return clear startup guidance that tells the operator to build the frontend or use the Vite dev server instead of returning a blank page.</action>
  <acceptance_criteria>
    - `nanobot/tui/static.py` contains `importlib.resources`.
    - `nanobot/tui/app.py` still exposes a default `create_app()` path that does not enable frontend serving implicitly.
    - `nanobot/tui/app.py` contains an explicit frontend opt-in flag and mounts the SPA fallback only for runtime-enabled apps.
    - `nanobot/tui/web/vite.config.ts` contains `/api`.
    - `nanobot/tui/web/src/lib/api/client.ts` contains `resolveApiBase`.
    - `nanobot/tui/web/src/lib/chat-events.ts` references the same base-resolution or `/chat` proxy contract.
    - `nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx` references the updated API client.
    - `nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx` references the updated API client.
    - `nanobot/tui/web/src/features/workspace-routes.test.tsx` exists and covers shared route behavior with the updated client contract.
    - `npm --prefix nanobot/tui/web run build` passes.
    - `npm --prefix nanobot/tui/web run test -- --run` passes.
    - `uv run --extra dev pytest tests/test_tui_p20_static.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run build && npm --prefix nanobot/tui/web run test -- --run && uv run --extra dev pytest tests/test_tui_p20_static.py -q</automated>
  </verify>
  <done>Development mode can reach the backend predictably, and built frontend assets can be served from FastAPI without changing existing browser API routes or the health-only default app seam.</done>
</task>

</tasks>

<verification>
Use the `20-02` commands from `20-VALIDATION.md`. If the local environment still blocks `uv run --extra dev pytest ...`, use the documented fallback `.venv/bin/python -m pytest tests/test_tui_p20_static.py -q` after the frontend test/build slice succeeds.
</verification>

<success_criteria>
Development mode can reach the FastAPI backend predictably, built frontend assets can be served safely from FastAPI when present, and the chat/operations route components share one explicit typed API/SSE bridge.
</success_criteria>

<output>
After completion, create `.planning/phases/20-web-app-integration-and-verification/20-web-app-integration-and-verification-02-SUMMARY.md`
</output>
