---
phase: 19-operations-console
plan: "03"
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - tests/test_tui_p19_traces.py
  - nanobot/tui/contracts.py
  - nanobot/tui/schemas/traces.py
  - nanobot/tui/schemas/__init__.py
  - nanobot/tui/services/traces.py
  - nanobot/tui/services/__init__.py
  - nanobot/tui/dependencies.py
  - nanobot/tui/routes/traces.py
  - nanobot/tui/routes/__init__.py
  - nanobot/tui/app.py
autonomous: true
requirements: [OPS-03]
must_haves:
  truths:
    - "Browser clients can inspect structured trace events and filtered log lines for a web-triggered run without terminal access."
    - "Trace and log inspection stay browser-safe by allowlisting fields and hiding raw artifact layout, prompt text, and other noisy internals."
    - "Public inspection routes are addressed by `run_id`; artifact paths stay private to the backend."
    - "Phase 17-19 backend regressions prove the operations console closes out without destabilizing the existing CLI/chat/runtime seams."
  artifacts:
    - path: "tests/test_tui_p19_traces.py"
      provides: "Wave 0 and green regression coverage for filtered trace/log inspection"
      min_lines: 120
    - path: "nanobot/tui/services/traces.py"
      provides: "Artifact reader that parses and filters JSONL traces into stable DTOs"
      contains: "class TraceInspectionService"
    - path: "nanobot/tui/routes/traces.py"
      provides: "HTTP routes for run trace and log detail"
      exports: ["router"]
  key_links:
    - from: "nanobot/tui/routes/traces.py"
      to: "nanobot/tui/services/traces.py"
      via: "FastAPI dependency injection"
      pattern: "Depends\\(get_trace_inspection_service\\)"
    - from: "nanobot/tui/services/traces.py"
      to: "opengui_runs/**/trace*.jsonl or workspace/gui_runs/**"
      via: "filtered artifact parsing"
      pattern: "trace"
    - from: "nanobot/tui/app.py"
      to: "nanobot/tui/routes/traces.py"
      via: "runtime-enabled route registration"
      pattern: "include_router\\(traces_router\\)"
---

<objective>
Expose filtered trace and log inspection for the operations console, then close Phase 19 with the regression slice that proves the new APIs stay isolated under `nanobot/tui`.

Purpose: Satisfy `OPS-03` and finish the backend side of the operations console before Phase 20 takes over the React/Vite shell work.
Output: Typed trace/log schemas, filtered artifact-reader service, routes wired into the TUI app, and closeout regression coverage.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-operations-console/19-RESEARCH.md
@.planning/phases/19-operations-console/19-01-PLAN.md
@.planning/phases/19-operations-console/19-02-PLAN.md
@nanobot/tui/app.py
@nanobot/tui/contracts.py
@nanobot/tui/dependencies.py
@nanobot/tui/routes/runtime.py
@nanobot/tui/routes/tasks.py
@nanobot/agent/tools/gui.py
@opengui/agent.py
@tests/test_tui_p17_runtime.py
@tests/test_tui_p18_chat.py
@tests/test_tui_p18_streaming.py
@tests/test_opengui_p3_nanobot.py
@tests/test_opengui_p16_host_integration.py

<interfaces>
From `nanobot/agent/tools/gui.py`:
```python
return json.dumps(
    {
        "success": result.success,
        "summary": summary,
        "trace_path": str(trace_path) if trace_path is not None else result.trace_path,
        "steps_taken": result.steps_taken,
        "error": error,
    },
    ensure_ascii=False,
)
```

From `opengui/agent.py`:
```python
class AgentResult:
    success: bool
    summary: str
    trace_path: str | None = None
    steps_taken: int = 0
    error: str | None = None
```

Expected from `19-02` registry state:
```python
run_id: str
task_kind: str
status: Literal["queued", "running", "succeeded", "failed"]
trace_path: str | None  # internal only; public APIs resolve by run_id
```

Required browser-safe allowlist for this plan:
```python
ALLOWED_TRACE_EVENT_TYPES = (
    "metadata",
    "attempt_start",
    "attempt_result",
    "attempt_exception",
    "retry",
    "step",
    "result",
)

ALLOWED_TRACE_FIELDS = (
    "event_id",
    "event_type",
    "timestamp",
    "step_index",
    "status",
    "summary",
    "success",
    "done",
    "retry_count",
)

ALLOWED_LOG_FIELDS = ("timestamp", "level", "code", "message")
TRUNCATE_TEXT_AT = 240
```

Explicit exclusions: `prompt`, `model_output`, raw tool arguments, screenshot paths, artifact paths, and raw JSON trace payloads.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 trace/log inspection tests and typed browser-safe schemas</name>
  <files>tests/test_tui_p19_traces.py, nanobot/tui/contracts.py, nanobot/tui/schemas/traces.py, nanobot/tui/schemas/__init__.py</files>
  <behavior>
    - Test 1: Trace detail endpoints return filtered event DTOs keyed by run id.
    - Test 2: Log detail endpoints return useful status/error lines while excluding prompt text, raw paths, and arbitrary payload dumps.
    - Test 3: Missing or incomplete artifacts return typed not-found/empty responses instead of leaking internal file layout assumptions.
  </behavior>
  <action>Create `tests/test_tui_p19_traces.py` first with failing expectations for filtered run detail endpoints. Add dedicated trace/log schemas under `nanobot/tui/schemas/traces.py` and extend `nanobot/tui/contracts.py` with a typed inspection contract. Lock the browser-safe contract before implementation: allowed event types are `metadata`, `attempt_start`, `attempt_result`, `attempt_exception`, `retry`, `step`, and `result`; allowed event fields are `event_id`, `event_type`, `timestamp`, `step_index`, `status`, `summary`, `success`, `done`, and `retry_count`; allowed log fields are `timestamp`, `level`, `code`, and `message`. Summaries and log messages must be truncated to a short cap such as 240 characters. The DTOs must not expose raw trace JSON, prompt/model snapshots, raw tool arguments, screenshot paths, artifact paths, or future credential-bearing payloads.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p19_traces.py::test_trace_endpoint_returns_filtered_events_for_browser_consumers tests/test_tui_p19_traces.py::test_log_endpoint_returns_filtered_lines_without_raw_paths_or_prompts -q</automated>
  </verify>
  <done>Wave 0 trace/log tests and DTOs exist, and the browser-safe inspection contract is locked before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement filtered artifact readers, wire routes, and close the regression slice</name>
  <files>nanobot/tui/services/traces.py, nanobot/tui/services/__init__.py, nanobot/tui/dependencies.py, nanobot/tui/routes/traces.py, nanobot/tui/routes/__init__.py, nanobot/tui/app.py</files>
  <behavior>
    - Test 1: Completed or failed runs expose filtered trace/log detail through typed routes.
    - Test 2: The route layer reads run ids through the shared registry and artifact readers instead of exposing raw browsing semantics.
    - Test 3: The Phase 17-19 regression slice remains green after trace routes are added to the runtime-enabled TUI app.
  </behavior>
  <action>Implement `TraceInspectionService` under `nanobot/tui/services/traces.py` to parse existing JSONL artifacts from nanobot/OpenGUI run outputs and map them into browser-safe DTOs. Resolve all public lookups from `run_id` through the shared registry; clients must never provide or receive artifact paths. Add a dedicated `nanobot/tui/routes/traces.py` module plus dependency providers and export wiring. Register the route only on runtime-enabled TUI apps, preserving Phase 17's health-only default. Keep filtering strict and allowlist-based; do not proxy raw files or expose direct artifact paths. Close Phase 19 by rerunning the full Phase 17-19 backend regression slice, including the existing nanobot/OpenGUI host integration tests named in the validation ledger.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py tests/test_tui_p18_chat.py tests/test_tui_p18_streaming.py tests/test_tui_p19_runtime.py tests/test_tui_p19_tasks.py tests/test_tui_p19_traces.py tests/test_opengui_p3_nanobot.py tests/test_opengui_p16_host_integration.py -q</automated>
  </verify>
  <done>The operations console exposes filtered diagnostics for web-triggered runs, route wiring stays isolated under `nanobot/tui`, and the full backend regression slice proves Phase 19 is ready for the Phase 20 UI shell.</done>
</task>

</tasks>

<verification>
Run the `19-03` closeout slice from `19-VALIDATION.md`. If `uv run --extra dev pytest ...` is blocked locally, use the documented fallback `.venv/bin/python -m pytest tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py tests/test_tui_p18_chat.py tests/test_tui_p18_streaming.py tests/test_tui_p19_runtime.py tests/test_tui_p19_tasks.py tests/test_tui_p19_traces.py tests/test_opengui_p3_nanobot.py tests/test_opengui_p16_host_integration.py -q`.
</verification>

<success_criteria>
`OPS-03` is satisfied with browser-safe trace/log APIs, all Phase 19 backend contracts live under `nanobot/tui`, and the resulting status, launch, and inspection surfaces are ready for Phase 20 to consume without reopening backend architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/19-operations-console/19-operations-console-03-SUMMARY.md`
</output>
