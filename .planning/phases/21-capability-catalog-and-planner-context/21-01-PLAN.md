---
phase: 21-capability-catalog-and-planner-context
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - nanobot/agent/capabilities.py
  - nanobot/agent/planner.py
  - nanobot/agent/loop.py
  - tests/test_opengui_p21_planner_context.py
  - tests/test_opengui_p8_planning.py
autonomous: true
requirements: [CAP-01]
must_haves:
  truths:
    - "Planner receives a bounded catalog of concrete currently available routes before it decomposes a task."
    - "ATOM nodes can carry `route_id`, `route_reason`, and `fallback_route_ids` without breaking legacy plan payloads that only specify `instruction` and `capability`."
    - "Planner logs expose route identity in both the human-readable tree and the raw serialized plan payload."
  artifacts:
    - path: "nanobot/agent/capabilities.py"
      provides: "Planner-only capability catalog DTOs and the live runtime builder"
      contains: "class CapabilityCatalogBuilder"
    - path: "nanobot/agent/planner.py"
      provides: "Backward-compatible PlanNode route metadata and planner prompt/catalog plumbing"
      contains: "route_id"
    - path: "nanobot/agent/loop.py"
      provides: "Runtime catalog injection into `_plan_and_execute()` and route-aware tree formatting"
      contains: "Decomposed plan"
    - path: "tests/test_opengui_p21_planner_context.py"
      provides: "Catalog and route-metadata regression coverage for Phase 21"
      contains: "catalog"
  key_links:
    - from: "nanobot/agent/loop.py"
      to: "nanobot/agent/capabilities.py"
      via: "build live planner catalog immediately before `planner.plan(...)`"
      pattern: "CapabilityCatalogBuilder"
    - from: "nanobot/agent/planner.py"
      to: "nanobot/agent/capabilities.py"
      via: "planner prompt serialization consumes catalog summaries instead of raw tool schemas"
      pattern: "PlanningContext"
    - from: "nanobot/agent/loop.py"
      to: "nanobot/agent/planner.py"
      via: "pretty tree and raw `to_dict()` logging surface route metadata"
      pattern: "route_id"
---

<objective>
Give the planner a compact, live route inventory and a backward-compatible route-aware plan schema so capability choice is grounded in actual runtime options instead of only coarse capability labels.

Purpose: Satisfy `CAP-01` without pulling Phase 22 dispatch behavior forward.
Output: Planner-only catalog DTOs and builder, optional route metadata on `PlanNode`, prompt/context plumbing, and route-aware planner logging.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-capability-catalog-and-planner-context/21-RESEARCH.md
@.planning/phases/21-capability-catalog-and-planner-context/21-VALIDATION.md
@docs/plans/2026-03-22-capability-aware-planner-routing-design.md
@nanobot/agent/planner.py
@nanobot/agent/loop.py
@nanobot/agent/router.py
@nanobot/agent/tools/registry.py
@nanobot/agent/tools/mcp.py
@nanobot/agent/tools/filesystem.py
@nanobot/agent/tools/shell.py
@nanobot/agent/tools/web.py
@nanobot/agent/tools/gui.py
@tests/test_opengui_p8_planning.py

<interfaces>
Current planner and loop seams to preserve while extending:
```python
@dataclass(frozen=True)
class PlanNode:
    node_type: NodeType
    instruction: str = ""
    capability: CapabilityType = "tool"
    children: tuple["PlanNode", ...] = field(default_factory=tuple)

class TaskPlanner:
    async def plan(self, task: str, *, context: str = "") -> PlanNode: ...
```

```python
planner = TaskPlanner(llm=self.provider)
tree = await planner.plan(task)
logger.info("Decomposed plan:\n{}", self._format_plan_tree(tree))
logger.debug("Decomposed plan (raw): {}", tree.to_dict())
```

Live tool names already present in the runtime:
```text
gui_task
exec
read_file
write_file
edit_file
list_dir
web_search
web_fetch
mcp_<server>_<tool>
```

Phase 21 constraints:
```text
- add planner-only route summaries; do not dump raw tool schemas into the prompt
- allowlist routeable tools; exclude support/control tools like message, spawn, and cron
- add optional route metadata only; do not implement route_id-based dispatch in TreeRouter
- preserve old PlanNode payload parsing and existing Phase 8 tests
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the capability-catalog contracts and failing route-selection coverage</name>
  <files>nanobot/agent/capabilities.py, nanobot/agent/planner.py, tests/test_opengui_p21_planner_context.py, tests/test_opengui_p8_planning.py</files>
  <read_first>.planning/phases/21-capability-catalog-and-planner-context/21-RESEARCH.md, .planning/phases/21-capability-catalog-and-planner-context/21-VALIDATION.md, docs/plans/2026-03-22-capability-aware-planner-routing-design.md, nanobot/agent/planner.py, nanobot/agent/loop.py, nanobot/agent/tools/registry.py, nanobot/agent/tools/mcp.py, nanobot/agent/tools/filesystem.py, nanobot/agent/tools/shell.py, nanobot/agent/tools/web.py, nanobot/agent/tools/gui.py, tests/test_opengui_p8_planning.py, tests/test_opengui_p21_planner_context.py</read_first>
  <behavior>
    - Test 1: a new `CapabilityCatalogBuilder` produces bounded `RouteSummary` items from the live registry, exposes stable planner-facing route IDs, and excludes non-route support tools such as `message`, `spawn`, and `cron`.
    - Test 2: `PlanNode.to_dict()` and `PlanNode.from_dict()` round-trip optional `route_id`, `route_reason`, and `fallback_route_ids`, while old payloads without those keys still parse unchanged.
    - Test 3: the new planner context contract has a dedicated `PlanningContext` wrapper so later plans can add memory hints without changing the planner entrypoint again.
  </behavior>
  <action>Create `nanobot/agent/capabilities.py` first with planner-only dataclasses `RouteSummary`, `CapabilityCatalog`, `PlanningContext`, and the builder shell `CapabilityCatalogBuilder`. Use explicit route-id mapping instead of raw tool names: `gui.desktop` for `gui_task`, `tool.exec_shell` for `exec`, `tool.filesystem.read` for `read_file`, `tool.filesystem.write` for `write_file`, `tool.filesystem.edit` for `edit_file`, `tool.filesystem.list` for `list_dir`, `tool.web.search` for `web_search`, `tool.web.fetch` for `web_fetch`, and `mcp.<server>.<tool>` derived from `mcp_<server>_<tool>` wrappers. Write failing tests in `tests/test_opengui_p21_planner_context.py` for allowlisted catalog generation, stable route IDs, and exclusion of `message`/`spawn`/`cron`. Extend `tests/test_opengui_p8_planning.py` with failing coverage for backward-compatible route metadata serialization on `PlanNode`. Do not implement router dispatch, do not dump `ToolRegistry.get_definitions()` into the prompt, and do not make the new route fields required.</action>
  <acceptance_criteria>
    - `nanobot/agent/capabilities.py` contains the literals `class RouteSummary`, `class CapabilityCatalog`, `class PlanningContext`, and `class CapabilityCatalogBuilder`.
    - `tests/test_opengui_p21_planner_context.py` contains coverage with names matching `catalog` and `route_metadata`.
    - `nanobot/agent/planner.py` contains the exact field names `route_id`, `route_reason`, and `fallback_route_ids`.
    - `tests/test_opengui_p8_planning.py` contains a regression that round-trips route metadata while preserving legacy payload parsing.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_opengui_p21_planner_context.py tests/test_opengui_p8_planning.py -k "catalog or route_metadata"</automated>
  </verify>
  <done>The route catalog contracts exist, failing coverage proves the expected route IDs and schema compatibility, and the implementation boundary for Phase 21 is explicit before prompt plumbing begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the live catalog and wire route metadata into planner prompts and logs</name>
  <files>nanobot/agent/capabilities.py, nanobot/agent/planner.py, nanobot/agent/loop.py, tests/test_opengui_p21_planner_context.py, tests/test_opengui_p8_planning.py</files>
  <read_first>.planning/phases/21-capability-catalog-and-planner-context/21-RESEARCH.md, .planning/phases/21-capability-catalog-and-planner-context/21-VALIDATION.md, nanobot/agent/capabilities.py, nanobot/agent/planner.py, nanobot/agent/loop.py, nanobot/agent/router.py, nanobot/agent/tools/registry.py, nanobot/agent/tools/mcp.py, tests/test_opengui_p21_planner_context.py, tests/test_opengui_p8_planning.py</read_first>
  <behavior>
    - Test 1: `AgentLoop._plan_and_execute()` builds a `PlanningContext` from the live runtime and passes it into `TaskPlanner.plan(...)` before logging the plan.
    - Test 2: `TaskPlanner` includes a compact capability-catalog section in the system prompt and instructs the model to select `route_id`, optional `fallback_route_ids`, and a short `route_reason`.
    - Test 3: `_format_plan_tree()` and raw plan logging expose route metadata when present without regressing the existing Phase 8 readable tree behavior for nodes that do not use routes.
  </behavior>
  <action>Implement `CapabilityCatalogBuilder.build(...)` so it reads the live `ToolRegistry`, GUI availability, `AgentLoop.exec_config.enable`, and connected MCP wrappers to produce compact `RouteSummary` items with fields `route_id`, `capability`, `kind`, `summary`, `use_for`, `avoid_for`, and `availability`. Keep the builder allowlist-based and keep all execution semantics unchanged. Extend `TaskPlanner.plan()` and prompt helpers to accept `planning_context: PlanningContext | None = None`; serialize the catalog into a bounded prompt block instead of raw schemas, and update the `create_plan` tool contract description so ATOM nodes may include `route_id`, `route_reason`, and `fallback_route_ids`. Update `PlanNode.to_dict()`/`from_dict()` to include those fields only when present. In `AgentLoop._plan_and_execute()`, build the catalog immediately before planning and log route-aware trees by appending route labels such as `via gui.desktop` and `fallback -> gui.desktop` when metadata exists. Do not change `TreeRouter._run_tool()` or `_run_mcp()` beyond carrying the existing node data through unchanged.</action>
  <acceptance_criteria>
    - `nanobot/agent/planner.py` contains the signature fragment `planning_context: PlanningContext | None = None`.
    - `nanobot/agent/planner.py` contains the literal keys `route_id`, `route_reason`, and `fallback_route_ids` inside both prompt/schema or serialization code.
    - `nanobot/agent/loop.py` contains `CapabilityCatalogBuilder` and route-aware tree text containing `fallback ->`.
    - `nanobot/agent/capabilities.py` contains stable planner-facing route prefixes `gui.`, `tool.`, and `mcp.`.
    - `tests/test_opengui_p8_planning.py` contains route-aware log assertions for both the pretty tree and raw `to_dict()` output.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_opengui_p21_planner_context.py tests/test_opengui_p8_planning.py tests/test_mcp_tool.py -k "catalog or route or plan_and_execute_logs_tree or format_plan_tree"</automated>
  </verify>
  <done>The planner sees a compact live route catalog, emits optional route metadata in parsed plans, and surfaces route choice in planner logs without changing execution behavior.</done>
</task>

</tasks>

<verification>
Run the targeted Phase 21 slice from `21-VALIDATION.md` after each task: `uv run pytest -q tests/test_opengui_p21_planner_context.py tests/test_opengui_p8_planning.py tests/test_mcp_tool.py`. If MCP-related helper changes force a new regression in `tests/test_mcp_tool.py`, keep the change planner-only and verify wrapped tool-name normalization rather than real dispatch behavior.
</verification>

<success_criteria>
`CAP-01` is satisfied when the planner receives a bounded live route catalog, plan parsing remains backward-compatible, and both human-readable and raw planning logs show route metadata clearly enough for inspection.
</success_criteria>

<output>
After completion, create `.planning/phases/21-capability-catalog-and-planner-context/21-01-SUMMARY.md`
</output>
