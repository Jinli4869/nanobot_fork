---
phase: 12-background-runtime-contracts
plan: "04"
type: execute
wave: 3
depends_on:
  - 12-02
  - 12-03
files_modified:
  - .planning/phases/12-background-runtime-contracts/12-VALIDATION.md
autonomous: true
requirements:
  - BGND-05
  - BGND-06
  - BGND-07

must_haves:
  truths:
    - "The full Phase 12 regression suite is green."
    - "The validation document marks Wave 0 complete and Nyquist compliance true."
    - "Every task ID in the validation map points at a passing automated command."
  artifacts:
    - path: ".planning/phases/12-background-runtime-contracts/12-VALIDATION.md"
      provides: "Green validation ledger for Phase 12"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/12-background-runtime-contracts/12-VALIDATION.md"
      to: "tests/test_opengui_p12_runtime_contracts.py"
      via: "task verification map"
      pattern: "tests/test_opengui_p12_runtime_contracts.py"
    - from: ".planning/phases/12-background-runtime-contracts/12-VALIDATION.md"
      to: "tests/test_opengui_p5_cli.py"
      via: "task verification map"
      pattern: "tests/test_opengui_p5_cli.py"
---

<objective>
Close Phase 12 by running the full regression command and recording the final validation state in the phase ledger.

Purpose: Execute-phase needs a clean validation target before Phase 13 starts consuming the shared contracts.
Output: Updated `12-VALIDATION.md` with green statuses, `nyquist_compliant: true`, and `wave_0_complete: true`.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-background-runtime-contracts/12-VALIDATION.md
@tests/test_opengui_p12_runtime_contracts.py
@tests/test_opengui_p5_cli.py
@tests/test_opengui_p11_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full Phase 12 suite and mark the validation ledger green</name>
  <files>.planning/phases/12-background-runtime-contracts/12-VALIDATION.md</files>
  <read_first>
    - .planning/phases/12-background-runtime-contracts/12-VALIDATION.md
    - tests/test_opengui_p12_runtime_contracts.py
    - tests/test_opengui_p5_cli.py
    - tests/test_opengui_p11_integration.py
  </read_first>
  <action>
Run the full Phase 12 regression command from `12-VALIDATION.md`:

`uv run pytest tests/test_opengui_p12_runtime_contracts.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py -q`

If that command is green, update `.planning/phases/12-background-runtime-contracts/12-VALIDATION.md` exactly as follows:
- set frontmatter `status: ready`
- set frontmatter `nyquist_compliant: true`
- set frontmatter `wave_0_complete: true`
- change every pending task row in the verification map to `✅ green`
- keep the same task IDs and commands already listed in the file
- tick these checklist items at the bottom:
  - `All tasks have <automated> verify or Wave 0 dependencies`
  - `Sampling continuity: no 3 consecutive tasks without automated verify`
  - `Wave 0 covers all MISSING references`
  - `No watch-mode flags`
  - `Feedback latency < 20s`

Do not change the command strings unless the implemented tests forced a path rename.
  </action>
  <acceptance_criteria>
    - `rg -n "^status: ready$" .planning/phases/12-background-runtime-contracts/12-VALIDATION.md` returns one match.
    - `rg -n "^nyquist_compliant: true$" .planning/phases/12-background-runtime-contracts/12-VALIDATION.md` returns one match.
    - `rg -n "^wave_0_complete: true$" .planning/phases/12-background-runtime-contracts/12-VALIDATION.md` returns one match.
    - `rg -n "✅ green" .planning/phases/12-background-runtime-contracts/12-VALIDATION.md` returns at least nine matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p12_runtime_contracts.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py -q</automated>
  </verify>
  <done>Phase 12 has one green regression command and a validation file that records the phase as Nyquist-compliant and ready for downstream execution.</done>
</task>

</tasks>

<verification>
The only acceptable completion state is a green full-suite command and an updated validation file that matches it. If the suite fails, this plan is not complete.
</verification>

<success_criteria>
Phase 12 can hand off to Phase 13 with a passing regression sample and a validation ledger that already points to the correct runtime, CLI, and nanobot tests.
</success_criteria>

<output>
After completion, create `.planning/phases/12-background-runtime-contracts/12-background-runtime-contracts-04-SUMMARY.md`
</output>
