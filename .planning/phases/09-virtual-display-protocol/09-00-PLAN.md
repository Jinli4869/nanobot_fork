---
phase: 09-virtual-display-protocol
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_opengui_p9_virtual_display.py
  - tests/test_opengui_p9_xvfb.py
autonomous: true
requirements:
  - VDISP-01
  - VDISP-02
  - VDISP-03
  - VDISP-04

must_haves:
  truths:
    - "Test stub files exist and are importable by pytest"
    - "All 13 test cases are declared as xfail stubs (expected to fail until implementation)"
    - "Running pytest on stub files produces 13 xfail results (not errors)"
  artifacts:
    - path: "tests/test_opengui_p9_virtual_display.py"
      provides: "xfail stubs for 9 protocol/DisplayInfo/NoOp tests"
      min_lines: 30
    - path: "tests/test_opengui_p9_xvfb.py"
      provides: "xfail stubs for 4 Xvfb error-path tests (12 total in final)"
      min_lines: 20
  key_links: []
---

<objective>
Create Wave 0 test stub files so that Nyquist sampling is satisfied before any production code is written in Plans 01 and 02.

Purpose: Establish test file scaffolds with xfail-decorated stubs for all 13 test cases across both test files. Plans 01 and 02 will fill in the test implementations (not create the files from scratch).
Output: Two test files with pytest.mark.xfail stubs that run without import errors.
</objective>

<execution_context>
@/Users/jinli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-virtual-display-protocol/09-CONTEXT.md
@.planning/phases/09-virtual-display-protocol/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create xfail test stubs for virtual_display.py and xvfb.py</name>
  <files>tests/test_opengui_p9_virtual_display.py, tests/test_opengui_p9_xvfb.py</files>
  <read_first>
    - tests/test_opengui_agent.py
    - opengui/backends/virtual_display.py
    - opengui/backends/displays/xvfb.py
  </read_first>
  <action>
Create two test stub files following the project convention from `tests/test_opengui_agent.py`.

**File 1: `tests/test_opengui_p9_virtual_display.py`**

Create 9 test functions, each decorated with `@pytest.mark.xfail(reason="Wave 0 stub — implementation in Plan 01")` and containing only `assert False, "stub"`:

1. `test_protocol_importable` — VDISP-01
2. `test_protocol_methods_are_async` — VDISP-01
3. `test_display_info_frozen` — VDISP-02
4. `test_display_info_field_names` — VDISP-02
5. `test_display_info_defaults` — VDISP-02
6. `test_noop_start_returns_display_info` — VDISP-03
7. `test_noop_custom_dimensions` — VDISP-03
8. `test_noop_stop_is_idempotent` — VDISP-03
9. `test_noop_start_no_subprocess` — VDISP-03

Include these imports at the top (they will resolve once Plan 01 Task 1 adds re-exports, but the xfail decoration means import failures won't break the run — wrap imports in a try/except at module level and use `pytest.importorskip` or guard with a module-level skip):

```python
import pytest

# Guarded imports — these modules may not have re-exports yet in Wave 0
try:
    from opengui.interfaces import VirtualDisplayManager, DisplayInfo
    from opengui.backends.virtual_display import NoOpDisplayManager
    _IMPORTS_OK = True
except ImportError:
    _IMPORTS_OK = False

pytestmark = pytest.mark.skipif(not _IMPORTS_OK, reason="Wave 0: imports not yet wired")
```

Actually, since the draft `virtual_display.py` already exists with the correct classes, the imports WILL resolve. Use direct imports without guards:

```python
import pytest

from opengui.backends.virtual_display import (
    VirtualDisplayManager,
    DisplayInfo,
    NoOpDisplayManager,
)
```

Each stub function:
```python
@pytest.mark.xfail(reason="Wave 0 stub")
async def test_protocol_importable():
    assert False, "stub"
```

Use `async def` for all test functions (asyncio_mode="auto" is configured).

**File 2: `tests/test_opengui_p9_xvfb.py`**

Create 12 test functions (covering the full set from Plan 02), each with `@pytest.mark.xfail(reason="Wave 0 stub — implementation in Plan 02")` and `assert False, "stub"`:

1. `test_xvfb_isinstance_check` — VDISP-04
2. `test_xvfb_start_returns_display_info` — VDISP-04
3. `test_xvfb_start_custom_dimensions` — VDISP-04
4. `test_xvfb_not_found_error` — VDISP-04
5. `test_xvfb_start_timeout` — VDISP-04
6. `test_xvfb_auto_increment` — VDISP-04
7. `test_xvfb_auto_increment_all_locked` — VDISP-04
8. `test_xvfb_crash_detection` — VDISP-04
9. `test_xvfb_stop_never_started` — VDISP-04
10. `test_xvfb_stop_idempotent` — VDISP-04
11. `test_xvfb_stop_terminates_process` — VDISP-04
12. `test_xvfb_stderr_is_piped` — VDISP-04

Imports for xvfb stubs — the draft xvfb.py exists but does NOT yet have XvfbNotFoundError or XvfbCrashedError (those are added by Plan 02 Task 1). Use guarded imports:

```python
import pytest

try:
    from opengui.backends.displays.xvfb import (
        XvfbDisplayManager,
        XvfbNotFoundError,
        XvfbCrashedError,
    )
    from opengui.backends.virtual_display import VirtualDisplayManager, DisplayInfo
    _IMPORTS_OK = True
except ImportError:
    _IMPORTS_OK = False

pytestmark = [
    pytest.mark.skipif(not _IMPORTS_OK, reason="Wave 0: error types not yet defined"),
    pytest.mark.xfail(reason="Wave 0 stub"),
]
```

Each stub function:
```python
async def test_xvfb_isinstance_check():
    assert False, "stub"
```

Note: With `pytestmark` containing `xfail`, individual decorators are not needed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_opengui_p9_virtual_display.py tests/test_opengui_p9_xvfb.py -v --tb=no 2>&1 | tail -25</automated>
  </verify>
  <done>Both test files exist, pytest discovers all 21 test functions, all are either xfail or skipped (zero errors, zero unexpected failures)</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_opengui_p9_virtual_display.py tests/test_opengui_p9_xvfb.py -v --tb=no` shows 21 tests (xfail or skip)
- `python -m pytest tests/ -x -q` passes (no regressions)
</verification>

<success_criteria>
- tests/test_opengui_p9_virtual_display.py exists with 9 test functions
- tests/test_opengui_p9_xvfb.py exists with 12 test functions
- All stubs are xfail-decorated or module-level skipped
- pytest runs clean (no errors, no unexpected failures)
</success_criteria>

<output>
After completion, create `.planning/phases/09-virtual-display-protocol/09-00-SUMMARY.md`
</output>
