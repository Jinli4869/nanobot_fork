---
phase: 17-web-runtime-boundary
plan: "02"
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - nanobot/config/schema.py
  - nanobot/config/loader.py
  - nanobot/tui/app.py
  - nanobot/tui/config.py
  - nanobot/tui/__main__.py
  - nanobot/tui/dependencies.py
  - nanobot/tui/routes/sessions.py
  - nanobot/tui/routes/runtime.py
  - nanobot/tui/routes/tasks.py
  - nanobot/tui/schemas/__init__.py
  - nanobot/tui/schemas/sessions.py
  - nanobot/tui/schemas/runtime.py
  - nanobot/tui/schemas/tasks.py
  - nanobot/tui/services/__init__.py
  - nanobot/tui/services/sessions.py
  - nanobot/tui/services/runtime.py
  - nanobot/tui/services/tasks.py
  - tests/test_tui_p17_runtime.py
  - tests/test_tui_p17_config.py
  - tests/test_commands.py
  - tests/test_config_paths.py
autonomous: true
requirements:
  - ISO-01
  - ISO-02
must_haves:
  truths:
    - "The web runtime defaults to localhost-safe serving and explicit configuration instead of broad network exposure."
    - "Starting the TUI backend uses narrow startup wiring that does not change existing CLI, channel, or background execution defaults."
    - "Config loading exposes a dedicated web runtime section instead of overloading unrelated gateway or GUI settings."
    - "Read-only session, runtime, and task-contract endpoints expose future browser seams without making the API mutating yet."
  artifacts:
    - path: "nanobot/config/schema.py"
      provides: "Dedicated web/TUI config schema with local-first defaults"
      contains: "TuiConfig"
    - path: "nanobot/tui/config.py"
      provides: "Normalization helpers for bind host, port, and launch options"
      contains: "resolve_tui_runtime_config"
    - path: "nanobot/tui/__main__.py"
      provides: "Import-safe module startup seam for the TUI backend"
      contains: "main"
    - path: "nanobot/tui/routes/tasks.py"
      provides: "Read-only task-contract endpoint for future launch flows"
      contains: "router"
    - path: "tests/test_tui_p17_config.py"
      provides: "Regression coverage for local-first defaults and startup wiring"
      contains: "test_tui_defaults_bind_to_localhost"
  key_links:
    - from: "nanobot/config/loader.py::load_config"
      to: "nanobot/config/schema.py::TuiConfig"
      via: "Existing config loading path validates the new web section without special-case loaders"
      pattern: "Config.model_validate"
    - from: "nanobot/tui/__main__.py"
      to: "nanobot/tui/app.py::create_app"
      via: "Module startup reuses the app factory instead of duplicating route registration or runtime wiring"
      pattern: "create_app"
    - from: "nanobot/tui/routes/tasks.py"
      to: "nanobot/tui/services/tasks.py::TaskLaunchService"
      via: "Read-only task endpoint exposes launch contract metadata without triggering execution"
      pattern: "TaskLaunchService"
    - from: "tests/test_commands.py"
      to: "nanobot/cli/commands.py"
      via: "Regression tests prove the existing CLI behavior remains unchanged while TUI startup is added separately"
      pattern: "runner.invoke"
---

<objective>
Add the local-first config and startup seam that make the new backend runnable without coupling it to the existing CLI, channel, or background runtime paths.

Purpose: Phase 17 is not complete until the isolated backend can be configured and started safely on localhost, expose its initial read-only browser-facing seams, and preserve all current nanobot/OpenGUI entry points.
Output: Dedicated TUI config schema, read-only session/runtime/task-contract route exposure, import-safe module startup wiring, config normalization helpers, and regression coverage proving existing CLI/config behavior stays intact.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-web-runtime-boundary/17-RESEARCH.md
@.planning/phases/17-web-runtime-boundary/17-VALIDATION.md
@.planning/phases/17-web-runtime-boundary/17-01-PLAN.md
@nanobot/config/schema.py
@nanobot/config/loader.py
@nanobot/cli/commands.py
@pyproject.toml
@tests/test_commands.py
@tests/test_config_paths.py

<interfaces>
From `nanobot/config/schema.py`:
```python
class GatewayConfig(Base):
    host: str = "0.0.0.0"
    port: int = 18790

class Config(BaseSettings):
    gateway: GatewayConfig = Field(default_factory=GatewayConfig)
    gui: GuiConfig | None = None
```

From `nanobot/config/loader.py`:
```python
def load_config(config_path: Path | None = None) -> Config: ...
def save_config(config: Config, config_path: Path | None = None) -> None: ...
```

From `nanobot/cli/commands.py`:
```python
def _load_runtime_config(config: str | None = None, workspace: str | None = None) -> Config: ...

@app.command()
def gateway(...): ...

@app.command()
def agent(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated local-first TUI config schema and validation coverage</name>
  <files>nanobot/config/schema.py, nanobot/config/loader.py, nanobot/tui/config.py, tests/test_tui_p17_config.py, tests/test_config_paths.py</files>
  <read_first>
    - nanobot/config/schema.py
    - nanobot/config/loader.py
    - tests/test_config_paths.py
    - .planning/phases/17-web-runtime-boundary/17-RESEARCH.md
    - .planning/phases/17-web-runtime-boundary/17-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: TUI defaults bind to `127.0.0.1` and an explicit port, not `0.0.0.0`.
    - Test 2: config loading validates a dedicated `tui` or `web` section through the existing `load_config()` path.
    - Test 3: config path and runtime-path helpers remain unchanged for non-TUI callers.
  </behavior>
  <action>
Add a dedicated config surface for the web backend in `nanobot/config/schema.py`. Use a narrow shape such as `TuiConfig` or `WebRuntimeConfig` with fields for:
- `enabled` or equivalent explicit startup gate if needed
- `host` defaulting to `127.0.0.1`
- `port` defaulting to a TUI-specific local port
- optional dev/static settings only if they are needed for safe startup semantics now

Then add `nanobot/tui/config.py` to normalize and expose the runtime settings the app/module launcher will consume. Keep it thin and local-first.

In `tests/test_tui_p17_config.py`, add these exact tests:
- `test_tui_defaults_bind_to_localhost`
- `test_load_config_accepts_explicit_tui_section`
- `test_tui_runtime_normalization_does_not_reuse_gateway_defaults`

Use `tests/test_config_paths.py` only for regression assertions if the new config path logic risks altering shared path behavior. Do not create a second config loader or repurpose `gateway.host` for the TUI server defaults.
  </action>
  <acceptance_criteria>
    - `rg -n "class TuiConfig|class WebRuntimeConfig" nanobot/config/schema.py` returns one match.
    - `rg -n "127\\.0\\.0\\.1|def test_tui_defaults_bind_to_localhost|load_config" tests/test_tui_p17_config.py nanobot/tui/config.py nanobot/config/schema.py` returns at least five total matches.
    - `uv run --extra dev pytest tests/test_tui_p17_config.py::test_tui_defaults_bind_to_localhost tests/test_tui_p17_config.py::test_load_config_accepts_explicit_tui_section tests/test_tui_p17_config.py::test_tui_runtime_normalization_does_not_reuse_gateway_defaults tests/test_config_paths.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p17_config.py::test_tui_defaults_bind_to_localhost tests/test_tui_p17_config.py::test_load_config_accepts_explicit_tui_section tests/test_tui_p17_config.py::test_tui_runtime_normalization_does_not_reuse_gateway_defaults tests/test_config_paths.py -q</automated>
  </verify>
  <done>The config model now includes a dedicated, local-first TUI runtime section that loads through the normal config path and stays isolated from gateway defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose read-only browser seams and wire import-safe startup for the TUI backend</name>
  <files>nanobot/tui/app.py, nanobot/tui/__main__.py, nanobot/tui/config.py, nanobot/tui/dependencies.py, nanobot/tui/routes/sessions.py, nanobot/tui/routes/runtime.py, nanobot/tui/routes/tasks.py, nanobot/tui/schemas/__init__.py, nanobot/tui/schemas/sessions.py, nanobot/tui/schemas/runtime.py, nanobot/tui/schemas/tasks.py, nanobot/tui/services/__init__.py, nanobot/tui/services/sessions.py, nanobot/tui/services/runtime.py, nanobot/tui/services/tasks.py, tests/test_tui_p17_runtime.py, tests/test_tui_p17_config.py, tests/test_commands.py</files>
  <read_first>
    - nanobot/tui/app.py
    - nanobot/tui/dependencies.py
    - nanobot/tui/contracts.py
    - nanobot/tui/config.py
    - nanobot/cli/commands.py
    - tests/test_tui_p17_runtime.py
    - tests/test_commands.py
    - tests/test_tui_p17_config.py
    - .planning/phases/17-web-runtime-boundary/17-01-PLAN.md
  </read_first>
  <behavior>
    - Test 1: `python -m nanobot.tui` or the equivalent module entry path resolves local-first config and constructs the FastAPI app through the existing app factory.
    - Test 2: the TUI startup seam does not mutate or replace `gateway`, `agent`, or existing config-loading CLI behavior.
    - Test 3: read-only `/sessions`, `/runtime`, and task-contract endpoints are exposed through explicit adapter seams, while startup wiring stays lazy enough that importing `nanobot.tui` does not trigger channel startup, template sync, or GUI runtime initialization.
  </behavior>
  <action>
Finish the backend boundary by exposing the read-only routes deferred from `17-01`:
- `routes/sessions.py`: read-only session metadata endpoint using `SessionService`
- `routes/runtime.py`: read-only runtime inspection shell using `RuntimeService`
- `routes/tasks.py`: read-only task-contract or capability endpoint using `TaskLaunchService`

Create the concrete schema/service layer deferred from Wave 1:
- `schemas/sessions.py`, `schemas/runtime.py`, `schemas/tasks.py`
- `services/sessions.py`, `services/runtime.py`, `services/tasks.py`
- update `dependencies.py` so the concrete services satisfy the typed contracts introduced in `17-01`

Wire those routers into `nanobot/tui/app.py` without broadening route handlers into runtime orchestrators. The task endpoint must expose typed launch metadata or supported contract fields only; it must not start `AgentLoop`, call `GuiSubagentTool.execute()`, or mutate runtime state.

Add an import-safe startup seam in `nanobot/tui/__main__.py` that:
- loads config through the existing loader path
- resolves TUI runtime settings via `nanobot.tui.config`
- builds the app through `create_app(...)`
- runs the server through uvicorn only at module-entry time, not import time

Regression expectations:
- do not add a `nanobot` Typer subcommand in this phase unless it can be done without touching unrelated command flows; prefer `python -m nanobot.tui` for Phase 17
- keep existing CLI/channel/background behavior unchanged
- if a tiny shared seam is needed, keep it narrow and avoid broad rewrites of `nanobot/cli/commands.py`

In `tests/test_tui_p17_config.py`, add:
- `test_tui_startup_wiring_uses_create_app_and_local_runtime_config`
- `test_tui_module_import_does_not_boot_cli_runtime`

In `tests/test_tui_p17_runtime.py`, add:
- `test_tui_routes_expose_read_only_session_runtime_and_task_contracts`

In `tests/test_commands.py`, add a regression test proving current `gateway` or `agent` commands keep their pre-existing config/default behavior after the TUI startup seam lands. The point is not to make CLI start the web app; it is to prove the new module startup path did not change CLI semantics.
  </action>
  <acceptance_criteria>
    - `rg -n "def main|uvicorn|create_app" nanobot/tui/__main__.py` returns at least three total matches.
    - `rg -n "router" nanobot/tui/routes/sessions.py nanobot/tui/routes/runtime.py nanobot/tui/routes/tasks.py` returns at least three total matches.
    - `rg -n "def test_tui_routes_expose_read_only_session_runtime_and_task_contracts" tests/test_tui_p17_runtime.py` returns one match.
    - `rg -n "test_tui_startup_wiring_uses_create_app_and_local_runtime_config|test_tui_module_import_does_not_boot_cli_runtime" tests/test_tui_p17_config.py` returns two matches.
    - `rg -n "gateway|agent|tui" tests/test_commands.py tests/test_tui_p17_config.py` returns at least six total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p17_runtime.py::test_tui_routes_expose_read_only_session_runtime_and_task_contracts tests/test_tui_p17_config.py::test_tui_startup_wiring_uses_create_app_and_local_runtime_config tests/test_tui_p17_config.py::test_tui_module_import_does_not_boot_cli_runtime tests/test_commands.py -q</automated>
  </verify>
  <done>The TUI backend exposes read-only session, runtime, and task-contract routes plus a runnable local-first module entry seam, with regression coverage proving existing nanobot CLI flows remain intact.</done>
</task>

</tasks>

<verification>
Keep the startup seam separate from the current host entry points. If a change would make the web runtime mandatory for normal CLI/chat/channel startup, it is out of scope for this phase.
</verification>

<success_criteria>
The TUI backend starts from a dedicated module entry with localhost-safe defaults, config stays explicit and isolated, and automated regressions prove existing nanobot entry points still behave as before.
</success_criteria>

<output>
After completion, create `.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-02-SUMMARY.md`
</output>
