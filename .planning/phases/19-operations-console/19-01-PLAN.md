---
phase: 19-operations-console
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_tui_p19_runtime.py
  - nanobot/tui/contracts.py
  - nanobot/tui/schemas/runtime.py
  - nanobot/tui/schemas/__init__.py
  - nanobot/tui/services/operations_registry.py
  - nanobot/tui/services/runtime.py
  - nanobot/tui/services/__init__.py
  - nanobot/tui/dependencies.py
  - nanobot/tui/routes/runtime.py
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Browser runtime inspection returns current session counts, active runs, and recent failure summaries without starting the CLI runtime."
    - "Runtime status exposes stable run identifiers and filtered failure metadata instead of raw filesystem browsing."
    - "All inspection APIs stay `run_id`-addressed; artifact paths remain internal implementation detail only."
    - "Phase 17 and Phase 18 browser/session boundaries stay intact while operations state is added under nanobot/tui."
  artifacts:
    - path: "tests/test_tui_p19_runtime.py"
      provides: "Wave 0 and green regression coverage for runtime aggregation endpoints"
      min_lines: 80
    - path: "nanobot/tui/services/operations_registry.py"
      provides: "App-local active run registry with typed status snapshots"
      contains: "class OperationsRegistry"
    - path: "nanobot/tui/routes/runtime.py"
      provides: "HTTP runtime status endpoints for operations inspection"
      exports: ["router"]
  key_links:
    - from: "nanobot/tui/routes/runtime.py"
      to: "nanobot/tui/services/runtime.py"
      via: "FastAPI dependency injection"
      pattern: "Depends\\(get_runtime_service\\)"
    - from: "nanobot/tui/services/runtime.py"
      to: "nanobot/session/manager.py"
      via: "SessionContract.list_sessions"
      pattern: "list_sessions"
    - from: "nanobot/tui/services/runtime.py"
      to: "nanobot/tui/services/operations_registry.py"
      via: "registry snapshot and recent-run summaries"
      pattern: "OperationsRegistry"
---

<objective>
Create the shared operations-status foundation for Phase 19 so the browser can inspect sessions, active runs, and recent failures from a stable `nanobot/tui` API.

Purpose: Satisfy `OPS-01` first, because typed launch and filtered trace detail both need a shared run identifier, status vocabulary, and app-local registry.
Output: Runtime contracts, schemas, registry-backed service wiring, and route-level tests for browser-safe status inspection.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-operations-console/19-RESEARCH.md
@.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-01-SUMMARY.md
@.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-02-SUMMARY.md
@.planning/phases/18-chat-workspace/18-chat-workspace-01-SUMMARY.md
@.planning/phases/18-chat-workspace/18-chat-workspace-02-SUMMARY.md
@.planning/phases/18-chat-workspace/18-chat-workspace-03-SUMMARY.md
@nanobot/tui/contracts.py
@nanobot/tui/dependencies.py
@nanobot/tui/routes/runtime.py
@nanobot/tui/services/runtime.py
@tests/test_tui_p17_runtime.py
@tests/test_tui_p17_config.py

<interfaces>
From `nanobot/tui/contracts.py`:
```python
@dataclass(slots=True)
class RuntimeInspectionContract:
    inspect_runtime: Callable[[], dict[str, Any]]
```

From `nanobot/tui/dependencies.py`:
```python
def get_runtime_inspection_contract() -> RuntimeInspectionContract: ...
def get_runtime_service() -> RuntimeService: ...
```

From `nanobot/tui/schemas/runtime.py`:
```python
class RuntimeInspectionResponse(BaseModel):
    status: str
    channel_runtime_booted: bool
    agent_loop_booted: bool
    task_launch_available: bool
```

From `nanobot/session/manager.py`:
```python
class SessionManager:
    def list_sessions(self) -> list[dict[str, Any]]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 runtime-status tests and typed inspection contracts</name>
  <files>tests/test_tui_p19_runtime.py, nanobot/tui/contracts.py, nanobot/tui/schemas/runtime.py, nanobot/tui/schemas/__init__.py</files>
  <behavior>
    - Test 1: `GET /runtime` returns an aggregate payload that includes session counts, active run summaries, and recent failures.
    - Test 2: Recent failure summaries expose only browser-safe fields such as `run_id`, `task_kind`, `status`, `summary`, `steps_taken`, and timestamps.
    - Test 3: Runtime inspection remains import-safe and does not boot `AgentLoop` or CLI startup paths.
  </behavior>
  <action>Create `tests/test_tui_p19_runtime.py` first with failing route/service expectations for the new aggregate runtime payload. Expand `nanobot/tui/contracts.py` and `nanobot/tui/schemas/runtime.py` with explicit status DTOs for session stats, active run summaries, and recent failure summaries. Keep the payload browser-safe: do not expose raw filesystem paths, arbitrary dict blobs, or prompt/model internals. Update `nanobot/tui/schemas/__init__.py` so later plans can import the new models without rediscovery.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p19_runtime.py::test_runtime_endpoint_reports_sessions_runs_and_recent_failures tests/test_tui_p19_runtime.py::test_runtime_recent_failures_are_filtered_to_browser_safe_fields -q</automated>
  </verify>
  <done>`tests/test_tui_p19_runtime.py` exists, fails red before implementation, and the runtime contract/schemas define the exact fields later tasks will satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the operations registry and aggregate runtime routes</name>
  <files>nanobot/tui/services/operations_registry.py, nanobot/tui/services/runtime.py, nanobot/tui/services/__init__.py, nanobot/tui/dependencies.py, nanobot/tui/routes/runtime.py</files>
  <behavior>
    - Test 1: Runtime routes report empty but well-typed run/failure collections before any web-triggered launch exists.
    - Test 2: Registry-backed active runs appear in runtime inspection without requiring persisted trace parsing to succeed.
    - Test 3: Recent failure summaries can be derived from existing artifacts or registry-completed runs while remaining filtered and stable.
  </behavior>
  <action>Implement a small app-local `OperationsRegistry` under `nanobot/tui/services/operations_registry.py` that tracks run id, task kind, status, timestamps, summary, steps, and optional trace references for browser-launched work. Update `nanobot/tui/services/runtime.py` so it aggregates `SessionManager.list_sessions()`, registry snapshots, and recent failure summaries into the new response model. Lock recent failure summaries to the shared allowlist that Phase 19 will reuse in trace detail endpoints: `run_id`, `task_kind`, `status`, `summary`, `steps_taken`, `started_at`, and `finished_at`. Wire lazy providers through `nanobot/tui/dependencies.py` and expand `nanobot/tui/routes/runtime.py` into explicit operations-focused endpoints such as `/runtime` and `/runtime/runs/{run_id}` if needed. Stay inside `nanobot/tui`; do not shell out to `opengui.cli`, do not boot the full nanobot channel runtime, and do not expose raw artifact paths as the public contract.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p19_runtime.py tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py -q</automated>
  </verify>
  <done>The browser has a stable runtime-status API with typed active-run and recent-failure summaries, and Phase 17 runtime/config regressions still pass unchanged.</done>
</task>

</tasks>

<verification>
Run the `19-01` slice from `19-VALIDATION.md`. If `uv run --extra dev pytest ...` is blocked locally, use the documented fallback `.venv/bin/python -m pytest tests/test_tui_p19_runtime.py tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py -q`.
</verification>

<success_criteria>
`OPS-01` is concretely supported at the list/status layer: browser clients can fetch runtime state, inspect current run summaries, and see recent failures through stable DTOs, all without broadening the nanobot or OpenGUI core runtime boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/19-operations-console/19-operations-console-01-SUMMARY.md`
</output>
