---
phase: 13-macos-background-execution
plan: "04"
type: execute
wave: 4
depends_on:
  - 13-01
  - 13-02
  - 13-03
files_modified:
  - .planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md
  - tests/test_opengui_p13_macos_display.py
  - tests/test_opengui_p4_desktop.py
  - tests/test_opengui_p5_cli.py
  - tests/test_opengui_p11_integration.py
autonomous: true
requirements:
  - MAC-01
  - MAC-02
  - MAC-03

must_haves:
  truths:
    - "The full Phase 13 regression slice proves the macOS contract without regressing shared runtime behavior."
    - "The phase leaves behind a concrete manual smoke checklist for real macOS hosts."
    - "Supported-host, denied-permission, and scaled-layout smoke scenarios are explicitly called out."
    - "Any regression failures discovered during closeout are fixed in the same wave rather than deferred silently."
  artifacts:
    - path: ".planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md"
      provides: "Operator checklist for real macOS validation"
      contains: "Supported Host"
    - path: "tests/test_opengui_p13_macos_display.py"
      provides: "Final regression-safe Phase 13 unit coverage"
      contains: "test_macos_target_surface_routing_keeps_observe_and_execute_aligned"
  key_links:
    - from: ".planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md::Supported Host"
      to: "opengui/cli.py::run_cli"
      via: "real-host smoke instructions exercise the planned CLI isolated path"
      pattern: "Supported Host"
---

<objective>
Close the phase with the full regression slice and a real-host smoke checklist for macOS support, remediation, and geometry validation.

Purpose: Phase 13 has platform-specific risk. The execution plan needs an explicit final wave that proves automated coverage is green and captures how to verify the real macOS behavior that CI cannot honestly simulate.
Output: full-suite green run for the Phase 13 slice and a `13-MANUAL-SMOKE.md` checklist covering supported, denied-permission, and scaled-layout cases.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-macos-background-execution/13-RESEARCH.md
@.planning/phases/13-macos-background-execution/13-VALIDATION.md
@tests/test_opengui_p13_macos_display.py
@tests/test_opengui_p4_desktop.py
@tests/test_opengui_p5_cli.py
@tests/test_opengui_p11_integration.py
@tests/test_opengui_p12_runtime_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and stabilize the full Phase 13 automated regression slice</name>
  <files>tests/test_opengui_p13_macos_display.py, tests/test_opengui_p4_desktop.py, tests/test_opengui_p5_cli.py, tests/test_opengui_p11_integration.py</files>
  <read_first>
    - .planning/phases/13-macos-background-execution/13-VALIDATION.md
    - tests/test_opengui_p13_macos_display.py
    - tests/test_opengui_p4_desktop.py
    - tests/test_opengui_p5_cli.py
    - tests/test_opengui_p11_integration.py
    - tests/test_opengui_p12_runtime_contracts.py
  </read_first>
  <action>
Run the full Phase 13 regression slice exactly as specified in `13-VALIDATION.md`:

`uv run pytest tests/test_opengui_p13_macos_display.py tests/test_opengui_p4_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q`

If any test fails, fix the production code or tests in this same wave instead of deferring:
- keep Linux/shared-runtime expectations green
- do not loosen assertions that protect MAC-02 remediation text or MAC-03 routing alignment
- prefer targeted fixture/helper updates over broad test rewrites

If the full slice is green immediately, leave the files untouched and record the clean pass in the summary output.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_opengui_p13_macos_display.py tests/test_opengui_p4_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q` exits 0.
    - `rg -n "macos_screen_recording_denied|cgvirtualdisplay|configure_target_display" tests/test_opengui_p13_macos_display.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py` returns at least three total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p13_macos_display.py tests/test_opengui_p4_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q</automated>
  </verify>
  <done>The full automated Phase 13 regression slice is green without regressing shared-runtime or Linux expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Add the real-host macOS smoke checklist</name>
  <files>.planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md</files>
  <read_first>
    - .planning/phases/13-macos-background-execution/13-RESEARCH.md
    - .planning/phases/13-macos-background-execution/13-VALIDATION.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
Create `.planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md` with these exact sections and scenario bullets:

1. `## Supported Host`
   - macOS support-floor host with PyObjC installed
   - Screen Recording granted
   - Accessibility granted
   - event-post capability available
   - expected result: isolated mode logs `macos_virtual_display_available`, creates the target surface, captures the correct monitor, and tears down cleanly

2. `## Denied Permissions`
   - deny Screen Recording
   - deny Accessibility
   - expected result: shared remediation mentions `System Settings > Privacy & Security`
   - verify the run blocks or warns before the first automation step

3. `## Scaled / Offset Layout`
   - place the isolated display at a non-zero origin
   - use a scaled/Retina layout
   - run a tap and drag scenario
   - expected result: screenshot dimensions match the target display and input lands on the same surface

4. `## Cleanup Checks`
   - stop the run mid-flight
   - rerun immediately
   - expected result: no stale target-display metadata and no double-stop failure

Keep the checklist phase-local. Do not add product documentation outside `.planning/` in this wave.
  </action>
  <acceptance_criteria>
    - `.planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md` exists.
    - `rg -n "^## Supported Host|^## Denied Permissions|^## Scaled / Offset Layout|^## Cleanup Checks" .planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md` returns four matches.
    - `rg -n "System Settings > Privacy & Security|macos_virtual_display_available|non-zero origin" .planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md` returns at least three matches.
  </acceptance_criteria>
  <verify>
    <manual>.planning/phases/13-macos-background-execution/13-MANUAL-SMOKE.md contains all four required sections and expected-result checks.</manual>
  </verify>
  <done>The phase now has an honest real-host smoke checklist for the macOS behaviors CI cannot fully simulate.</done>
</task>

</tasks>

<verification>
Use the full automated regression command first. Only after that passes, write the manual smoke checklist.
</verification>

<success_criteria>
The phase ends with both strong automated coverage and a concrete manual verification path for supported, denied-permission, and scaled-layout macOS hosts.
</success_criteria>

<output>
After completion, create `.planning/phases/13-macos-background-execution/13-macos-background-execution-04-SUMMARY.md`
</output>
