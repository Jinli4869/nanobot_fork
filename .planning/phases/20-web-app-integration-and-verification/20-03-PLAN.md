---
phase: 20-web-app-integration-and-verification
plan: "03"
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - nanobot/tui/__main__.py
  - nanobot/tui/web/__init__.py
  - pyproject.toml
  - tests/test_tui_p17_config.py
  - tests/test_tui_p20_entrypoints.py
  - tests/test_commands.py
  - nanobot/tui/web/src/app/shell.test.tsx
  - tests/test_tui_p20_static.py
  - README.md
  - .planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md
autonomous: true
requirements: [WEB-01, WEB-02, SHIP-01]
must_haves:
  truths:
    - "`python -m nanobot.tui` remains the canonical web-runtime seam, and the optional `nanobot-tui` script launches the same app without replacing the existing `nanobot` CLI."
    - "Automated regression proves the packaged web shell, backend routes, and existing CLI surface all still coexist after Phase 20."
    - "A user can follow one documented local workflow for development mode and one for built/local mode without guessing ports, build steps, or which CLI remains authoritative."
  artifacts:
    - path: "tests/test_tui_p20_entrypoints.py"
      provides: "Canonical startup regression coverage for `python -m nanobot.tui` and the packaged `nanobot-tui` script"
      contains: "main("
    - path: "pyproject.toml"
      provides: "Packaged web-asset inclusion and `nanobot-tui` startup metadata"
      contains: "nanobot-tui"
    - path: "tests/test_commands.py"
      provides: "CLI regression proving the existing nanobot entry point remains intact"
      contains: "nanobot"
    - path: ".planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md"
      provides: "Real-browser and CLI smoke path for milestone closeout"
      contains: "python -m nanobot.tui"
  key_links:
    - from: "nanobot/tui/__main__.py"
      to: "nanobot/tui/app.py"
      via: "canonical module entry builds runtime app with frontend enabled"
      pattern: "create_app"
    - from: "pyproject.toml"
      to: "nanobot/tui/web/dist"
      via: "build metadata includes the prebuilt frontend bundle in wheel and sdist outputs"
      pattern: "nanobot/tui/web/dist"
    - from: "tests/test_commands.py"
      to: "pyproject.toml"
      via: "existing CLI script preservation after adding nanobot-tui"
      pattern: "nanobot"
    - from: "README.md"
      to: "nanobot/tui/__main__.py"
      via: "documented startup commands for dev and built mode"
      pattern: "python -m nanobot.tui"
---

<objective>
Preserve the canonical packaged startup seam and close Phase 20 with regression coverage and operator-facing guidance so the new web workspace is runnable, verifiable, and clearly bounded against the existing CLI-first nanobot workflows.

Purpose: Satisfy the remaining packaged-delivery seam of `SHIP-01`, then turn the Phase 20 implementation into a shippable slice by proving the integrated web surface works and documenting how to run it locally in both dev and built modes.
Output: Packaging and entrypoint regressions, CLI protection tests, README startup instructions, and a phase-local manual smoke checklist.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md
@.planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md
@pyproject.toml
@nanobot/tui/__main__.py
@nanobot/tui/app.py
@tests/test_tui_p17_config.py
@tests/test_tui_p20_entrypoints.py
@tests/test_commands.py
@nanobot/tui/web/package.json
@nanobot/tui/web/src/app/router.tsx
@nanobot/tui/web/src/app/shell.tsx

<interfaces>
Validation commands for this closeout plan:
```text
npm --prefix nanobot/tui/web run build
npm --prefix nanobot/tui/web run test -- --run
uv run --extra dev pytest tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py tests/test_tui_p18_chat.py tests/test_tui_p18_streaming.py tests/test_tui_p19_runtime.py tests/test_tui_p19_tasks.py tests/test_tui_p19_traces.py tests/test_tui_p20_static.py tests/test_tui_p20_entrypoints.py tests/test_commands.py -q
```

Phase 20 closeout constraints:
```text
- keep implementation primarily under nanobot/tui
- do not broaden scope into auth, remote hosting, or core nanobot/OpenGUI rewrites
- preserve existing CLI-first behavior
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve the canonical startup seam and package the built frontend assets</name>
  <files>nanobot/tui/__main__.py, nanobot/tui/web/__init__.py, pyproject.toml, tests/test_tui_p17_config.py, tests/test_tui_p20_entrypoints.py, tests/test_commands.py</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, .planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-02-SUMMARY.md, nanobot/tui/__main__.py, nanobot/tui/app.py, nanobot/tui/config.py, pyproject.toml, tests/test_tui_p17_config.py, tests/test_tui_p20_entrypoints.py, tests/test_commands.py</read_first>
  <behavior>
    - Test 1: `python -m nanobot.tui` remains the canonical startup seam and enables both runtime routes and frontend serving.
    - Test 2: the packaged `nanobot-tui` script points at the same startup path without replacing the existing `nanobot` CLI.
    - Test 3: build metadata includes the frontend assets needed for installed-package lookup.
  </behavior>
  <action>Create or extend `tests/test_tui_p20_entrypoints.py`, `tests/test_tui_p17_config.py`, and `tests/test_commands.py` first with failing coverage for the packaged-startup and CLI-safety promises in `SHIP-01`. Then update `nanobot/tui/__main__.py` so `python -m nanobot.tui` builds the app with `include_runtime_routes=True` and `serve_frontend=True` while preserving the local-first host and port resolution from Phase 17. Add `nanobot/tui/web/__init__.py` so `importlib.resources` can resolve built assets safely. Update `pyproject.toml` to add `nanobot-tui = "nanobot.tui.__main__:main"` under `[project.scripts]` without touching the existing `nanobot` and `opengui` entries, and extend Hatch include rules so `nanobot/tui/web/dist/**`, `nanobot/tui/web/index.html`, and the minimum package files needed for asset lookup ship in wheel and sdist builds.</action>
  <acceptance_criteria>
    - `pyproject.toml` contains the literal script entry `nanobot-tui = "nanobot.tui.__main__:main"`.
    - `pyproject.toml` includes `nanobot/tui/web/dist` in build metadata.
    - `nanobot/tui/__main__.py` still loads config through `resolve_tui_runtime_config` and still calls `uvicorn.run`.
    - `tests/test_tui_p17_config.py` asserts frontend serving is enabled through the canonical startup seam.
    - `tests/test_tui_p20_entrypoints.py` asserts the same startup path remains canonical.
    - `tests/test_commands.py` contains a regression specifically protecting the existing `nanobot` CLI surface.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run build && uv run --extra dev pytest tests/test_tui_p20_entrypoints.py tests/test_tui_p17_config.py tests/test_commands.py -q</automated>
  </verify>
  <done>The packaged app can serve built frontend assets through the same canonical Python startup seam, and the existing CLI surface remains protected.</done>
</task>

<task type="auto">
  <name>Task 2: Add final integrated regressions, startup docs, and the real-browser smoke checklist</name>
  <files>nanobot/tui/web/src/app/shell.test.tsx, tests/test_tui_p20_static.py, README.md, .planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, nanobot/tui/web/src/app/router.tsx, nanobot/tui/web/src/app/shell.tsx, nanobot/tui/__main__.py, pyproject.toml, nanobot/tui/web/package.json, tests/test_tui_p20_static.py, tests/test_commands.py</read_first>
  <action>Expand regression coverage rather than broadening runtime code. Strengthen `nanobot/tui/web/src/app/shell.test.tsx` so it seeds router state with an active `sessionId` and `runId`, navigates across the SPA, and asserts that the same identifiers remain present in both the URL and visible UI. Extend `tests/test_tui_p20_static.py` with packaged deep-link regressions for `/chat/{session_id}` and `/operations?runId=...` after the built assets and packaged startup seam exist. Update `README.md` with exactly two supported startup workflows: dev mode as `python -m nanobot.tui` for the backend on `127.0.0.1:18791` plus `npm --prefix nanobot/tui/web run dev` for the frontend proxy, and built/local mode as `npm --prefix nanobot/tui/web run build` followed by `python -m nanobot.tui` or `nanobot-tui`. State explicitly that existing `nanobot` CLI usage remains unchanged. Create `.planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md` with numbered steps that open the browser workspace, create or resume a chat session, switch to operations and back without losing context, inspect one run, and then rerun one representative CLI command after the browser check.</action>
  <acceptance_criteria>
    - `nanobot/tui/web/src/app/shell.test.tsx` contains explicit assertions for both `sessionId` and `runId`.
    - `tests/test_tui_p20_static.py` asserts packaged `/chat/...` and `/operations?...` responses.
    - `README.md` contains the literal commands `python -m nanobot.tui`, `npm --prefix nanobot/tui/web run dev`, `npm --prefix nanobot/tui/web run build`, and `nanobot-tui`.
    - `README.md` states that existing `nanobot` CLI usage is unchanged.
    - `20-MANUAL-SMOKE.md` contains numbered browser and CLI verification steps.
    - The final Phase 20 validation command from `20-VALIDATION.md` passes.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "python -m nanobot.tui|npm --prefix nanobot/tui/web run dev|npm --prefix nanobot/tui/web run build|nanobot-tui" README.md && test -f .planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md && rg -n "1\\.|switch to operations and back|CLI" .planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md && npm --prefix nanobot/tui/web run build && npm --prefix nanobot/tui/web run test -- --run && uv run --extra dev pytest tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py tests/test_tui_p18_chat.py tests/test_tui_p18_streaming.py tests/test_tui_p19_runtime.py tests/test_tui_p19_tasks.py tests/test_tui_p19_traces.py tests/test_tui_p20_static.py tests/test_tui_p20_entrypoints.py tests/test_commands.py -q</automated>
  </verify>
  <done>The startup documentation is explicit, the smoke path is phase-local, and the full Phase 17-20 regression slice includes the packaged entrypoint and CLI-safety checks needed for closeout.</done>
</task>

</tasks>

<verification>
Run the full `20-03` command from `20-VALIDATION.md`. If `uv run --extra dev pytest ...` is still blocked by the local optional-build issue documented in Phases 18-19, use the `.venv/bin/python -m pytest ...` fallback with the same test file list after the frontend build and frontend tests succeed.
</verification>

<success_criteria>
Phase 20 is shippable: the packaged web startup seam is explicit and protected, the existing CLI surface remains intact, the web workspace has integrated regression proof, local startup commands are documented for both dev and built modes, and a human smoke checklist covers the real browser behavior automation cannot fully sample.
</success_criteria>

<output>
After completion, create `.planning/phases/20-web-app-integration-and-verification/20-web-app-integration-and-verification-03-SUMMARY.md`
</output>
