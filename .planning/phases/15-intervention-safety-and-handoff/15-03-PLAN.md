---
phase: 15-intervention-safety-and-handoff
plan: "03"
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - opengui/cli.py
  - nanobot/agent/tools/gui.py
  - opengui/backends/background.py
  - opengui/backends/windows_isolated.py
  - tests/test_opengui_p5_cli.py
  - tests/test_opengui_p11_integration.py
  - tests/test_opengui_p10_background.py
  - tests/test_opengui_p14_windows_desktop.py
autonomous: true
requirements:
  - SAFE-02
  - SAFE-03
  - SAFE-04

must_haves:
  truths:
    - "CLI and nanobot both route intervention through the same structured handler contract instead of bespoke logic."
    - "Background backends expose the safe target-surface metadata the host needs for human handoff."
    - "Host-visible intervention logging stays scrubbed while preserving enough metadata to debug the handoff path."
  artifacts:
    - path: "opengui/cli.py"
      provides: "CLI intervention handler and explicit resume confirmation flow"
      contains: "request_intervention"
    - path: "nanobot/agent/tools/gui.py"
      provides: "Nanobot intervention handler wiring that reuses the same core contract"
      contains: "intervention_handler"
    - path: "opengui/backends/background.py"
      provides: "Linux/macOS background target metadata for human handoff"
      contains: "get_intervention_target"
    - path: "opengui/backends/windows_isolated.py"
      provides: "Windows isolated-desktop target metadata for human handoff"
      contains: "get_intervention_target"
    - path: "tests/test_opengui_p5_cli.py"
      provides: "CLI intervention integration coverage"
      contains: "test_run_cli_intervention_flow_resumes_after_confirmation"
  key_links:
    - from: "opengui/cli.py::run_cli"
      to: "opengui/agent.py::GuiAgent"
      via: "the CLI supplies the host intervention handler when building the agent"
      pattern: "intervention_handler"
    - from: "nanobot/agent/tools/gui.py::_run_task"
      to: "opengui/agent.py::GuiAgent"
      via: "nanobot uses the same handler contract instead of forking pause logic"
      pattern: "intervention_handler"
    - from: "opengui/backends/background.py::get_intervention_target"
      to: "opengui/backends/windows_isolated.py::get_intervention_target"
      via: "both background backend families expose a safe handoff descriptor"
      pattern: "target"
---

<objective>
Wire the Phase 15 intervention contract through the real host entry points and expose backend handoff metadata so the user can enter the correct automation target before resuming.

Purpose: After Plan 02, the agent can pause safely. This plan makes that behavior usable from the CLI and nanobot and teaches the background backends how to describe the isolated target the human should enter.
Output: CLI/nanobot intervention handlers, background backend target metadata, and integration tests that prove resume flows and scrubbed logging work end to end.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-intervention-safety-and-handoff/15-RESEARCH.md
@.planning/phases/15-intervention-safety-and-handoff/15-VALIDATION.md
@opengui/agent.py
@opengui/interfaces.py
@opengui/cli.py
@nanobot/agent/tools/gui.py
@opengui/backends/background.py
@opengui/backends/windows_isolated.py
@tests/test_opengui_p5_cli.py
@tests/test_opengui_p11_integration.py
@tests/test_opengui_p10_background.py
@tests/test_opengui_p14_windows_desktop.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration tests for host-side intervention handlers and backend handoff metadata</name>
  <files>tests/test_opengui_p5_cli.py, tests/test_opengui_p11_integration.py, tests/test_opengui_p10_background.py, tests/test_opengui_p14_windows_desktop.py</files>
  <read_first>
    - tests/test_opengui_p5_cli.py
    - tests/test_opengui_p11_integration.py
    - tests/test_opengui_p10_background.py
    - tests/test_opengui_p14_windows_desktop.py
    - opengui/cli.py
    - nanobot/agent/tools/gui.py
    - opengui/backends/background.py
    - opengui/backends/windows_isolated.py
    - .planning/phases/15-intervention-safety-and-handoff/15-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: CLI intervention flow pauses, surfaces target metadata, waits for resume confirmation, and continues from a resumed result.
    - Test 2: Nanobot wires an intervention handler into `GuiAgent` and surfaces a structured non-leaking response on cancel/resume paths.
    - Test 3: Linux/macOS and Windows background backends expose safe target metadata without changing normal observe/execute behavior.
    - Test 4: Host-visible logs and payloads stay scrubbed.
  </behavior>
  <action>
Add these exact integration tests:

In `tests/test_opengui_p5_cli.py`:
- `test_run_cli_intervention_flow_resumes_after_confirmation`
- `test_run_cli_intervention_logs_are_scrubbed`

In `tests/test_opengui_p11_integration.py`:
- `test_gui_tool_intervention_flow_returns_structured_resume_result`
- `test_gui_tool_intervention_trace_payload_is_scrubbed`

In background-backend coverage:
- `tests/test_opengui_p10_background.py::test_background_backend_exposes_handoff_target_metadata`
- `tests/test_opengui_p14_windows_desktop.py::test_windows_isolated_backend_exposes_handoff_target_metadata`

The backend metadata tests must assert concrete keys:
- Linux/macOS wrapper: `display_id`, `monitor_index`, `width`, `height`
- Windows isolated backend: `display_id`, `desktop_name`, `width`, `height`
  </action>
  <acceptance_criteria>
    - `rg -n "test_run_cli_intervention_flow_resumes_after_confirmation|test_run_cli_intervention_logs_are_scrubbed" tests/test_opengui_p5_cli.py` returns two matches.
    - `rg -n "test_gui_tool_intervention_flow_returns_structured_resume_result|test_gui_tool_intervention_trace_payload_is_scrubbed" tests/test_opengui_p11_integration.py` returns two matches.
    - `rg -n "test_background_backend_exposes_handoff_target_metadata" tests/test_opengui_p10_background.py` returns one match.
    - `rg -n "test_windows_isolated_backend_exposes_handoff_target_metadata" tests/test_opengui_p14_windows_desktop.py` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p10_background.py tests/test_opengui_p14_windows_desktop.py -k "intervention or handoff_target_metadata" -q</automated>
  </verify>
  <done>The integration suite now pins host-side handoff behavior and the backend target metadata needed for human intervention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CLI/nanobot intervention handlers and backend handoff metadata</name>
  <files>opengui/cli.py, nanobot/agent/tools/gui.py, opengui/backends/background.py, opengui/backends/windows_isolated.py, tests/test_opengui_p5_cli.py, tests/test_opengui_p11_integration.py, tests/test_opengui_p10_background.py, tests/test_opengui_p14_windows_desktop.py</files>
  <read_first>
    - opengui/cli.py
    - nanobot/agent/tools/gui.py
    - opengui/backends/background.py
    - opengui/backends/windows_isolated.py
    - opengui/interfaces.py
    - opengui/agent.py
    - tests/test_opengui_p5_cli.py
    - tests/test_opengui_p11_integration.py
    - tests/test_opengui_p10_background.py
    - tests/test_opengui_p14_windows_desktop.py
  </read_first>
  <behavior>
    - Test 1: CLI passes an intervention handler into `GuiAgent` and requires explicit textual confirmation before resume.
    - Test 2: Nanobot passes the same handler contract into `GuiAgent` without duplicating the pause/resume logic.
    - Test 3: Background wrappers expose safe target metadata the handler can present to the user.
    - Test 4: Scrubbed intervention payloads survive the host path unchanged.
  </behavior>
  <action>
Implement host and backend wiring with these exact target states:

1. In `opengui/backends/background.py`:
   - add `def get_intervention_target(self) -> dict[str, Any]`
   - return a dict with:
     - `display_id`
     - `monitor_index`
     - `width`
     - `height`
     - `platform`
   - if `_display_info` is `None`, return an empty dict

2. In `opengui/backends/windows_isolated.py`:
   - add `def get_intervention_target(self) -> dict[str, Any]`
   - return a dict with:
     - `display_id`
     - `desktop_name`
     - `width`
     - `height`
     - `platform`
   - preserve all existing worker-routing behavior from Phase 14

3. In `opengui/cli.py`:
   - add a small async handler class or helper that implements `InterventionHandler`
   - print a short intervention message containing the scrubbed reason and target metadata
   - require exact user input `resume` to continue; any other response must resolve with `resume_confirmed=False`
   - pass the handler into `GuiAgent(...)` in `_execute_agent(...)`

4. In `nanobot/agent/tools/gui.py`:
   - add a private helper that builds an `InterventionHandler` for the current task
   - pass that handler into `GuiAgent(...)`
   - on cancellation, preserve the existing JSON result shape while surfacing an `intervention_cancelled`-style summary/error string instead of raw sensitive text

Do not broaden this plan into remote notification transports; Phase 15 only needs local, explicit confirmation semantics.
  </action>
  <acceptance_criteria>
    - `rg -n "def get_intervention_target" opengui/backends/background.py opengui/backends/windows_isolated.py` returns two matches.
    - `rg -n "resume_confirmed|request_intervention" opengui/cli.py nanobot/agent/tools/gui.py` returns at least four total matches.
    - `rg -n "display_id|desktop_name|monitor_index" opengui/backends/background.py opengui/backends/windows_isolated.py` returns at least five total matches.
    - `uv run pytest tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p10_background.py tests/test_opengui_p14_windows_desktop.py -k "intervention or handoff_target_metadata" -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p10_background.py tests/test_opengui_p14_windows_desktop.py -k "intervention or handoff_target_metadata" -q</automated>
  </verify>
  <done>The CLI, nanobot, and background backends all speak the same Phase 15 intervention contract and provide enough safe metadata for human handoff.</done>
</task>

</tasks>

<verification>
Preserve the working Phase 13/14 background execution paths. This plan adds handoff metadata and host wiring; it must not regress the existing isolated runtime behavior.
</verification>

<success_criteria>
The user can be handed into the correct automation target from either host entry point, resume explicitly, and receive only scrubbed intervention artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/15-intervention-safety-and-handoff/15-intervention-safety-and-handoff-03-SUMMARY.md`
</output>
