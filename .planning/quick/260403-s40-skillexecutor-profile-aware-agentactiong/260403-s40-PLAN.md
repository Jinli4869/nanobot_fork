# Quick Task 260403-s40: 把 SkillExecutor 这条链补成 profile-aware，把 _AgentActionGrounder 和 _AgentSubgoalRunner 接到同一个 agent_profiles seam 上

**Status:** In Progress
**Created:** 2026-04-03

## Goal

Extend the new OpenGUI agent-profile seam into the SkillExecutor path so profile-specific GUI model contracts are honored both in direct agent execution and in skill grounding / recovery loops.

## Tasks

### Task 1
- files: `opengui/agent.py`, `opengui/cli.py`, `nanobot/agent/tools/gui.py`
- action: Thread `agent_profile` through SkillExecutor construction and make `_AgentActionGrounder` plus `_AgentSubgoalRunner` use `agent_profiles` for prompt/tool selection, response normalization, and coordinate handling.
- verify: Targeted tests cover profile-aware grounding and subgoal execution plus wiring from CLI/nanobot entry points.
- done: SkillExecutor uses the same profile seam as the main GuiAgent loop.

### Task 2
- files: `tests/test_opengui.py`, `tests/test_opengui_p5_cli.py`, `tests/test_gui_skill_executor_wiring.py`
- action: Add failing regression tests first for qwen-style content-only grounding, subgoal parsing, CLI component wiring, and nanobot wiring propagation.
- verify: New tests fail before implementation and pass after the code changes.
- done: Regression coverage proves the skill path is profile-aware end to end.

### Task 3
- files: `.planning/STATE.md`, `.planning/quick/260403-s40-skillexecutor-profile-aware-agentactiong/260403-s40-SUMMARY.md`
- action: Record the quick task outcome, summary, verification commands, and implementation commit in planning artifacts.
- verify: Summary exists and STATE quick-task table includes the new row.
- done: Quick-task bookkeeping is complete and ready for commit.
