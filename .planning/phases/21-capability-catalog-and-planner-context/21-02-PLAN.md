---
phase: 21-capability-catalog-and-planner-context
plan: "02"
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - nanobot/agent/planning_memory.py
  - nanobot/agent/planner.py
  - nanobot/agent/loop.py
  - tests/test_opengui_p21_planner_context.py
  - tests/test_opengui_p8_planning.py
autonomous: true
requirements: [CAP-02]
must_haves:
  truths:
    - "Planner prompt includes routing-relevant memory hints only when they are relevant and small enough to fit the prompt budget."
    - "Unrelated long-term memory text does not get copied wholesale into the planner prompt."
    - "Empty or truncated routing-memory results remain safe and do not block planning or remove the live capability catalog."
  artifacts:
    - path: "nanobot/agent/planning_memory.py"
      provides: "Planner-only routing-memory DTOs and extraction/serialization guardrails"
      contains: "class PlanningMemoryHintExtractor"
    - path: "nanobot/agent/planner.py"
      provides: "Prompt assembly for bounded routing-memory hint sections"
      contains: "memory hints"
    - path: "nanobot/agent/loop.py"
      provides: "Runtime extraction of planner memory hints before `planner.plan(...)`"
      contains: "PlanningMemoryHintExtractor"
    - path: "tests/test_opengui_p21_planner_context.py"
      provides: "Regression coverage for hint extraction, exclusion, and truncation guardrails"
      contains: "memory_hint"
  key_links:
    - from: "nanobot/agent/planning_memory.py"
      to: "nanobot/agent/memory.py"
      via: "read-only extraction from `MemoryStore` files"
      pattern: "MemoryStore"
    - from: "nanobot/agent/loop.py"
      to: "nanobot/agent/planning_memory.py"
      via: "planner context includes extracted hints before the planning call"
      pattern: "PlanningMemoryHintExtractor"
    - from: "nanobot/agent/planner.py"
      to: "nanobot/agent/planning_memory.py"
      via: "hint serialization enforces count and character caps"
      pattern: "max_chars"
---

<objective>
Inject bounded routing-memory hints into planner context so the planner can bias route choice using prior evidence without inheriting the full agent memory prompt.

Purpose: Satisfy `CAP-02` while keeping prompt growth controlled and preserving the planner-only boundary established in `21-01`.
Output: Planner-only routing-memory DTOs and extractor, prompt guardrails, and regression coverage proving unrelated memory stays out.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-capability-catalog-and-planner-context/21-RESEARCH.md
@.planning/phases/21-capability-catalog-and-planner-context/21-VALIDATION.md
@docs/plans/2026-03-22-capability-aware-planner-routing-design.md
@nanobot/agent/memory.py
@nanobot/agent/context.py
@nanobot/agent/planner.py
@nanobot/agent/loop.py
@nanobot/agent/capabilities.py
@tests/test_opengui_p21_planner_context.py
@tests/test_opengui_p8_planning.py

<interfaces>
Phase 21-01 establishes these planner-facing contracts that 21-02 must extend, not replace:
```python
@dataclass(frozen=True)
class PlanningContext:
    catalog: CapabilityCatalog
    memory_hints: tuple["PlanningMemoryHint", ...] = ()
```

```python
class TaskPlanner:
    async def plan(
        self,
        task: str,
        *,
        context: str = "",
        planning_context: PlanningContext | None = None,
    ) -> PlanNode: ...
```

Memory boundary to preserve:
```python
class MemoryStore:
    def read_long_term(self) -> str: ...
    def get_memory_context(self) -> str: ...
    history_file: Path
```

Phase 21 constraints:
```text
- do not reuse ContextBuilder.build_system_prompt() for planner prompts
- do not paste raw MEMORY.md or HISTORY.md into the planner prompt
- keep hint extraction read-only; route-outcome persistence belongs to Phase 23
- keep empty-hint behavior safe and leave router execution semantics unchanged
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define routing-memory hint contracts and failing exclusion/guardrail coverage</name>
  <files>nanobot/agent/planning_memory.py, tests/test_opengui_p21_planner_context.py</files>
  <read_first>.planning/phases/21-capability-catalog-and-planner-context/21-RESEARCH.md, .planning/phases/21-capability-catalog-and-planner-context/21-VALIDATION.md, docs/plans/2026-03-22-capability-aware-planner-routing-design.md, nanobot/agent/memory.py, nanobot/agent/context.py, nanobot/agent/planner.py, nanobot/agent/capabilities.py, tests/test_opengui_p21_planner_context.py</read_first>
  <behavior>
    - Test 1: `PlanningMemoryHintExtractor` returns only routing-relevant hints tied to known catalog route IDs or fallback guidance and ignores unrelated narrative memory.
    - Test 2: hint serialization caps individual hint length and overall hint count before prompt wiring begins.
    - Test 3: an empty memory store or memory text with no route evidence returns an empty tuple instead of broad prompt text.
  </behavior>
  <action>Create `nanobot/agent/planning_memory.py` with planner-only dataclasses `PlanningMemoryHint` and `PlanningMemoryHintExtractor`. Make the extractor accept `task: str`, `catalog: CapabilityCatalog`, and a workspace or `MemoryStore`, then read `MEMORY.md` plus a bounded tail of `HISTORY.md` in read-only mode. Write failing tests in `tests/test_opengui_p21_planner_context.py` that only accept hints when the source text includes a known catalog `route_id` or an alias tied to success/failure language such as `worked`, `succeeded`, `failed`, or `fallback`. Include explicit negative coverage showing that general user preferences or unrelated notes do not become planner hints. Define prompt-budget constants now: max 5 hints, max 160 characters per rendered hint, and a total serialized hint budget of 900 characters. Do not touch `ContextBuilder.build_system_prompt()` and do not add any write path to `MemoryStore`.</action>
  <acceptance_criteria>
    - `nanobot/agent/planning_memory.py` contains `class PlanningMemoryHint` and `class PlanningMemoryHintExtractor`.
    - `nanobot/agent/planning_memory.py` contains the literal numeric guardrails `5`, `160`, and `900` in named constants or serializer code.
    - `tests/test_opengui_p21_planner_context.py` contains coverage names matching `memory_hint`, `guardrail`, and `excludes`.
    - `tests/test_opengui_p21_planner_context.py` includes at least one negative assertion for unrelated memory content.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_opengui_p21_planner_context.py -k "memory_hint or guardrail or excludes"</automated>
  </verify>
  <done>The routing-memory contracts are explicit, failing tests prove exclusion and budget behavior, and Phase 21 still has no raw-memory prompt path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire bounded memory hints into the planner context and preserve prompt-size safety</name>
  <files>nanobot/agent/planning_memory.py, nanobot/agent/planner.py, nanobot/agent/loop.py, tests/test_opengui_p21_planner_context.py, tests/test_opengui_p8_planning.py</files>
  <read_first>.planning/phases/21-capability-catalog-and-planner-context/21-RESEARCH.md, .planning/phases/21-capability-catalog-and-planner-context/21-VALIDATION.md, nanobot/agent/planning_memory.py, nanobot/agent/planner.py, nanobot/agent/loop.py, nanobot/agent/memory.py, nanobot/agent/context.py, nanobot/agent/capabilities.py, tests/test_opengui_p21_planner_context.py, tests/test_opengui_p8_planning.py</read_first>
  <behavior>
    - Test 1: `AgentLoop._plan_and_execute()` extracts routing-memory hints and passes them into `PlanningContext(memory_hints=...)` alongside the live catalog from `21-01`.
    - Test 2: `TaskPlanner` renders a separate routing-memory block only when hints exist, truncates it to the configured budget, and leaves unrelated memory out of the prompt.
    - Test 3: planner logging and existing Phase 8 plan execution tests remain green when memory hints are absent.
  </behavior>
  <action>Implement `PlanningMemoryHintExtractor.build(...)` and a compact serializer such as `PlanningMemoryHint.to_prompt_line(max_chars: int = 160) -> str`. Update `TaskPlanner._build_system_prompt(...)` so it accepts the full `PlanningContext`, renders the capability catalog first, then appends a separate `Routing memory hints:` section only when `memory_hints` is non-empty, truncating after 5 lines or 900 total characters and appending one explicit omission marker when extra hints are dropped. In `AgentLoop._plan_and_execute()`, instantiate the extractor right before planning and pass the resulting hints through the existing planning-context seam from `21-01`. Extend tests so prompt snapshots prove unrelated memory text never appears, empty hints skip the section cleanly, and route-aware logging still works. Do not call `MemoryStore.get_memory_context()` and do not modify `TreeRouter` dispatch behavior.</action>
  <acceptance_criteria>
    - `nanobot/agent/loop.py` contains `PlanningMemoryHintExtractor` and passes `memory_hints=` into planner context construction.
    - `nanobot/agent/planner.py` contains the literal heading `Routing memory hints:` and an omission marker for truncated hints.
    - `nanobot/agent/planner.py` does not call `get_memory_context(`.
    - `tests/test_opengui_p21_planner_context.py` contains assertions that unrelated memory text is absent from the built prompt.
    - `tests/test_opengui_p8_planning.py` still contains route-aware log coverage for `_plan_and_execute()`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_opengui_p8_planning.py tests/test_mcp_tool.py tests/test_opengui_p21_planner_context.py -k "memory_hint or guardrail or route or plan_and_execute"</automated>
  </verify>
  <done>The planner receives small, routing-relevant memory hints only when warranted, prompt growth stays capped, unrelated memory remains excluded, and existing planning regressions stay intact.</done>
</task>

</tasks>

<verification>
Run the full Phase 21 quick slice from `21-VALIDATION.md`: `uv run pytest -q tests/test_opengui_p8_planning.py tests/test_mcp_tool.py tests/test_opengui_p21_planner_context.py`. Confirm the prompt builder never includes raw `MEMORY.md` text outside the dedicated bounded hint section.
</verification>

<success_criteria>
`CAP-02` is satisfied when routing-relevant memory hints are extracted through a planner-only read path, injected under explicit count/size guardrails, and proven not to dump unrelated memory into the planner prompt.
</success_criteria>

<output>
After completion, create `.planning/phases/21-capability-catalog-and-planner-context/21-02-SUMMARY.md`
</output>
