---
phase: 16-host-integration-and-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - opengui/cli.py
  - tests/test_opengui_p5_cli.py
autonomous: true
requirements:
  - INTG-05
  - TEST-V12-01

must_haves:
  truths:
    - "CLI reports the shared background decision tokens before agent execution across macOS and Windows paths."
    - "CLI keeps cleanup and handoff evidence visible while still scrubbing sensitive intervention reason text."
    - "Phase 16 adds CLI-specific regression coverage for host-entry parity instead of relying only on earlier phase tests."
  artifacts:
    - path: "opengui/cli.py"
      provides: "CLI host-entry formatting and decision ordering for Phase 16 parity"
      contains: "resolve_target_app_class"
    - path: "tests/test_opengui_p5_cli.py"
      provides: "CLI regression coverage for background decision tokens and scrubbed lifecycle evidence"
      contains: "test_run_cli_background_decision_tokens_stay_consistent_across_supported_hosts"
  key_links:
    - from: "opengui/cli.py::run_cli"
      to: "opengui/backends/background_runtime.py::resolve_run_mode"
      via: "CLI logs and surfaces the shared mode decision before blocked, fallback, or isolated execution proceeds"
      pattern: "background runtime resolved:"
    - from: "opengui/cli.py::_CliInterventionHandler"
      to: "opengui/agent.py::GuiAgent._scrub_for_log"
      via: "CLI handoff output preserves safe target metadata while redacting sensitive reason text"
      pattern: "<redacted:intervention_reason>"
---

<objective>
Tighten the CLI host path so macOS and Windows background capability decisions, cleanup evidence, and intervention handoff output stay consistent with the shared runtime contract.

Purpose: The milestone goal requires CLI to expose honest configuration and mode reporting before automation begins. This plan makes the CLI path the reference-quality host entry point for that contract.
Output: CLI regression tests plus any targeted `opengui/cli.py` updates needed to keep reason codes, remediation copy, cleanup tokens, and scrubbed intervention output aligned.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-host-integration-and-verification/16-RESEARCH.md
@.planning/phases/16-host-integration-and-verification/16-VALIDATION.md
@opengui/backends/background_runtime.py
@opengui/cli.py
@tests/test_opengui_p5_cli.py
@tests/test_opengui_p13_macos_display.py
@tests/test_opengui_p14_windows_desktop.py
@tests/test_opengui_p15_intervention.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI parity tests for background decision tokens and scrubbed lifecycle evidence</name>
  <files>tests/test_opengui_p5_cli.py</files>
  <read_first>
    - tests/test_opengui_p5_cli.py
    - opengui/cli.py
    - opengui/backends/background_runtime.py
    - tests/test_opengui_p13_macos_display.py
    - tests/test_opengui_p14_windows_desktop.py
    - tests/test_opengui_p15_intervention.py
    - .planning/phases/16-host-integration-and-verification/16-RESEARCH.md
    - .planning/phases/16-host-integration-and-verification/16-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: CLI emits the shared `owner=cli`, `mode=...`, and `reason=...` decision tokens before the first agent step for supported, fallback, and blocked paths.
    - Test 2: Windows isolated cleanup or startup-failure evidence stays visible to the operator through CLI output instead of being collapsed into generic prose.
    - Test 3: Intervention handoff output keeps safe target metadata while redacting the reason text as `<redacted:intervention_reason>`.
  </behavior>
  <action>
Extend `tests/test_opengui_p5_cli.py` with these exact tests:

- `test_run_cli_background_decision_tokens_stay_consistent_across_supported_hosts`
- `test_run_cli_handoff_and_cleanup_tokens_stay_visible_without_leaking_sensitive_reason`

The first test must cover at least these concrete CLI pre-agent scenarios:
- macOS isolated support with `reason_code="macos_virtual_display_available"`
- Windows unsupported app class with `reason_code="windows_app_class_unsupported"`
- a blocked or required-isolation failure path that still logs the decision tokens before returning

The assertions must check exact log-line fragments such as:
- `owner=cli`
- `mode=isolated`
- `mode=blocked` or `mode=fallback`
- `reason=macos_virtual_display_available`
- `reason=windows_app_class_unsupported`

The second test must exercise:
- a Windows cleanup-bearing failure string containing `cleanup_reason=startup_failed`
- an intervention handoff with safe target keys such as `display_id` or `desktop_name`
- the exact redaction token `<redacted:intervention_reason>`

Do not create a new CLI-only helper file in this task; keep the coverage in `tests/test_opengui_p5_cli.py`.
  </action>
  <acceptance_criteria>
    - `rg -n "def test_run_cli_background_decision_tokens_stay_consistent_across_supported_hosts" tests/test_opengui_p5_cli.py` returns one match.
    - `rg -n "def test_run_cli_handoff_and_cleanup_tokens_stay_visible_without_leaking_sensitive_reason" tests/test_opengui_p5_cli.py` returns one match.
    - `rg -n "owner=cli|macos_virtual_display_available|windows_app_class_unsupported|cleanup_reason=startup_failed|<redacted:intervention_reason>" tests/test_opengui_p5_cli.py` returns at least five total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p5_cli.py::test_run_cli_background_decision_tokens_stay_consistent_across_supported_hosts tests/test_opengui_p5_cli.py::test_run_cli_handoff_and_cleanup_tokens_stay_visible_without_leaking_sensitive_reason -q</automated>
  </verify>
  <done>The CLI suite now pins the Phase 16 contract around decision tokens, cleanup evidence, and scrubbed intervention output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align CLI mode reporting and lifecycle evidence with the shared runtime contract</name>
  <files>opengui/cli.py, tests/test_opengui_p5_cli.py</files>
  <read_first>
    - opengui/cli.py
    - opengui/backends/background_runtime.py
    - tests/test_opengui_p5_cli.py
    - .planning/phases/16-host-integration-and-verification/16-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `run_cli()` logs the background decision before blocked/fallback/isolation branching and keeps the shared reason/remediation vocabulary intact.
    - Test 2: Windows lifecycle errors and target-surface metadata remain visible when CLI surfaces failures or handoff information.
    - Test 3: Sensitive intervention reason text is never printed raw by CLI output.
  </behavior>
  <action>
Update `opengui/cli.py` to preserve these exact target states:

1. Keep `resolve_target_app_class(args, sys_platform=...)` returning:
   - `None` for non-background, non-local, or non-Windows paths
   - `args.target_app_class or "classic-win32"` for Windows background-local runs

2. In `run_cli()`:
   - ensure the resolved runtime decision is logged via `log_mode_resolution(...)` before any `blocked`, `fallback`, or `isolated` branch returns
   - keep the `decision.message` text from `background_runtime` intact instead of rephrasing it into host-local copy
   - preserve technical lifecycle tokens such as `cleanup_reason=...`, `backend_name=...`, `display_id=...`, or `desktop_name=...` when surfacing background failures or handoff context

3. In `_CliInterventionHandler.request_intervention(...)` and its helpers:
   - keep safe target keys only
   - print the scrubbed reason value, not the raw request reason
   - never leak raw intervention text even when other lifecycle tokens are preserved

Do not broaden this plan into nanobot parity or shared helper extraction. Keep the work scoped to the CLI host path.
  </action>
  <acceptance_criteria>
    - `rg -n "resolve_target_app_class|log_mode_resolution|_CliInterventionHandler|cleanup_reason" opengui/cli.py` returns at least four total matches.
    - `rg -n "<redacted:intervention_reason>|classic-win32|desktop_name|display_id" opengui/cli.py tests/test_opengui_p5_cli.py` returns at least six total matches.
    - `uv run pytest tests/test_opengui_p5_cli.py::test_run_cli_background_decision_tokens_stay_consistent_across_supported_hosts tests/test_opengui_p5_cli.py::test_run_cli_handoff_and_cleanup_tokens_stay_visible_without_leaking_sensitive_reason tests/test_opengui_p5_cli.py::test_run_cli_warns_for_windows_unsupported_app_class_before_agent_start -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p5_cli.py::test_run_cli_background_decision_tokens_stay_consistent_across_supported_hosts tests/test_opengui_p5_cli.py::test_run_cli_handoff_and_cleanup_tokens_stay_visible_without_leaking_sensitive_reason tests/test_opengui_p5_cli.py::test_run_cli_warns_for_windows_unsupported_app_class_before_agent_start -q</automated>
  </verify>
  <done>The CLI host path now exposes Phase 16 capability and lifecycle evidence without regressing privacy or shared-runtime semantics.</done>
</task>

</tasks>

<verification>
Keep the CLI path aligned to `background_runtime` as the source of truth. Do not invent new reason-code or remediation copy inside `opengui/cli.py`.
</verification>

<success_criteria>
CLI exposes honest background configuration and mode reporting for macOS and Windows, while preserving cleanup and intervention evidence in a scrubbed operator-safe form.
</success_criteria>

<output>
After completion, create `.planning/phases/16-host-integration-and-verification/16-host-integration-and-verification-01-SUMMARY.md`
</output>

