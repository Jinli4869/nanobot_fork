---
phase: 17-web-runtime-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - nanobot/tui/__init__.py
  - nanobot/tui/app.py
  - nanobot/tui/dependencies.py
  - nanobot/tui/routes/__init__.py
  - nanobot/tui/routes/health.py
  - nanobot/tui/contracts.py
  - tests/test_tui_p17_runtime.py
autonomous: true
requirements:
  - ISO-01
must_haves:
  truths:
    - "The web backend surface is importable from `nanobot.tui` without booting the CLI, channels, or OpenGUI runtime."
    - "A FastAPI app shell under `nanobot/tui` exists with only a minimal health route in Wave 1, so later routes can build on a stable foundation."
    - "Typed lazy adapter contracts exist for session inspection, runtime inspection, and future task-launch seams before any mutating API is added."
  artifacts:
    - path: "nanobot/tui/app.py"
      provides: "FastAPI app factory and isolated router registration"
      contains: "create_app"
    - path: "nanobot/tui/dependencies.py"
      provides: "Dependency-provider seams for TUI services"
      contains: "get_session_contract"
    - path: "nanobot/tui/contracts.py"
      provides: "Typed lazy contracts for future session, runtime, and task-launch phases"
      contains: "TaskLaunchContract"
    - path: "tests/test_tui_p17_runtime.py"
      provides: "Wave 0 and regression coverage for the isolated app shell"
      contains: "test_create_app_builds_isolated_tui_routes"
  key_links:
    - from: "nanobot/tui/dependencies.py"
      to: "nanobot/tui/contracts.py"
      via: "Dependency providers expose typed contracts without forcing concrete service construction in Wave 1"
      pattern: "get_session_contract"
    - from: "nanobot/tui/contracts.py"
      to: "nanobot/session/manager.py::SessionManager"
      via: "Session contract is shaped around persisted session semantics already owned by nanobot"
      pattern: "SessionManager|Protocol|TypedDict"
    - from: "nanobot/tui/contracts.py"
      to: "nanobot/agent/tools/gui.py"
      via: "Future task-launch seam is typed up front without triggering GUI execution in Phase 17"
      pattern: "TaskLaunchContract"
---

<objective>
Create the smallest stable `nanobot/tui` backend foundation: package layout, FastAPI app shell, and shared adapter contracts with Wave 0 tests that lock the boundary before Phase 18/19 add feature depth.

Purpose: Phase 17 succeeds only if the web surface lives under `nanobot/tui` and future React/Vite work can talk to stable backend contracts without forcing broad nanobot or OpenGUI refactors.
Output: A new `nanobot/tui` package, FastAPI app factory, minimal health route, typed session/runtime/task contracts, dependency wiring in `pyproject.toml`, and a Wave 0 runtime test file.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-web-runtime-boundary/17-RESEARCH.md
@.planning/phases/17-web-runtime-boundary/17-VALIDATION.md
@pyproject.toml
@nanobot/config/schema.py
@nanobot/config/loader.py
@nanobot/session/manager.py
@nanobot/agent/loop.py
@nanobot/agent/tools/gui.py
@nanobot/cli/commands.py
@opengui/cli.py

<interfaces>
From `nanobot/session/manager.py`:
```python
class SessionManager:
    def __init__(self, workspace: Path):
        self.sessions_dir = ensure_dir(self.workspace / "sessions")

    def get_or_create(self, key: str) -> Session: ...
    def save(self, session: Session) -> None: ...
    def invalidate(self, key: str) -> None: ...
    def list_sessions(self) -> list[dict[str, Any]]: ...
```

From `nanobot/agent/loop.py`:
```python
class AgentLoop:
    def __init__(
        self,
        bus: MessageBus,
        provider: LLMProvider,
        workspace: Path,
        ...,
        session_manager: SessionManager | None = None,
        gui_config: GuiConfig | None = None,
    ): ...
```

From `nanobot/config/schema.py`:
```python
class Config(BaseSettings):
    agents: AgentsConfig = Field(default_factory=AgentsConfig)
    gateway: GatewayConfig = Field(default_factory=GatewayConfig)
    gui: GuiConfig | None = None

    @property
    def workspace_path(self) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 runtime tests and dependency hooks for the isolated TUI backend</name>
  <files>tests/test_tui_p17_runtime.py, pyproject.toml</files>
  <read_first>
    - .planning/phases/17-web-runtime-boundary/17-RESEARCH.md
    - .planning/phases/17-web-runtime-boundary/17-VALIDATION.md
    - pyproject.toml
    - tests/test_commands.py
    - tests/test_session_manager_history.py
    - nanobot/session/manager.py
    - nanobot/cli/commands.py
  </read_first>
  <behavior>
    - Test 1: `create_app()` builds the TUI-only route table without importing or executing CLI startup behavior.
    - Test 2: typed lazy contracts exist for session inspection and runtime inspection without introducing a second store.
    - Test 3: a typed lazy task-launch adapter contract exists for future operations flows without exposing a mutating endpoint yet.
  </behavior>
  <action>
Create `tests/test_tui_p17_runtime.py` first as the Wave 0 contract file required by `17-VALIDATION.md`.

Add these exact tests:
- `test_create_app_builds_isolated_tui_routes`
- `test_tui_service_contracts_are_lazy_and_typed`
- `test_task_launch_contract_is_declared_without_mutating_routes`

The test file should:
- guard imports if needed using the repo's Phase 09 Wave 0 pattern only where production files do not exist yet
- assert route presence for `/health` and assert that app construction does not yet require `/sessions`, `/runtime`, or task-launch routers in Wave 1
- assert app construction does not call CLI helpers such as `sync_workspace_templates`, `ChannelManager`, or direct `AgentLoop(...)`
    - assert the session contract reuses `SessionManager.list_sessions()` output shape rather than inventing a new persistence layer
    - assert runtime and task-launch contracts are typed and lazy, with no route-level mutation behavior exposed yet

In `pyproject.toml`, add the minimal Python dependency seam needed for this plan:
- optional extra `web = ["fastapi>=0.11x", "uvicorn[standard]>=0.3x"]` or equivalent repo-consistent bounds
- add the same packages to the `dev` extra so every Phase 17 verify command can run consistently as `uv run --extra dev pytest ...`

Do not add any React/Vite or Node dependencies in this task. Phase 17 is backend boundary work only.
  </action>
  <acceptance_criteria>
    - `rg -n "def test_create_app_builds_isolated_tui_routes" tests/test_tui_p17_runtime.py` returns one match.
    - `rg -n "def test_tui_service_contracts_are_lazy_and_typed" tests/test_tui_p17_runtime.py` returns one match.
    - `rg -n "def test_task_launch_contract_is_declared_without_mutating_routes" tests/test_tui_p17_runtime.py` returns one match.
    - `rg -n "fastapi|uvicorn" pyproject.toml` returns at least two total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p17_runtime.py::test_create_app_builds_isolated_tui_routes tests/test_tui_p17_runtime.py::test_tui_service_contracts_are_lazy_and_typed tests/test_tui_p17_runtime.py::test_task_launch_contract_is_declared_without_mutating_routes -q</automated>
  </verify>
  <done>Wave 0 runtime tests exist and the repo has the minimal Python dependency wiring required to implement the isolated TUI backend shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the `nanobot/tui` app shell and typed contracts</name>
  <files>nanobot/tui/__init__.py, nanobot/tui/app.py, nanobot/tui/dependencies.py, nanobot/tui/routes/__init__.py, nanobot/tui/routes/health.py, nanobot/tui/contracts.py, tests/test_tui_p17_runtime.py</files>
  <read_first>
    - nanobot/session/manager.py
    - nanobot/agent/loop.py
    - nanobot/agent/tools/gui.py
    - nanobot/cli/commands.py
    - opengui/cli.py
    - tests/test_tui_p17_runtime.py
    - .planning/phases/17-web-runtime-boundary/17-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `nanobot.tui.app.create_app()` returns a FastAPI app with isolated router registration and no CLI/channel side effects on import.
    - Test 2: session and runtime contracts are typed, lazy, and adapter-shaped rather than deep-importing runtime work into routes.
    - Test 3: the task-launch contract is explicit enough for future operations work while remaining read-only and non-executing in this phase.
  </behavior>
  <action>
Implement the initial backend shell under `nanobot/tui` with this package shape:

- `app.py`: own the `create_app(...)` factory and router inclusion
- `dependencies.py`: own service providers and any lazy config/session dependency builders
- `routes/health.py`: trivial health/version endpoint for smoke checks
- `contracts.py`: typed lazy contracts for future session, runtime boundary metadata, and task launch capability inspection; no execution yet

Implementation constraints:
- keep all web-facing code primarily under `nanobot/tui`
- do not move existing runtime logic into the new package
- do not instantiate `AgentLoop`, channels, or GUI runs directly inside route handlers
- keep Wave 1 routes minimal: health only
- express session/runtime/task seams as typed contracts and dependency providers now; defer concrete service implementations plus read-only route exposure for those seams to `17-02`
- keep endpoint scope read-only in this plan; no chat mutation, no task launch, no streaming

Use FastAPI idioms already supported by the repo's typing and pytest setup. Favor plain dependency functions and small service classes over a global singleton container.
  </action>
  <acceptance_criteria>
    - `rg -n "def create_app|FastAPI" nanobot/tui/app.py` returns at least two total matches.
    - `rg -n "SessionContract|RuntimeInspectionContract|TaskLaunchContract" nanobot/tui/contracts.py nanobot/tui/dependencies.py` returns at least five total matches.
    - `rg -n "/health|TaskLaunchContract|SessionManager" nanobot/tui tests/test_tui_p17_runtime.py` returns at least six total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p17_runtime.py -q</automated>
  </verify>
  <done>The backend boundary now exists under `nanobot/tui` with a usable FastAPI app shell, a minimal health route, and explicit typed contracts for later session, runtime, and task-launch surfaces.</done>
</task>

</tasks>

<verification>
Keep the TUI backend as an adapter layer. If an endpoint would require copying existing nanobot or OpenGUI runtime behavior into `nanobot/tui`, stop and route through a thin contract instead.
</verification>

<success_criteria>
`nanobot/tui` owns the Phase 17 backend shell, `create_app()` is testable in isolation, and future browser features have explicit typed contracts for health, session metadata, runtime inspection, and task-launch seams without destabilizing current host flows.
</success_criteria>

<output>
After completion, create `.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-01-SUMMARY.md`
</output>
