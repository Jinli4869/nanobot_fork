---
phase: 01-p1-unit-tests
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/test_opengui_p1_trajectory.py
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "TrajectoryRecorder produces JSONL with metadata event first and result event last"
    - "TrajectoryRecorder records correct number of steps between metadata and result"
    - "TrajectoryRecorder start() creates the output file and returns its path"
    - "TrajectoryRecorder.set_phase() changes the execution phase for subsequent steps"
    - "TrajectorySummarizer returns a non-empty string summary from LLM"
  artifacts:
    - path: "tests/test_opengui_p1_trajectory.py"
      provides: "Unit tests for TrajectoryRecorder and TrajectorySummarizer"
      min_lines: 60
  key_links:
    - from: "tests/test_opengui_p1_trajectory.py"
      to: "opengui/trajectory/recorder.py"
      via: "import TrajectoryRecorder, ExecutionPhase"
      pattern: "from opengui\\.trajectory\\.recorder import"
    - from: "tests/test_opengui_p1_trajectory.py"
      to: "opengui/trajectory/summarizer.py"
      via: "import TrajectorySummarizer"
      pattern: "from opengui\\.trajectory\\.summarizer import"
---

<objective>
Create unit tests for the trajectory module: TrajectoryRecorder JSONL event sequencing and phase tracking, TrajectorySummarizer LLM-based output format.

Purpose: TEST-04 requires at least one test each for TrajectoryRecorder event sequencing and TrajectorySummarizer output format. These are the simplest P1 modules — recorder is fully synchronous, summarizer has one async method trivially mockable.

Output: tests/test_opengui_p1_trajectory.py with passing tests covering all TEST-04 requirements.
</objective>

<execution_context>
@/Users/jinli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p1-unit-tests/01-RESEARCH.md
@.planning/phases/01-p1-unit-tests/01-01-SUMMARY.md

@opengui/trajectory/recorder.py
@opengui/trajectory/summarizer.py
@opengui/interfaces.py
@tests/test_opengui.py

<interfaces>
<!-- Key types the executor needs -->

From opengui/trajectory/recorder.py:
```python
class ExecutionPhase(str, Enum):
    AGENT = "agent"
    SKILL = "skill"
    RETRY = "retry"
    RECOVERY = "recovery"

@dataclass
class TrajectoryRecorder:
    output_dir: Path
    task: str
    platform: str
    # Key methods:
    def start(self) -> Path: ...
    def record_step(self, action: dict, model_output: str, screenshot_path: str | None = None) -> None: ...
    def set_phase(self, phase: ExecutionPhase, reason: str = "") -> None: ...
    def finish(self, success: bool, error: str | None = None) -> None: ...
```

From opengui/trajectory/summarizer.py:
```python
class TrajectorySummarizer:
    def __init__(self, llm: LLMProvider) -> None: ...
    async def summarize_events(self, events: list[dict]) -> str: ...
```

From opengui/interfaces.py:
```python
@dataclass(frozen=True)
class LLMResponse:
    content: str
    tool_calls: list[ToolCall] | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write TrajectoryRecorder event sequencing tests</name>
  <files>tests/test_opengui_p1_trajectory.py</files>
  <behavior>
    - recorder.start() returns a Path that exists on disk
    - After start() + record_step() + finish(), JSONL has metadata first, result last
    - result event contains success=True and total_steps matching recorded step count
    - set_phase() causes subsequent steps to record with the new phase
    - record_step() before start() raises RuntimeError
  </behavior>
  <action>
Create `tests/test_opengui_p1_trajectory.py`. At the top define a `_ScriptedLLM` helper (same pattern as other test files: accepts canned response strings, returns `LLMResponse(content=response)` from `async def chat()`).

Write sync tests for TrajectoryRecorder (it is fully synchronous):

- `test_trajectory_recorder_start_creates_file`: Create TrajectoryRecorder(output_dir=tmp_path, task="test", platform="android"), call start(), assert returned Path exists on disk.

- `test_trajectory_recorder_event_order`: Create recorder, start(), record 2 steps with different actions, finish(success=True). Read the JSONL file, parse each line as JSON, assert:
  - First event type is "metadata" with task and platform fields
  - Last event type is "result" with success=True and total_steps=2
  - Middle events are "step" type
  - Events follow the exact research Code Example pattern

- `test_trajectory_recorder_set_phase`: Create recorder, start(), record_step (default phase should be "agent"), set_phase(ExecutionPhase.SKILL, reason="matched skill"), record_step. Parse JSONL, verify the second step event has phase="skill".

- `test_trajectory_recorder_not_started_raises`: Create recorder but do NOT call start(). Call record_step() and assert it raises RuntimeError. (Pitfall 4 from research.)

- `test_trajectory_recorder_finish_failure`: Create recorder, start(), record 1 step, finish(success=False, error="timeout"). Parse result event, assert success=False and error field present.

All tests use `tmp_path` fixture.
  </action>
  <verify>
    <automated>uv run pytest tests/test_opengui_p1_trajectory.py -k "recorder" -v</automated>
  </verify>
  <done>TrajectoryRecorder event sequencing, phase tracking, start/finish lifecycle, and error path are tested with passing tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write TrajectorySummarizer output format test</name>
  <files>tests/test_opengui_p1_trajectory.py</files>
  <behavior>
    - TrajectorySummarizer.summarize_events() returns a non-empty string from the LLM
    - The returned string is the stripped content from the LLM response
  </behavior>
  <action>
Append to `tests/test_opengui_p1_trajectory.py` (using the `_ScriptedLLM` helper already defined in Task 1):

- `test_trajectory_summarizer_returns_string`: Create _ScriptedLLM("The agent opened settings successfully."), create TrajectorySummarizer(llm), call summarize_events() with a list of 3 event dicts (metadata, step, result — matching the research Code Example pattern). Assert result is a non-empty string. Assert result contains "opened settings" (the canned response content).

This is a minimal but sufficient test since the summarizer is a thin wrapper that formats events into a prompt and returns the LLM's stripped response.
  </action>
  <verify>
    <automated>uv run pytest tests/test_opengui_p1_trajectory.py -k "summarizer" -v</automated>
  </verify>
  <done>TrajectorySummarizer output format test passes, confirming it returns a non-empty string from the mocked LLM</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_opengui_p1_trajectory.py -v` — all trajectory tests green
- `uv run pytest tests/ -v` — full suite (P0 + all P1) green
</verification>

<success_criteria>
- tests/test_opengui_p1_trajectory.py exists with 6+ tests
- TrajectoryRecorder event sequencing (metadata first, result last, correct step count) tested
- TrajectoryRecorder set_phase changes subsequent step phase
- TrajectoryRecorder not-started error path tested
- TrajectorySummarizer returns non-empty string from mocked LLM
- All tests pass without network access, real LLM, or real device
</success_criteria>

<output>
After completion, create `.planning/phases/01-p1-unit-tests/01-03-SUMMARY.md`
</output>
