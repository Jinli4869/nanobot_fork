---
phase: 24-schema-and-grounding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - opengui/skills/shortcut.py
  - opengui/skills/__init__.py
  - tests/test_opengui_p24_schema_grounding.py
  - tests/test_opengui_p1_skills.py
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]
must_haves:
  truths:
    - "ShortcutSkill is a frozen dataclass that round-trips through `to_dict()` / `from_dict()` with structured preconditions and postconditions instead of free-form strings."
    - "StateDescriptor and ParameterSlot are reusable schema primitives with serialization-friendly fields and tuple-based storage on ShortcutSkill."
    - "The new shortcut-layer schema is exported from `opengui.skills` without modifying or deleting the legacy `Skill` / `SkillStep` contracts."
  artifacts:
    - path: "opengui/skills/shortcut.py"
      provides: "StateDescriptor, ParameterSlot, and ShortcutSkill schema contracts"
      contains: "class ShortcutSkill"
    - path: "opengui/skills/__init__.py"
      provides: "Public exports for shortcut-layer schema types"
      contains: "ShortcutSkill"
    - path: "tests/test_opengui_p24_schema_grounding.py"
      provides: "Round-trip tests for ShortcutSkill, StateDescriptor, and ParameterSlot"
      contains: "test_shortcut_skill"
  key_links:
    - from: "opengui/skills/shortcut.py"
      to: "opengui/skills/data.py"
      via: "shared frozen-dataclass and serializer style"
      pattern: "to_dict"
    - from: "opengui/skills/__init__.py"
      to: "opengui/skills/shortcut.py"
      via: "public module export"
      pattern: "ShortcutSkill"
---

<objective>
Add the shared schema primitives and shortcut-layer skill contract that Phase 24 downstream work builds on.

Purpose: satisfy `SCHEMA-01` and `SCHEMA-02` with explicit, checkable state descriptors and typed parameter slots while preserving the existing legacy skill path.
Output: `shortcut.py` with `StateDescriptor`, `ParameterSlot`, and `ShortcutSkill`; updated `opengui.skills` exports; regression tests proving round-trip behavior.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-schema-and-grounding/24-CONTEXT.md
@.planning/phases/24-schema-and-grounding/24-RESEARCH.md
@.planning/phases/24-schema-and-grounding/24-VALIDATION.md
@opengui/skills/data.py
@opengui/skills/__init__.py
@tests/test_opengui_p1_skills.py

<interfaces>
Locked Phase 24 decisions to implement in this plan:
```python
@dataclass(frozen=True)
class StateDescriptor:
    kind: str
    value: str
    negated: bool = False

@dataclass(frozen=True)
class ParameterSlot:
    name: str
    type: str
    description: str
```

Shortcut-layer contract target:
```python
@dataclass(frozen=True)
class ShortcutSkill:
    skill_id: str
    name: str
    description: str
    app: str
    platform: str
    steps: tuple[SkillStep, ...] = ()
    parameter_slots: tuple[ParameterSlot, ...] = ()
    preconditions: tuple[StateDescriptor, ...] = ()
    postconditions: tuple[StateDescriptor, ...] = ()
    tags: tuple[str, ...] = ()
```

Implementation constraints:
```text
- keep frozen dataclasses and manual `to_dict()` / `from_dict()`
- keep the Phase 24 schema stdlib-only; do not introduce Pydantic into core `opengui` contracts
- do not modify or remove the existing `Skill` / `SkillStep` classes in this plan
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing schema round-trip coverage for shortcut-layer contracts</name>
  <files>tests/test_opengui_p24_schema_grounding.py, tests/test_opengui_p1_skills.py</files>
  <read_first>.planning/phases/24-schema-and-grounding/24-CONTEXT.md, .planning/phases/24-schema-and-grounding/24-RESEARCH.md, .planning/phases/24-schema-and-grounding/24-VALIDATION.md, opengui/skills/data.py, opengui/skills/__init__.py, tests/test_opengui_p1_skills.py</read_first>
  <behavior>
    - Test 1: `StateDescriptor(kind="element_visible", value="Settings button")` round-trips through `to_dict()` and `from_dict()` with `negated=False` preserved.
    - Test 2: `ParameterSlot(name="panel", type="str", description="Settings panel name")` round-trips through `to_dict()` and `from_dict()`.
    - Test 3: `ShortcutSkill` round-trips with tuple-based `parameter_slots`, `preconditions`, `postconditions`, and legacy `SkillStep` entries in `steps`.
    - Test 4: `opengui.skills.__all__` exports `StateDescriptor`, `ParameterSlot`, and `ShortcutSkill` without dropping `Skill` or `SkillStep`.
  </behavior>
  <action>Create `tests/test_opengui_p24_schema_grounding.py` with failing tests named `test_state_descriptor_round_trip`, `test_parameter_slot_round_trip`, `test_shortcut_skill_round_trip`, and `test_opengui_skills_exports_shortcut_schema`. Import `SkillStep` from `opengui.skills.data` and expect `ShortcutSkill.steps` to reuse that existing type. Update `tests/test_opengui_p1_skills.py` only if needed to add a compatibility assertion that importing `Skill`, `SkillStep`, and the new shortcut-layer exports from `opengui.skills` works in one module. Do not write executor or grounding tests in this task.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p24_schema_grounding.py` exists and contains the exact test names `test_state_descriptor_round_trip`, `test_parameter_slot_round_trip`, and `test_shortcut_skill_round_trip`
    - `tests/test_opengui_p24_schema_grounding.py` imports `SkillStep` from `opengui.skills.data`
    - `tests/test_opengui_p24_schema_grounding.py` contains an assertion against `opengui.skills.__all__`
    - `uv run pytest -q tests/test_opengui_p24_schema_grounding.py -k "state_descriptor or parameter_slot or shortcut_skill or exports"` exits non-zero before implementation and exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_opengui_p24_schema_grounding.py -k "state_descriptor or parameter_slot or shortcut_skill or exports"</automated>
  </verify>
  <done>The failing tests define the exact shortcut-layer contract and public exports before any production schema code is added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement `shortcut.py` and export the new shortcut-layer schema</name>
  <files>opengui/skills/shortcut.py, opengui/skills/__init__.py, tests/test_opengui_p24_schema_grounding.py, tests/test_opengui_p1_skills.py</files>
  <read_first>.planning/phases/24-schema-and-grounding/24-CONTEXT.md, .planning/phases/24-schema-and-grounding/24-RESEARCH.md, opengui/skills/data.py, opengui/skills/__init__.py, tests/test_opengui_p24_schema_grounding.py, tests/test_opengui_p1_skills.py</read_first>
  <behavior>
    - `StateDescriptor.to_dict()` returns keys `kind`, `value`, and `negated` only when `negated` is true or differs from the default policy chosen in code; `from_dict()` restores the same values.
    - `ParameterSlot.to_dict()` returns keys `name`, `type`, and `description`; `from_dict()` restores the same values.
    - `ShortcutSkill.to_dict()` serializes `steps` with `SkillStep.to_dict()` and serializes `parameter_slots`, `preconditions`, and `postconditions` as lists of dicts.
    - `ShortcutSkill.from_dict()` reconstructs tuple-based fields and preserves `skill_id`, `name`, `description`, `app`, `platform`, `tags`, and `created_at`.
  </behavior>
  <action>Create `opengui/skills/shortcut.py` with `from __future__ import annotations`, stdlib imports only, and three frozen dataclasses exactly named `StateDescriptor`, `ParameterSlot`, and `ShortcutSkill`. Implement explicit `to_dict()` / `from_dict()` methods on each class. In `ShortcutSkill`, use these concrete field names: `skill_id`, `name`, `description`, `app`, `platform`, `steps`, `parameter_slots`, `preconditions`, `postconditions`, `tags`, and `created_at`. Reuse `SkillStep` from `opengui.skills.data` for the `steps` field. Update `opengui/skills/__init__.py` to import and export `StateDescriptor`, `ParameterSlot`, and `ShortcutSkill` while preserving the existing exports for `Skill`, `SkillStep`, `SkillExecutor`, and `SkillExtractor`. Keep all serializer output JSON-friendly by converting tuples to lists.</action>
  <acceptance_criteria>
    - `opengui/skills/shortcut.py` contains the exact class names `StateDescriptor`, `ParameterSlot`, and `ShortcutSkill`
    - `opengui/skills/shortcut.py` contains `parameter_slots: tuple[ParameterSlot, ...] = ()`
    - `opengui/skills/shortcut.py` contains `preconditions: tuple[StateDescriptor, ...] = ()` and `postconditions: tuple[StateDescriptor, ...] = ()`
    - `opengui/skills/__init__.py` contains the strings `"StateDescriptor"`, `"ParameterSlot"`, and `"ShortcutSkill"` in `__all__`
    - `uv run pytest -q tests/test_opengui_p24_schema_grounding.py -k "state_descriptor or parameter_slot or shortcut_skill or exports"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_opengui_p24_schema_grounding.py -k "state_descriptor or parameter_slot or shortcut_skill or exports"</automated>
  </verify>
  <done>The shortcut-layer schema is implemented, exported publicly, and verified to round-trip without disturbing the legacy skill contracts.</done>
</task>

</tasks>

<verification>
Run the Phase 24 quick slice after this plan: `uv run pytest -q tests/test_opengui_p1_skills.py tests/test_opengui_p24_schema_grounding.py`. Confirm `opengui.skills` exports both the legacy and new shortcut-layer contracts.
</verification>

<success_criteria>
`SCHEMA-01` and `SCHEMA-02` are satisfied when `ShortcutSkill` stores structured pre/post descriptors and typed parameter slots in explicit frozen dataclasses that serialize and round-trip cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/24-schema-and-grounding/24-01-SUMMARY.md`
</output>
