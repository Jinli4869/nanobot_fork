---
phase: 06-fix-integration-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_opengui_p6_wiring.py
  - nanobot/config/schema.py
  - nanobot/agent/tools/gui.py
  - pyproject.toml
autonomous: true
requirements:
  - NANO-03
  - BACK-03
  - CLI-01
must_haves:
  truths:
    - "`GuiConfig` exposes optional `embedding_model` and accepts the camelCase alias `embeddingModel`."
    - "When `gui.embedding_model` is configured, `GuiSubagentTool` instantiates `NanobotEmbeddingAdapter` and passes it to `SkillLibrary` as `embedding_provider`."
    - "When `gui.embedding_model` is absent, `GuiSubagentTool` preserves the current graceful fallback with `embedding_provider=None`."
    - "`pyproject.toml` declares `Pillow>=10.0` in both the `desktop` and `dev` extras."
    - "`pyproject.toml` declares `opengui = \"opengui.cli:main\"` under `[project.scripts]`."
  artifacts:
    - path: "tests/test_opengui_p6_wiring.py"
      provides: "Focused regression coverage for the Phase 6 gap-closure contract"
      contains:
        - "test_gui_config_accepts_embedding_model_alias"
        - "test_gui_tool_wires_embedding_adapter_when_configured"
        - "test_gui_tool_skips_embedding_adapter_without_config"
        - "test_pyproject_declares_pillow_for_desktop_and_dev"
        - "test_pyproject_declares_opengui_console_script"
    - path: "nanobot/config/schema.py"
      provides: "Nanobot GUI config surface for the optional embedding model"
      contains:
        - "embedding_model: str | None = None"
    - path: "nanobot/agent/tools/gui.py"
      provides: "Nanobot GUI tool wiring for embedding-backed skill search"
      contains:
        - "NanobotEmbeddingAdapter"
        - "litellm.aembedding"
        - "embedding_provider=self._embedding_adapter"
    - path: "pyproject.toml"
      provides: "Desktop dependency metadata and installable `opengui` console script"
      contains:
        - "Pillow>=10.0"
        - "opengui = \"opengui.cli:main\""
  key_links:
    - from: "nanobot/agent/tools/gui.py"
      to: "nanobot/agent/gui_adapter.py"
      via: "instantiates NanobotEmbeddingAdapter"
      pattern: "NanobotEmbeddingAdapter"
    - from: "nanobot/agent/tools/gui.py"
      to: "nanobot/config/schema.py"
      via: "reads GuiConfig.embedding_model to decide whether embeddings are enabled"
      pattern: "embedding_model"
    - from: "pyproject.toml"
      to: "opengui/cli.py"
      via: "declares the installable console entry point"
      pattern: "opengui = \"opengui.cli:main\""
---

<objective>
Close the three broken integration seams left after Phases 3-5: nanobot embedding search wiring, desktop Pillow packaging, and the installable `opengui` console script.

Purpose: satisfy the Phase 6 gap-closure goal without reopening prior phase scope.
Output: focused regression coverage, `GuiConfig.embedding_model`, embedding adapter wiring in `GuiSubagentTool`, and `pyproject.toml` metadata fixes.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-integration-wiring/06-CONTEXT.md
@.planning/phases/06-fix-integration-wiring/06-RESEARCH.md
@.planning/phases/06-fix-integration-wiring/06-VALIDATION.md
@.planning/phases/03-nanobot-subagent/03-01-SUMMARY.md
@.planning/phases/03-nanobot-subagent/03-02-SUMMARY.md
@.planning/phases/04-desktop-backend/04-01-SUMMARY.md
@.planning/phases/05-cli-extensions/05-01-SUMMARY.md
@nanobot/agent/gui_adapter.py
@nanobot/agent/tools/gui.py
@nanobot/config/schema.py
@nanobot/providers/base.py
@nanobot/providers/litellm_provider.py
@nanobot/providers/custom_provider.py
@opengui/cli.py
@opengui/backends/desktop.py
@opengui/skills/library.py
@pyproject.toml

<interfaces>
From `nanobot/agent/gui_adapter.py`:
```python
class NanobotEmbeddingAdapter:
    def __init__(self, embed_fn: Callable[[list[str]], Awaitable[np.ndarray]]) -> None: ...
    async def embed(self, texts: list[str]) -> np.ndarray: ...
```

From `nanobot/config/schema.py`:
```python
class GuiConfig(Base):
    backend: Literal["adb", "local", "dry-run"] = "adb"
    adb: AdbConfig = Field(default_factory=AdbConfig)
    artifacts_dir: str = "gui_runs"
    max_steps: int = 15
    skill_threshold: float = 0.6
```

From `nanobot/agent/tools/gui.py`:
```python
class GuiSubagentTool(Tool):
    def __init__(..., gui_config: GuiConfig | None, provider: LLMProvider, model: str, workspace: Path) -> None: ...
    def _get_skill_library(self, platform: str) -> Any: ...
```

From `opengui/skills/library.py`:
```python
@dataclass
class SkillLibrary:
    store_dir: Path
    embedding_provider: EmbeddingProvider | None = None
    merge_llm: LLMProvider | None = None
```

From the installed LiteLLM package (`.venv`):
```python
async def aembedding(*args, **kwargs) -> EmbeddingResponse: ...

class EmbeddingResponse(BaseModel):
    data: list
```

From `pyproject.toml`:
```toml
[project.optional-dependencies]
desktop = [
    "mss>=10.0",
    "pyautogui>=0.9.50",
    "pyperclip>=1.8",
]
dev = [
    ...
]

[project.scripts]
nanobot = "nanobot.cli.commands:app"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused Phase 6 regression coverage for config, embedding wiring, and packaging metadata</name>
  <files>tests/test_opengui_p6_wiring.py</files>
  <read_first>
tests/test_opengui_p3_nanobot.py
tests/test_opengui_p4_desktop.py
tests/test_opengui_p5_cli.py
nanobot/config/schema.py
nanobot/agent/tools/gui.py
nanobot/agent/gui_adapter.py
nanobot/providers/base.py
nanobot/providers/litellm_provider.py
pyproject.toml
.planning/phases/06-fix-integration-wiring/06-CONTEXT.md
.planning/phases/06-fix-integration-wiring/06-RESEARCH.md
.planning/phases/06-fix-integration-wiring/06-VALIDATION.md
  </read_first>
  <action>
Create `tests/test_opengui_p6_wiring.py` as the dedicated regression file for this gap-closure phase.

Add `test_gui_config_accepts_embedding_model_alias` that constructs `Config(gui={"backend": "dry-run", "embeddingModel": "text-embedding-3-small"})` and asserts `config.gui.embedding_model == "text-embedding-3-small"`.

Add `test_gui_tool_wires_embedding_adapter_when_configured` that:
- defines a fake provider object exposing `api_key`, `api_base`, `extra_headers`, and `_resolve_model(model) -> "resolved/" + model`
- monkeypatches `litellm.aembedding` to an async fake returning an object with `.data = [SimpleNamespace(embedding=[1.0, 2.0]), SimpleNamespace(embedding=[3.0, 4.0])]`
- instantiates `GuiSubagentTool` with `Config(gui={"backend": "dry-run", "embeddingModel": "embed-model"})`
- asserts `tool._embedding_adapter is not None`
- asserts `tool._skill_libraries["dry-run"].embedding_provider is tool._embedding_adapter`
- awaits `tool._embedding_adapter.embed(["hello", "world"])` and asserts the result is a `numpy.ndarray` with `dtype == numpy.float32`, `shape == (2, 2)`, and that `litellm.aembedding` was called with `model="resolved/embed-model"`, `input=["hello", "world"]`, plus the provider `api_key`, `api_base`, and `extra_headers`

Add `test_gui_tool_skips_embedding_adapter_without_config` that instantiates `GuiSubagentTool` with `Config(gui={"backend": "dry-run"})` and asserts both `tool._embedding_adapter is None` and `tool._skill_libraries["dry-run"].embedding_provider is None`.

Add `test_pyproject_declares_pillow_for_desktop_and_dev` and `test_pyproject_declares_opengui_console_script` that parse `pyproject.toml` with `tomllib` and assert the exact metadata contract:
- `desktop` contains a string starting with `Pillow>=10.0`
- `dev` contains a string starting with `Pillow>=10.0`
- `[project.scripts]["opengui"] == "opengui.cli:main"`

Do not import nanobot runtime modules from other test files. Keep the Phase 6 test file self-contained.
  </action>
  <acceptance_criteria>
`rg -n "def test_gui_config_accepts_embedding_model_alias" tests/test_opengui_p6_wiring.py` returns 1 match.
`rg -n "def test_gui_tool_wires_embedding_adapter_when_configured" tests/test_opengui_p6_wiring.py` returns 1 match.
`rg -n "def test_gui_tool_skips_embedding_adapter_without_config" tests/test_opengui_p6_wiring.py` returns 1 match.
`rg -n "def test_pyproject_declares_pillow_for_desktop_and_dev" tests/test_opengui_p6_wiring.py` returns 1 match.
`rg -n "def test_pyproject_declares_opengui_console_script" tests/test_opengui_p6_wiring.py` returns 1 match.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_opengui_p6_wiring.py -x -q</automated>
  </verify>
  <done>The Phase 6 contract is locked behind a dedicated regression file that will fail until the missing wiring and packaging metadata are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement embedding adapter wiring and package metadata fixes</name>
  <files>tests/test_opengui_p6_wiring.py, nanobot/config/schema.py, nanobot/agent/tools/gui.py, pyproject.toml</files>
  <read_first>
tests/test_opengui_p6_wiring.py
nanobot/config/schema.py
nanobot/agent/tools/gui.py
nanobot/agent/gui_adapter.py
nanobot/providers/base.py
nanobot/providers/litellm_provider.py
nanobot/providers/custom_provider.py
opengui/cli.py
opengui/backends/desktop.py
pyproject.toml
.planning/phases/06-fix-integration-wiring/06-CONTEXT.md
.planning/phases/06-fix-integration-wiring/06-RESEARCH.md
  </read_first>
  <action>
Update `nanobot/config/schema.py` so `GuiConfig` includes the exact field `embedding_model: str | None = None`. Preserve the existing alias-generator behavior so both `embeddingModel` and `embedding_model` work.

Update `nanobot/agent/tools/gui.py` to import `litellm`, `numpy as np`, and `NanobotEmbeddingAdapter`, then replace the unconditional `self._embedding_adapter = None` assignment with a helper that builds the adapter only when `self._gui_config.embedding_model` is truthy.

Implement the embedding helper with this exact behavior:
- if `texts` is empty, return `np.zeros((0, 0), dtype=np.float32)`
- resolve the configured embedding model by calling `self._provider._resolve_model(self._gui_config.embedding_model)` when that attribute exists and is callable; otherwise use `self._gui_config.embedding_model` as-is
- call `await litellm.aembedding(model=resolved_model, input=texts, api_key=self._provider.api_key if present, api_base=self._provider.api_base if present, extra_headers=self._provider.extra_headers if present and non-empty)`
- normalize `response.data[*].embedding` into `np.array(vectors, dtype=np.float32)`
- wrap that callable with `NanobotEmbeddingAdapter(embed_fn)` and store it on `self._embedding_adapter`

Do not change the current fallback path: when `embedding_model` is missing, `self._embedding_adapter` must remain `None` and `SkillLibrary` must still be created successfully with `embedding_provider=None`.

Update `pyproject.toml` so:
- `[project.optional-dependencies].desktop` contains `Pillow>=10.0`
- `[project.optional-dependencies].dev` contains `Pillow>=10.0`
- `[project.scripts]` contains `opengui = "opengui.cli:main"` alongside the existing `nanobot` script

Promote the new Phase 6 tests from RED to GREEN without widening the phase scope into unrelated agent-loop work.
  </action>
  <acceptance_criteria>
`rg -n "embedding_model: str \\| None = None" nanobot/config/schema.py` returns 1 match.
`rg -n "NanobotEmbeddingAdapter" nanobot/agent/tools/gui.py` returns at least 1 match.
`rg -n "litellm\\.aembedding" nanobot/agent/tools/gui.py` returns 1 match.
`rg -n "embedding_provider=self\\._embedding_adapter" nanobot/agent/tools/gui.py` returns 1 match.
`rg -n "Pillow>=10.0" pyproject.toml` returns at least 2 matches.
`rg -n "opengui = \"opengui\\.cli:main\"" pyproject.toml` returns 1 match.
`./.venv/bin/python -m pytest tests/test_opengui_p6_wiring.py -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_opengui_p6_wiring.py -x -q</automated>
    <automated>PATH="$(pwd)/.venv/bin:$PATH" ./.venv/bin/python -m pytest tests/ -x -q</automated>
  </verify>
  <done>The nanobot path can use embedding-backed skill search when configured, desktop installs include Pillow explicitly, and package installs expose `opengui` as a console script.</done>
</task>

</tasks>

<verification>
Run `./.venv/bin/python -m pytest tests/test_opengui_p6_wiring.py -x -q`.
Run `PATH="$(pwd)/.venv/bin:$PATH" ./.venv/bin/python -m pytest tests/ -x -q`.
Run `rg -n "embedding_model|NanobotEmbeddingAdapter|litellm\\.aembedding|Pillow>=10.0|opengui = \"opengui\\.cli:main\"" nanobot/config/schema.py nanobot/agent/tools/gui.py pyproject.toml tests/test_opengui_p6_wiring.py`.
</verification>

<success_criteria>
`tests/test_opengui_p6_wiring.py` exists and locks all three phase gaps behind automated checks.
`nanobot/config/schema.py` exposes `GuiConfig.embedding_model`.
`nanobot/agent/tools/gui.py` wires `NanobotEmbeddingAdapter` through `SkillLibrary` when configured and keeps the no-embedding fallback when not configured.
`pyproject.toml` explicitly declares `Pillow>=10.0` for desktop installs and `opengui = "opengui.cli:main"` for installed CLI usage.
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-integration-wiring/06-01-SUMMARY.md`
</output>
