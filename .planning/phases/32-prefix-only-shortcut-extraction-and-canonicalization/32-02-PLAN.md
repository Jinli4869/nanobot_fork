---
phase: 32-prefix-only-shortcut-extraction-and-canonicalization
plan: "02"
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - opengui/skills/shortcut_extractor.py
  - tests/test_opengui_p26_quality_gated_extraction.py
autonomous: true
requirements: [SXTR-07]
must_haves:
  truths:
    - "Dynamic action arguments that vary at reuse time are emitted as placeholders and parameter slots instead of frozen recorded literals."
    - "Placeholder emission expands beyond `input_text.text` without breaking the Phase 30/31 executor contract that merges `step.parameters`, grounding output, and caller overrides."
    - "Stable literals and control-only fields are not over-generalized into meaningless slots."
  artifacts:
    - path: "opengui/skills/shortcut_extractor.py"
      provides: "Broadened placeholder inference and parameter generalization"
      contains: "_generalize_parameters"
    - path: "opengui/skills/shortcut_extractor.py"
      provides: "Named-slot inference for dynamic targets and selector-like fields"
      contains: "_infer_placeholder_name"
    - path: "tests/test_opengui_p26_quality_gated_extraction.py"
      provides: "Unit coverage for widened placeholder emission"
      contains: "test_shortcut_skill_producer_generalizes_dynamic_fields_beyond_input_text"
  key_links:
    - from: "opengui/skills/shortcut_extractor.py"
      to: "tests/test_opengui_p26_quality_gated_extraction.py"
      via: "Generalization rules are asserted at producer level before promotion/store integration"
      pattern: "generalizes_dynamic_fields|selector|coordinate"
    - from: "opengui/skills/shortcut_extractor.py"
      to: "opengui/skills/multi_layer_executor.py"
      via: "Rendered parameters remain compatible with runtime grounding merge order"
      pattern: "step.parameters|rendered_parameters"
---

<objective>
Widen placeholder emission so reusable shortcuts preserve dynamic fields as slots instead of brittle recorded literals.

Purpose: satisfy `SXTR-07` in the extractor seam that already owns `SkillStep` and `ParameterSlot` generation.
Output: broader but controlled placeholder inference in `ShortcutSkillProducer` plus unit tests proving dynamic text/selectors/coordinates are generalized while stable literals are preserved.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-RESEARCH.md
@.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-VALIDATION.md
@opengui/skills/shortcut_extractor.py
@opengui/skills/multi_layer_executor.py
@tests/test_opengui_p26_quality_gated_extraction.py
@tests/test_opengui_p31_shortcut_observability.py

<interfaces>
Placeholder/generalization targets in `ShortcutSkillProducer`:
```text
- keep templating `input_text.text`
- generalize selector-like string fields such as `text`, `label`, `value`, `query`, `selector`, `resource_id`, and `accessibility_id` when they are task payload rather than stable chrome
- continue dropping stale pointer coordinates from promoted tap/click-like actions
- allow slot names such as `recipient`, `message`, `search_term`, `selector`, `value`, `x`, `y`
```

Concrete non-goals:
```text
- do not generalize `action_type`
- do not convert every literal into a slot
- do not move slot rendering into the executor
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing producer tests for widened placeholder emission</name>
  <files>tests/test_opengui_p26_quality_gated_extraction.py</files>
  <read_first>.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-RESEARCH.md, opengui/skills/shortcut_extractor.py, opengui/skills/multi_layer_executor.py, tests/test_opengui_p26_quality_gated_extraction.py</read_first>
  <action>Add these exact tests to `tests/test_opengui_p26_quality_gated_extraction.py`: `test_shortcut_skill_producer_generalizes_dynamic_fields_beyond_input_text` and `test_shortcut_skill_producer_preserves_stable_literals_without_placeholder_explosion`. In the first test, build steps whose actions include `{"action_type": "tap", "resource_id": "thread_alice", "x": 540, "y": 960}`, `{"action_type": "input_text", "text": "hello alice"}`, and `{"action_type": "press", "label": "Send"}` with model outputs indicating reusable target slots like `Open chat for {{recipient}}` and `Type {{message}}`; assert the produced shortcut exposes parameter-slot names including `recipient` and `message`, drops stale pointer coordinates from tap parameters, and rewrites dynamic selector/text fields to `{{recipient}}` / `{{message}}` where appropriate. In the second test, build a stable chrome-only step such as `press Back` or `tap Compose button` and assert `parameter_slots == ()` and the action parameters keep stable literal values instead of inventing placeholders.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p26_quality_gated_extraction.py` contains the exact test names `test_shortcut_skill_producer_generalizes_dynamic_fields_beyond_input_text` and `test_shortcut_skill_producer_preserves_stable_literals_without_placeholder_explosion`
    - `tests/test_opengui_p26_quality_gated_extraction.py` contains the exact strings `resource_id`, `recipient`, `message`, and `parameter_slots`
    - At least one new assertion checks that tap/click coordinates are not preserved in generalized parameters
    - `uv run pytest tests/test_opengui_p26_quality_gated_extraction.py -k "dynamic_fields_beyond_input_text or placeholder_explosion" -q` exits non-zero before implementation and exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p26_quality_gated_extraction.py -k "dynamic_fields_beyond_input_text or placeholder_explosion" -q</automated>
  </verify>
  <done>The tests define which dynamic fields must become slots and which stable literals must stay literal.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement broader but controlled placeholder inference in ShortcutSkillProducer</name>
  <files>opengui/skills/shortcut_extractor.py, tests/test_opengui_p26_quality_gated_extraction.py</files>
  <read_first>.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-RESEARCH.md, opengui/skills/shortcut_extractor.py, opengui/skills/multi_layer_executor.py, tests/test_opengui_p26_quality_gated_extraction.py</read_first>
  <action>Update `opengui/skills/shortcut_extractor.py` so `_generalize_parameters(...)` and any new helper it needs generalize dynamic string fields beyond `input_text.text`. Use these exact candidate keys for templating when the associated target/model output already implies task-specific data or when the value is clearly variable: `text`, `label`, `value`, `query`, `selector`, `resource_id`, and `accessibility_id`. Continue to remove `_COORD_KEYS` from pointer actions. Add a helper such as `_infer_placeholder_name(action_type, key, target, value)` that prefers concrete slot names `recipient`, `message`, `search_term`, `query`, `selector`, `value`, `x`, and `y` based on target text and field name. Only emit a placeholder when the value is non-empty and task-varying; preserve stable controls like `Send`, `Back`, `Compose`, or already-template-free static chrome labels as literals. Ensure `_infer_parameter_slots(...)` still discovers every emitted `{{slot}}` string from both target and parameters so the produced `ShortcutSkill.parameter_slots` matches the widened generalization behavior.</action>
  <acceptance_criteria>
    - `opengui/skills/shortcut_extractor.py` contains the exact field names `resource_id`, `accessibility_id`, `selector`, `query`, `value`, and `label`
    - `opengui/skills/shortcut_extractor.py` contains a helper or logic path that infers slot names `recipient` and `message`
    - `opengui/skills/shortcut_extractor.py` still excludes `_COORD_KEYS` from pointer-action parameters
    - `uv run pytest tests/test_opengui_p26_quality_gated_extraction.py -k "dynamic_fields_beyond_input_text or placeholder_explosion" -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p26_quality_gated_extraction.py -k "dynamic_fields_beyond_input_text or placeholder_explosion" -q</automated>
  </verify>
  <done>The extractor now emits reusable placeholders for task-varying fields without turning stable controls into unnecessary slots.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_opengui_p26_quality_gated_extraction.py -k "dynamic_fields_beyond_input_text or placeholder_explosion" -q` and confirm widened generalization produces meaningful slot names while preserving stable literals.
</verification>

<success_criteria>
`SXTR-07` is satisfied when stored shortcuts replace task-varying selector/text payloads with placeholders and parameter slots, while the executor contract remains compatible with grounded runtime values.
</success_criteria>

<output>
After completion, create `.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-02-SUMMARY.md`
</output>
