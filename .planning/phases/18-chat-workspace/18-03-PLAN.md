---
phase: 18-chat-workspace
plan: "03"
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - nanobot/tui/contracts.py
  - nanobot/tui/dependencies.py
  - nanobot/tui/routes/chat.py
  - nanobot/tui/schemas/chat.py
  - nanobot/tui/services/chat.py
  - nanobot/tui/services/event_stream.py
  - tests/test_tui_p18_chat.py
  - tests/test_tui_p18_streaming.py
  - tests/test_commands.py
autonomous: true
requirements:
  - CHAT-03
must_haves:
  truths:
    - "Refreshing the browser can rebuild recent transcript state from persisted backend sessions."
    - "Reconnecting to the event stream does not require re-running a completed assistant turn to see the final transcript."
    - "Existing CLI chat behavior remains unchanged and regression-tested while chat workspace recovery is added."
  artifacts:
    - path: "nanobot/tui/services/chat.py"
      provides: "Recovery-focused transcript loading and message metadata shaping"
    - path: "nanobot/tui/services/event_stream.py"
      provides: "Reconnect-safe event replay or cursor handling for in-flight browser streams"
    - path: "tests/test_commands.py"
      provides: "CLI regression coverage proving no browser-chat coupling leaked into Typer flows"
  key_links:
    - from: "nanobot/tui/services/chat.py"
      to: "nanobot.session.manager.Session.messages"
      via: "refresh-time transcript rebuild"
      pattern: "messages"
    - from: "nanobot/tui/routes/chat.py"
      to: "nanobot/tui/services/event_stream.py"
      via: "Last-Event-ID or equivalent replay cursor handling"
      pattern: "last-event-id|Last-Event-ID|cursor"
    - from: "tests/test_commands.py"
      to: "nanobot/cli/commands.py"
      via: "unchanged CLI direct-chat regression"
      pattern: "process_direct"
---

<objective>
Finish Phase 18 by making browser reconnect/history recovery dependable while proving the preexisting CLI path still behaves exactly the same.

Purpose: satisfy `CHAT-03` and close the Phase 18 verification loop without absorbing the React shell work reserved for Phase 20.
Output: recovery-oriented transcript/event behavior and the regression slice that protects existing CLI chat.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-chat-workspace/18-RESEARCH.md
@.planning/phases/18-chat-workspace/18-VALIDATION.md
@.planning/phases/18-chat-workspace/18-01-PLAN.md
@.planning/phases/18-chat-workspace/18-02-PLAN.md
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/session/manager.py
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/cli/commands.py
@/Users/jinli/Documents/Personal/nanobot_fork/tests/test_commands.py

<interfaces>
Durable recovery source:
```python
session = SessionManager(workspace).get_or_create(f"tui:{session_id}")
history = session.messages
```

Existing CLI invariant:
```python
response = await agent_loop.process_direct(message, session_id, on_progress=_cli_progress)
```

Validation targets:
```text
uv run --extra dev pytest tests/test_tui_p18_chat.py::test_reconnect_recovers_recent_session_history_from_persisted_state tests/test_commands.py::test_agent_uses_default_config_when_no_workspace_or_config_flags -q
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement reconnect-safe transcript recovery and event replay semantics</name>
  <files>nanobot/tui/contracts.py, nanobot/tui/dependencies.py, nanobot/tui/routes/chat.py, nanobot/tui/schemas/chat.py, nanobot/tui/services/chat.py, nanobot/tui/services/event_stream.py, tests/test_tui_p18_chat.py, tests/test_tui_p18_streaming.py</files>
  <behavior>
    - Test 1: after a completed run, `GET /chat/sessions/{session_id}` reconstructs the latest transcript from persisted session messages after a browser refresh.
    - Test 2: reconnecting to the SSE endpoint during or just after a run can resume from a cursor or `Last-Event-ID` without duplicating durable history logic.
    - Test 3: completed replies remain visible after reconnect even if the transient stream buffer has already advanced, because transcript recovery is sourced from `SessionManager`.
  </behavior>
  <action>Finish the recovery behavior promised in research: persisted transcript state comes from `SessionManager`, while the event broker only handles transient replay for in-flight transport. Add minimal replay/cursor semantics to the SSE route if needed for reconnect safety, but keep them local to `nanobot/tui` and do not introduce a new database, websocket stack, or React client cache dependency. Keep browser-facing APIs scoped to Phase 18; navigation, shell composition, and browser UX stay in Phase 20.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p18_chat.py::test_reconnect_recovers_recent_session_history_from_persisted_state tests/test_tui_p18_streaming.py::test_sse_stream_emits_progress_and_final_reply_events -q</automated>
  </verify>
  <done>Refreshing or reconnecting can recover recent browser chat state from persisted backend sessions, and any stream replay logic remains a thin transport aid rather than the durable record.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the CLI regression boundary and rerun the full Phase 18 validation slice</name>
  <files>tests/test_commands.py, tests/test_tui_p18_chat.py, tests/test_tui_p18_streaming.py</files>
  <action>Add or update only the regression coverage needed to prove browser chat work did not alter existing CLI defaults or direct-chat invocation behavior. Reuse the existing `tests/test_commands.py` patterns instead of changing `nanobot/cli/commands.py`. Then run the Phase 18 full slice from `18-VALIDATION.md`, including the Phase 17 runtime/config tests, so the phase closes with explicit evidence of isolation and no CLI drift.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py tests/test_tui_p18_chat.py tests/test_tui_p18_streaming.py tests/test_commands.py -q</automated>
  </verify>
  <done>The Phase 18 chat/reconnect tests pass, the CLI regression test remains green without behavior changes, and the full validation slice demonstrates the Phase 17 isolation boundary still holds.</done>
</task>

</tasks>

<verification>
Run the full Phase 18 validation command from `18-VALIDATION.md`; this is the required closeout proof that chat workspace work remained backend-only, session-backed, and CLI-safe.
</verification>

<success_criteria>
`CHAT-03` is satisfied when a browser refresh or reconnect can recover recent transcript state from persisted backend sessions, while the existing CLI agent flow stays unchanged and green under regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/18-chat-workspace/18-chat-workspace-03-SUMMARY.md`
</output>
