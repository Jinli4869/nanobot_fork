---
phase: 02-agent-loop-integration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_opengui_p2_integration.py
  - tests/test_opengui_p2_memory.py
autonomous: true
requirements: [TEST-05, AGENT-04, AGENT-05, AGENT-06, MEM-05, SKILL-08, TRAJ-03]

must_haves:
  truths:
    - "Wave 0 test stub files exist before any production code changes"
    - "Each stub has at least one failing test per requirement"
    - "Running the stubs produces predictable ImportError or AssertionError failures (not syntax errors)"
  artifacts:
    - path: "tests/test_opengui_p2_integration.py"
      provides: "Stub integration tests for AGENT-04, AGENT-05, AGENT-06, SKILL-08, TRAJ-03, TEST-05"
      min_lines: 40
    - path: "tests/test_opengui_p2_memory.py"
      provides: "Stub memory tests for MEM-05"
      min_lines: 20
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 2 requirements so that automated verify commands in Waves 1-3 have test files to run against.

Purpose: Nyquist compliance requires test files to exist BEFORE production code is written. These stubs contain failing tests that will be made green by the corresponding Wave 1-3 plans.
Output: Two test stub files with `pytest.skip()` or `pytest.xfail()` markers that can be imported and collected by pytest without errors.
</objective>

<execution_context>
@/Users/jinli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-loop-integration/02-CONTEXT.md
@.planning/phases/02-agent-loop-integration/02-RESEARCH.md
@.planning/phases/02-agent-loop-integration/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for all Phase 2 requirements</name>
  <files>tests/test_opengui_p2_integration.py, tests/test_opengui_p2_memory.py</files>
  <action>
Create two test stub files. Each test should be marked with `@pytest.mark.xfail(reason="Wave 0 stub — implementation pending in Wave N")` so pytest collects and runs them without blocking CI. Tests should attempt real imports so they fail with ImportError or AssertionError (not skip silently).

**tests/test_opengui_p2_integration.py** — stub tests for integration requirements:

```python
"""Phase 2 integration test stubs — Wave 0.

These tests are xfail stubs created before production code.
Each will be replaced with a real implementation in plan 02-04 (Wave 3).
"""
from __future__ import annotations
import pytest

# AGENT-04: Memory entries appear in system prompt
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_memory_injected_into_system_prompt(tmp_path):
    """GuiAgent.run() should inject memory entries into the system prompt."""
    pytest.fail("Not implemented — awaiting plan 02-02 + 02-04")

# AGENT-05: Skill matched above threshold triggers SkillExecutor path
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_skill_path_chosen_above_threshold(tmp_path):
    """When a matching skill exists above threshold, GuiAgent should use SkillExecutor."""
    pytest.fail("Not implemented — awaiting plan 02-02 + 02-04")

# AGENT-05: No skill match falls through to free explore
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_free_explore_when_no_skill_match(tmp_path):
    """When no skill matches, GuiAgent should use free exploration."""
    pytest.fail("Not implemented — awaiting plan 02-02 + 02-04")

# AGENT-06 + TRAJ-03: Trajectory recording
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_trajectory_recorded_on_run(tmp_path):
    """Every agent run should produce a JSONL trajectory with metadata/step/result."""
    pytest.fail("Not implemented — awaiting plan 02-02 + 02-04")

# AGENT-04: TaskPlanner decomposes task
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_planner_decomposes_task(tmp_path):
    """TaskPlanner should decompose a task into an AND/OR/ATOM tree."""
    pytest.fail("Not implemented — awaiting plan 02-03 + 02-04")

# AGENT-04/05/06: Router dispatches atoms by capability type
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_router_dispatches_gui_and_tool_atoms(tmp_path):
    """TreeRouter should dispatch ATOM nodes to correct executor by capability."""
    pytest.fail("Not implemented — awaiting plan 02-03 + 02-04")

# AGENT-04: Router replans on AND-child failure
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_router_replans_on_and_child_failure(tmp_path):
    """On AND-child failure, the router should trigger replanning."""
    pytest.fail("Not implemented — awaiting plan 02-03 + 02-04")

# TEST-05: Full flow with mock LLM + memory + skills
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_full_flow_with_mock_llm(tmp_path):
    """Full flow with DryRunBackend + mock LLM + pre-seeded memory + skill runs to completion."""
    pytest.fail("Not implemented — awaiting plan 02-04")
```

**tests/test_opengui_p2_memory.py** — stub tests for MEM-05:

```python
"""Phase 2 memory test stubs — Wave 0.

These tests are xfail stubs created before production code.
Each will be replaced with a real implementation in plan 02-04 (Wave 3).
"""
from __future__ import annotations
import pytest

# MEM-05: POLICY entries always included regardless of relevance
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_policy_always_included(tmp_path):
    """POLICY memory entries should always appear in system prompt regardless of relevance score."""
    pytest.fail("Not implemented — awaiting plan 02-02 + 02-04")

# MEM-05: Memory context formatted correctly
@pytest.mark.xfail(reason="Wave 0 stub — implementation in 02-04", strict=False)
async def test_memory_context_formatted_in_system_prompt(tmp_path):
    """Memory context should be formatted and passed to build_system_prompt()."""
    pytest.fail("Not implemented — awaiting plan 02-02 + 02-04")
```

Both files use `strict=False` on xfail so they pass when the stub `pytest.fail()` is hit (xfail expected) AND also pass when the real implementation replaces them (xpass allowed).
  </action>
  <verify>
    <automated>cd /Users/jinli/Documents/Personal/nanobot_fork && uv run pytest tests/test_opengui_p2_integration.py tests/test_opengui_p2_memory.py -v --tb=no 2>&1 | tail -20</automated>
  </verify>
  <done>Both test stub files exist, pytest collects all tests, all are xfail (no errors, no unexpected passes). Wave 1-3 plans can reference these files in their automated verify commands.</done>
</task>

</tasks>

<verification>
Pytest collects stubs without errors:
```bash
uv run pytest tests/test_opengui_p2_integration.py tests/test_opengui_p2_memory.py --collect-only
```

All stubs are xfail:
```bash
uv run pytest tests/test_opengui_p2_integration.py tests/test_opengui_p2_memory.py -v --tb=no
```
</verification>

<success_criteria>
- tests/test_opengui_p2_integration.py exists with 8 xfail stub tests
- tests/test_opengui_p2_memory.py exists with 2 xfail stub tests
- pytest collects and runs all stubs without import errors or syntax errors
- All stubs report xfail (not error, not pass)
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-loop-integration/02-00-SUMMARY.md`
</output>
