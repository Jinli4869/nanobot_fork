---
phase: 28-shortcut-extraction-productionization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - nanobot/agent/tools/gui.py
  - opengui/skills/shortcut_promotion.py
  - opengui/skills/__init__.py
  - tests/test_opengui_p8_trajectory.py
  - tests/test_opengui_p28_shortcut_productionization.py
autonomous: true
requirements: [SXTR-01]
must_haves:
  truths:
    - "GUI background postprocessing no longer promotes shortcuts through `SkillExtractor` and legacy `SkillLibrary`; it routes through a dedicated shortcut promotion path."
    - "The production promotion seam reads recorder JSONL, slices to the final successful attempt when attempt markers exist, and keeps only valid `type == \"step\"` agent rows."
    - "Malformed traces, non-step-only traces, or promotion failures do not break the user-visible GUI task result or block background postprocessing completion."
  artifacts:
    - path: "opengui/skills/shortcut_promotion.py"
      provides: "Production trace-to-shortcut promotion seam for Phase 28"
      contains: "class ShortcutPromotionPipeline"
    - path: "nanobot/agent/tools/gui.py"
      provides: "GUI tool wiring for the new production shortcut promotion path"
      contains: "_schedule_trajectory_postprocessing"
    - path: "tests/test_opengui_p28_shortcut_productionization.py"
      provides: "Focused trace filtering and cutover regression coverage"
      contains: "test_promotion_pipeline_filters_final_successful_attempt_steps_only"
  key_links:
    - from: "nanobot/agent/tools/gui.py"
      to: "opengui/skills/shortcut_promotion.py"
      via: "post-run background postprocessing calls the new promotion seam"
      pattern: "ShortcutPromotionPipeline|_promote_shortcut"
    - from: "opengui/skills/shortcut_promotion.py"
      to: "opengui/skills/shortcut_extractor.py"
      via: "promotion reuses `ExtractionPipeline.run()` instead of the legacy extractor"
      pattern: "ExtractionPipeline"
    - from: "opengui/skills/shortcut_promotion.py"
      to: "opengui/trajectory/recorder.py"
      via: "trace parsing honors recorder `type`, `phase`, and `step_index` contract"
      pattern: "type|phase|step_index"
---

<objective>
Cut over GUI postprocessing from the legacy extraction path to a trace-backed shortcut promotion pipeline.

Purpose: satisfy `SXTR-01` by ensuring only valid recorder step events from the final successful attempt can become shortcut candidates, while preserving the existing non-blocking background postprocessing UX.
Output: new `shortcut_promotion.py` production seam, `GuiSubagentTool` wiring to it, and regression tests proving the legacy extractor path is no longer the production shortcut sink.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-shortcut-extraction-productionization/28-RESEARCH.md
@.planning/phases/28-shortcut-extraction-productionization/28-VALIDATION.md
@nanobot/agent/tools/gui.py
@opengui/trajectory/recorder.py
@opengui/skills/shortcut_extractor.py
@opengui/skills/shortcut_store.py
@tests/test_opengui_p8_trajectory.py

<interfaces>
Current production seam in `nanobot/agent/tools/gui.py`:
```python
async def _run_trajectory_postprocessing(self, trace_path: Path, is_success: bool, skill_library: Any, task: str) -> None:
    trajectory_summary = await self._summarize_trajectory(trace_path)
    await self._extract_skill(trace_path, is_success, skill_library)
    await self._maybe_run_evaluation(task=task, trace_path=trace_path, is_success=is_success)
```

Current legacy extraction sink:
```python
from opengui.skills.extractor import SkillExtractor
skill = await extractor.extract_from_file(trace_path, is_success=is_success)
decision, skill_id = await skill_library.add_or_merge(skill)
```

Phase 28 cutover target:
```text
- add `ShortcutPromotionPipeline` in `opengui/skills/shortcut_promotion.py`
- replace `_extract_skill(...)` with `_promote_shortcut(...)`
- update `_schedule_trajectory_postprocessing()` and `_run_trajectory_postprocessing()` to carry `platform` context from `active_backend.platform`
- parse recorder JSONL line-by-line
- if attempt markers exist, keep only the final successful attempt window
- after windowing, keep only `type == "step"` rows with:
  - `phase == "agent"`
  - dict `action` containing non-empty `action_type`
  - non-null `step_index`
- send filtered steps plus metadata into `ExtractionPipeline.run(...)`
- write candidates to `ShortcutSkillStore`, not `SkillLibrary`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing cutover and trace-filter regression tests</name>
  <files>tests/test_opengui_p28_shortcut_productionization.py, tests/test_opengui_p8_trajectory.py</files>
  <read_first>.planning/phases/28-shortcut-extraction-productionization/28-RESEARCH.md, .planning/phases/28-shortcut-extraction-productionization/28-VALIDATION.md, nanobot/agent/tools/gui.py, opengui/trajectory/recorder.py, tests/test_opengui_p8_trajectory.py</read_first>
  <behavior>
    - Test 1: `test_gui_postprocessing_uses_shortcut_promotion_not_legacy_extractor` patches `ShortcutPromotionPipeline.promote_from_trace` and `SkillExtractor.extract_from_file`; after `GuiSubagentTool.execute(...)`, assert the promotion pipeline is awaited and the legacy extractor is not called.
    - Test 2: `test_promotion_pipeline_filters_final_successful_attempt_steps_only` builds a synthetic recorder JSONL containing malformed lines, lifecycle rows, failed-attempt rows, and final-success `step` rows; assert only the final successful attempt's valid agent rows are passed into `ExtractionPipeline.run(...)`.
    - Test 3: `test_promotion_pipeline_skips_non_step_or_malformed_trace_without_store_write` asserts traces lacking promotable step rows return a structured skip and never call `ShortcutSkillStore.add`.
    - Test 4: extend the existing background-postprocessing seam test so the GUI tool still returns before promotion finishes.
  </behavior>
  <action>Create `tests/test_opengui_p28_shortcut_productionization.py` with the exact test names `test_gui_postprocessing_uses_shortcut_promotion_not_legacy_extractor`, `test_promotion_pipeline_filters_final_successful_attempt_steps_only`, and `test_promotion_pipeline_skips_non_step_or_malformed_trace_without_store_write`. In the filtering test, include concrete JSONL rows for `metadata`, `attempt_start`, `attempt_result`, `phase_change`, `step`, and `result`, plus one malformed non-JSON line. Make the successful window contain exactly two valid `step` rows with `phase="agent"` and `action={"action_type": "tap"}` / `{"action_type": "input_text"}` and assert those exact two step indices are retained. Extend `tests/test_opengui_p8_trajectory.py` so the postprocessing task still runs in the background with the new promotion seam.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p28_shortcut_productionization.py` exists and contains the exact test names `test_gui_postprocessing_uses_shortcut_promotion_not_legacy_extractor` and `test_promotion_pipeline_filters_final_successful_attempt_steps_only`
    - `tests/test_opengui_p28_shortcut_productionization.py` contains the exact strings `"attempt_start"`, `"attempt_result"`, `"type": "step"`, and `"phase": "agent"`
    - `tests/test_opengui_p8_trajectory.py` asserts background postprocessing still returns before completion with the new promotion seam
    - `uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p28_shortcut_productionization.py -k "promotion or postprocessing or final_successful_attempt" ` exits non-zero before implementation and exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p28_shortcut_productionization.py -k "promotion or postprocessing or final_successful_attempt"</automated>
  </verify>
  <done>The failing tests define the exact production cutover seam and final-attempt trace filtering behavior before code changes land.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the production shortcut promotion seam and wire GuiSubagentTool to it</name>
  <files>opengui/skills/shortcut_promotion.py, opengui/skills/__init__.py, nanobot/agent/tools/gui.py, tests/test_opengui_p8_trajectory.py, tests/test_opengui_p28_shortcut_productionization.py</files>
  <read_first>.planning/phases/28-shortcut-extraction-productionization/28-RESEARCH.md, nanobot/agent/tools/gui.py, opengui/trajectory/recorder.py, opengui/skills/shortcut_extractor.py, opengui/skills/shortcut_store.py, tests/test_opengui_p28_shortcut_productionization.py</read_first>
  <behavior>
    - `ShortcutPromotionPipeline.promote_from_trace(trace_path: Path, *, is_success: bool, store: ShortcutSkillStore) -> str | None` loads recorder rows, filters to the final successful attempt, and returns `None` when no promotable step rows remain.
    - The pipeline calls `ExtractionPipeline.run(filtered_steps, metadata)` with concrete metadata containing `task`, `platform`, and an app hint derived from retained rows.
    - `GuiSubagentTool._schedule_trajectory_postprocessing()` passes `active_backend.platform` into `_run_trajectory_postprocessing(...)`, and `_run_trajectory_postprocessing()` passes that same platform context into `_promote_shortcut(...)`.
    - `GuiSubagentTool._run_trajectory_postprocessing()` calls `_promote_shortcut(...)` instead of `_extract_skill(...)` and still runs summarization and evaluation as sibling background actions.
    - Promotion failures are logged and swallowed so the GUI task result remains unchanged.
  </behavior>
  <action>Create `opengui/skills/shortcut_promotion.py` with `from __future__ import annotations` and a concrete class `ShortcutPromotionPipeline`. Implement helper methods with these exact responsibilities: `_load_trace_rows(trace_path)` reads JSONL line-by-line and skips malformed lines; `_select_final_successful_attempt(rows)` returns the last successful attempt window when `attempt_start` / `attempt_result` rows exist and otherwise falls back to the whole trace only when the terminal `result.success` is true; `_filter_promotable_steps(rows)` keeps only rows where `type == "step"`, `phase == "agent"`, `action` is a dict, `action.action_type` is non-empty, and `step_index` is present. In `promote_from_trace(...)`, instantiate `ExtractionPipeline()`, build metadata keys `task`, `platform`, and `app`, run the pipeline, and on `ExtractionSuccess` call `store.add(result.candidate)`. Update `nanobot/agent/tools/gui.py` so `_schedule_trajectory_postprocessing(trace_path, is_success, skill_library, task)` becomes `_schedule_trajectory_postprocessing(trace_path, is_success, platform, task)` and `_run_trajectory_postprocessing(trace_path, is_success, task)` becomes `_run_trajectory_postprocessing(trace_path, is_success, platform, task)`. Add `_promote_shortcut(trace_path, is_success, platform)` that constructs `ShortcutSkillStore(store_dir=get_gui_skill_store_root(self._workspace), embedding_provider=self._embedding_adapter)` and delegates to `ShortcutPromotionPipeline`, passing the explicit `platform` through to promotion metadata instead of relying on hidden backend state. Replace the call to `_extract_skill(...)` in `_run_trajectory_postprocessing()` with `_promote_shortcut(...)`. Update `opengui/skills/__init__.py` to export `ShortcutPromotionPipeline` if that module is part of the public package surface.</action>
  <acceptance_criteria>
    - `opengui/skills/shortcut_promotion.py` contains `class ShortcutPromotionPipeline`
    - `opengui/skills/shortcut_promotion.py` contains the exact predicates `row.get("type") == "step"` and `row.get("phase") == "agent"`
    - `nanobot/agent/tools/gui.py` contains `_promote_shortcut`, `_schedule_trajectory_postprocessing(` with a `platform` parameter, and `_run_trajectory_postprocessing(` with a `platform` parameter
    - `nanobot/agent/tools/gui.py` no longer calls `SkillExtractor.extract_from_file` from `_run_trajectory_postprocessing()`
    - `nanobot/agent/tools/gui.py` constructs `ShortcutSkillStore(` inside the new promotion path
    - `uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p28_shortcut_productionization.py -k "promotion or postprocessing or final_successful_attempt"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p28_shortcut_productionization.py -k "promotion or postprocessing or final_successful_attempt"</automated>
  </verify>
  <done>The live GUI post-run path now routes through a dedicated trace-backed shortcut promotion seam and never relies on the legacy `SkillExtractor` for new shortcut promotion.</done>
</task>

</tasks>

<verification>
Run the validation quick slice for the cutover seam: `uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p28_shortcut_productionization.py -k "promotion or postprocessing or final_successful_attempt"`. Confirm the GUI tool still returns before background postprocessing completes and that only final-attempt `step` rows reach the promotion pipeline.
</verification>

<success_criteria>
`SXTR-01` is satisfied when the production post-run GUI path promotes only valid final-attempt recorder step events into the shortcut pipeline and no longer depends on the legacy extractor/library path for new shortcuts.
</success_criteria>

<output>
After completion, create `.planning/phases/28-shortcut-extraction-productionization/28-01-SUMMARY.md`
</output>
