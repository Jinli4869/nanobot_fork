---
phase: 32-prefix-only-shortcut-extraction-and-canonicalization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - opengui/skills/shortcut_promotion.py
  - tests/test_opengui_p28_shortcut_productionization.py
autonomous: true
requirements: [SXTR-05, SXTR-06]
must_haves:
  truths:
    - "Shortcut promotion canonicalizes successful agent-step rows before extraction and only stores the stable reusable prefix rather than the full replay tail."
    - "Prefix truncation no longer stops on broad long-horizon hints alone; it keeps reusable setup steps and cuts at the first task-specific payload or commit boundary."
    - "Replay-like duplicate waits and repeated unchanged-UI taps do not survive into stored shortcut step sequences."
  artifacts:
    - path: "opengui/skills/shortcut_promotion.py"
      provides: "Deterministic canonicalize -> reusable-prefix promotion flow"
      contains: "_canonicalize_steps"
    - path: "opengui/skills/shortcut_promotion.py"
      provides: "Stable-boundary prefix detection"
      contains: "_find_reusable_boundary"
    - path: "tests/test_opengui_p28_shortcut_productionization.py"
      provides: "Regression coverage for prefix truncation and canonicalization"
      contains: "test_promotion_pipeline_canonicalizes_duplicate_waits_and_unchanged_ui_retries"
  key_links:
    - from: "opengui/skills/shortcut_promotion.py"
      to: "tests/test_opengui_p28_shortcut_productionization.py"
      via: "Promotion canonicalization and prefix rules are exercised by new trace fixtures"
      pattern: "canonicalize|reusable_boundary|unchanged_ui"
    - from: "opengui/skills/shortcut_promotion.py"
      to: "opengui/skills/shortcut_extractor.py"
      via: "Only canonicalized reusable-prefix steps are passed into ExtractionPipeline.run()"
      pattern: "ExtractionPipeline"
---

<objective>
Make the promotion seam emit concise reusable prefixes by canonicalizing replay noise before extraction and cutting traces at a deterministic stable boundary.

Purpose: satisfy `SXTR-05` and `SXTR-06` at the right seam: `ShortcutPromotionPipeline` should normalize successful trace rows before storage, not rely on store merge/versioning to clean up noisy internals later.
Output: a deterministic `canonicalize -> reusable prefix -> extract` flow in `shortcut_promotion.py` and regression tests proving long traces no longer persist downstream task tails or duplicate/no-op steps.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-RESEARCH.md
@.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-VALIDATION.md
@opengui/skills/shortcut_promotion.py
@opengui/skills/shortcut_extractor.py
@tests/test_opengui_p28_shortcut_productionization.py

<interfaces>
Phase 32 promotion flow target:
```text
rows -> final successful attempt -> promotable step rows
     -> canonicalized step rows
     -> reusable prefix rows
     -> ExtractionPipeline.run(...)
```

Concrete helpers to introduce or strengthen in `opengui/skills/shortcut_promotion.py`:
```text
- _canonicalize_steps(steps: list[dict[str, Any]]) -> list[dict[str, Any]]
- _find_reusable_boundary(steps: list[dict[str, Any]]) -> int | None
- _is_task_specific_payload_step(step: dict[str, Any]) -> bool
- _is_commit_or_branch_step(step: dict[str, Any]) -> bool
```

Canonicalization rules to encode:
```text
- collapse consecutive wait steps that keep the same app/state evidence
- drop consecutive duplicate taps/clicks on unchanged UI when action_type, normalized target, normalized params, valid_state, expected_state, and app are unchanged
- keep the richer-evidence instance when duplicate candidates differ only in state richness
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock reusable-boundary and canonicalization behavior with failing trace-fixture tests</name>
  <files>tests/test_opengui_p28_shortcut_productionization.py</files>
  <read_first>.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-RESEARCH.md, .planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-VALIDATION.md, opengui/skills/shortcut_promotion.py, tests/test_opengui_p28_shortcut_productionization.py</read_first>
  <action>Add these exact async tests to `tests/test_opengui_p28_shortcut_productionization.py`: `test_promotion_pipeline_keeps_reusable_setup_before_payload_boundary`, `test_promotion_pipeline_canonicalizes_duplicate_waits_and_unchanged_ui_retries`, and `test_promotion_pipeline_keeps_richer_state_evidence_when_collapsing_duplicates`. In the reusable-boundary test, build a successful Android trace with step indices `0=open compose`, `1=focus recipient field`, `2=input_text recipient payload`, `3=send message`; assert the promotion seam forwards only step indices `[0, 1]` into `ExtractionPipeline.run(...)`. In the canonicalization test, build a successful trace containing two consecutive identical `wait` steps plus two identical `tap` retries on unchanged UI and assert only one wait and one tap survive into the captured `steps` passed to `ExtractionPipeline.run(...)`. In the richer-evidence test, build duplicate taps where the later step adds a non-empty `expected_state` and assert the retained row is the richer-evidence instance rather than the empty-state duplicate.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p28_shortcut_productionization.py` contains the exact test names `test_promotion_pipeline_keeps_reusable_setup_before_payload_boundary` and `test_promotion_pipeline_canonicalizes_duplicate_waits_and_unchanged_ui_retries`
    - `tests/test_opengui_p28_shortcut_productionization.py` contains the exact strings `Open compose`, `Focus recipient field`, `input_text`, `wait`, and `Send message`
    - At least one new test captures the `steps` argument passed to `ExtractionPipeline.run(...)` and asserts exact retained `step_index` values
    - `uv run pytest tests/test_opengui_p28_shortcut_productionization.py -k "reusable_boundary or canonicalizes_duplicate_waits or richer_state_evidence" -q` exits non-zero before implementation and exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p28_shortcut_productionization.py -k "reusable_boundary or canonicalizes_duplicate_waits or richer_state_evidence" -q</automated>
  </verify>
  <done>The tests define the exact boundary and replay-noise cleanup rules before the promotion seam changes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement canonicalization and stable reusable-boundary detection in the promotion seam</name>
  <files>opengui/skills/shortcut_promotion.py, tests/test_opengui_p28_shortcut_productionization.py</files>
  <read_first>.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-RESEARCH.md, opengui/skills/shortcut_promotion.py, opengui/skills/shortcut_extractor.py, tests/test_opengui_p28_shortcut_productionization.py</read_first>
  <action>Update `opengui/skills/shortcut_promotion.py` so `promote_from_trace(...)` runs `steps = self._canonicalize_steps(steps)` before prefix truncation, then replaces the current `_truncate_to_reusable_prefix()` heuristic with a boundary-driven implementation backed by `_find_reusable_boundary()`. Encode these concrete boundary rules: keep navigational/opening steps such as `tap`, `click`, `press`, `hotkey`, and non-payload waits while the trace is still moving into the reusable surface; cut before the first `input_text` step whose `action.text` is non-empty and not already templated; cut before any step whose action/model output indicates commit behavior using the exact lowercase hints `send`, `submit`, `confirm`, `pay`, `delete`, `share`, `post`, `publish`, or `checkout`; cut before branches whose `expected_state` or `model_output` depends on downstream task success rather than reaching the reusable UI surface. Implement `_canonicalize_steps()` so it collapses consecutive `wait` rows with identical normalized app/state evidence, removes consecutive duplicate taps/clicks on unchanged UI, and keeps the row with the richest state evidence by preferring more non-empty `valid_state` / `expected_state` strings when duplicates conflict. Preserve original row dicts for retained steps so downstream extraction and provenance still use the actual step payloads. Do not move canonicalization into `ShortcutSkillStore.add_or_merge()`.</action>
  <acceptance_criteria>
    - `opengui/skills/shortcut_promotion.py` contains `def _canonicalize_steps(` and `def _find_reusable_boundary(`
    - `opengui/skills/shortcut_promotion.py` contains the exact commit hints `send`, `submit`, `confirm`, `pay`, `delete`, `share`, `post`, `publish`, and `checkout`
    - `opengui/skills/shortcut_promotion.py` calls `_canonicalize_steps(` before `_truncate_to_reusable_prefix(` or equivalent reusable-prefix slicing
    - `uv run pytest tests/test_opengui_p28_shortcut_productionization.py -k "reusable_boundary or canonicalizes_duplicate_waits or richer_state_evidence" -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p28_shortcut_productionization.py -k "reusable_boundary or canonicalizes_duplicate_waits or richer_state_evidence" -q</automated>
  </verify>
  <done>The promotion seam now stores only canonicalized reusable prefixes instead of replay-like long tails.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_opengui_p28_shortcut_productionization.py -k "reusable_boundary or canonicalizes_duplicate_waits or richer_state_evidence" -q` and confirm captured extraction inputs exclude task-specific payload/commit steps plus duplicate replay noise.
</verification>

<success_criteria>
`SXTR-05` and `SXTR-06` are satisfied when long successful traces keep the stable setup prefix, drop downstream task-specific tails, and remove duplicate waits / unchanged-UI retries before extraction and storage.
</success_criteria>

<output>
After completion, create `.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-01-SUMMARY.md`
</output>
