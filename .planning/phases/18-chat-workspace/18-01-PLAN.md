---
phase: 18-chat-workspace
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - nanobot/tui/app.py
  - nanobot/tui/contracts.py
  - nanobot/tui/dependencies.py
  - nanobot/tui/routes/__init__.py
  - nanobot/tui/routes/chat.py
  - nanobot/tui/schemas/__init__.py
  - nanobot/tui/schemas/chat.py
  - nanobot/tui/services/__init__.py
  - nanobot/tui/services/chat.py
  - tests/test_tui_p18_chat.py
autonomous: true
requirements:
  - CHAT-01
must_haves:
  truths:
    - "A browser client can create a new chat session without using the terminal."
    - "A browser client can send follow-up messages into the same persisted nanobot session."
    - "Browser chat mutations reuse the existing direct-chat execution path instead of introducing a second chat engine."
  artifacts:
    - path: "nanobot/tui/dependencies.py"
      provides: "Explicit browser-chat runtime factory seam for provider, bus, cron, and AgentLoop construction"
      exports: ["get_chat_workspace_service", "get_chat_runtime_factory"]
    - path: "nanobot/tui/routes/chat.py"
      provides: "Session creation, transcript read, and message submission endpoints"
      exports: ["router"]
    - path: "nanobot/tui/services/chat.py"
      provides: "Thin chat workspace adapter over SessionManager and AgentLoop.process_direct()"
    - path: "tests/test_tui_p18_chat.py"
      provides: "Wave 0 and route-level coverage for session-backed chat APIs"
  key_links:
    - from: "nanobot/tui/routes/chat.py"
      to: "nanobot/tui/services/chat.py"
      via: "FastAPI dependency injection"
      pattern: "Depends\\(get_chat_workspace_service\\)"
    - from: "nanobot/tui/dependencies.py"
      to: "nanobot/cli/commands.py"
      via: "Browser chat runtime factory reuses provider/cron/loop wiring ideas without importing Typer commands or channel startup"
      pattern: "_load_runtime_config|_make_provider|CronService|MessageBus|AgentLoop"
    - from: "nanobot/tui/services/chat.py"
      to: "nanobot.agent.loop.AgentLoop.process_direct"
      via: "direct-chat adapter call"
      pattern: "process_direct\\("
    - from: "nanobot/tui/services/chat.py"
      to: "nanobot.session.manager.SessionManager"
      via: "tui:{session_id} persistence and transcript reads"
      pattern: "SessionManager|session_key"
---

<objective>
Create the Phase 18 chat workspace foundation: typed chat contracts plus session-backed FastAPI endpoints for create/read/send flows, all isolated under `nanobot/tui`.

Purpose: satisfy `CHAT-01` without reopening the Phase 17 boundary or changing existing CLI chat behavior.
Output: chat schemas, a chat service/route layer, and route tests that prove browser chat reuses persisted nanobot sessions.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-chat-workspace/18-RESEARCH.md
@.planning/phases/18-chat-workspace/18-VALIDATION.md
@.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-01-SUMMARY.md
@.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-02-SUMMARY.md
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/tui/app.py
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/tui/contracts.py
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/tui/dependencies.py
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/session/manager.py
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/agent/loop.py
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/cli/commands.py

<interfaces>
From `nanobot/tui/contracts.py`:
```python
@dataclass(slots=True)
class SessionContract:
    workspace_path: Path
    list_sessions: Callable[[], list[dict[str, Any]]]
```

From `nanobot/session/manager.py`:
```python
class SessionManager:
    def get_or_create(self, key: str) -> Session: ...
    def save(self, session: Session) -> None: ...
    def list_sessions(self) -> list[dict[str, Any]]: ...
```

From `nanobot/agent/loop.py`:
```python
async def process_direct(
    self,
    content: str,
    session_key: str = "cli:direct",
    channel: str = "cli",
    chat_id: str = "direct",
    on_progress: Callable[[str], Awaitable[None]] | None = None,
) -> str: ...
```

From `nanobot/cli/commands.py`:
```python
config = _load_runtime_config(config, workspace)
bus = MessageBus()
provider = _make_provider(config)
cron = CronService(cron_store_path)
agent_loop = AgentLoop(..., cron_service=cron, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define chat contracts, schemas, and Wave 0 route expectations</name>
  <files>nanobot/tui/contracts.py, nanobot/tui/schemas/__init__.py, nanobot/tui/schemas/chat.py, tests/test_tui_p18_chat.py</files>
  <behavior>
    - Test 1: creating a browser chat session returns a stable `session_id`, `session_key`, and empty or persisted transcript metadata under the `tui:` namespace.
    - Test 2: reading `/chat/sessions/{session_id}` returns transcript messages from `SessionManager` instead of an in-memory browser-only cache.
    - Test 3: message submission tests assert the browser path does not reuse `cli:direct` and does not require Typer command routing.
  </behavior>
  <action>Extend the Phase 17 contract layer with a dedicated chat contract and typed request/response models for session creation, transcript retrieval, and message submission. Create `tests/test_tui_p18_chat.py` first with route/service expectations that lock the browser session namespace to `tui:{session_id}` and verify durable history comes from `SessionManager`. Do not add any frontend files, Vite assets, or React-facing shell code in this task.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p18_chat.py::test_chat_routes_create_and_reuse_session_backed_conversations tests/test_tui_p18_chat.py::test_chat_history_route_reads_persisted_session_state -q</automated>
  </verify>
  <done>Chat request/response contracts exist, the new test file is in place, and the tests describe the exact Phase 18 browser chat behavior before implementation wiring begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chat workspace service, dependencies, routes, and app wiring</name>
  <files>nanobot/tui/app.py, nanobot/tui/dependencies.py, nanobot/tui/routes/__init__.py, nanobot/tui/routes/chat.py, nanobot/tui/services/__init__.py, nanobot/tui/services/chat.py, tests/test_tui_p18_chat.py</files>
  <behavior>
    - Test 1: `POST /chat/sessions` creates or returns a browser session keyed as `tui:{session_id}` without instantiating Typer commands.
    - Test 2: `POST /chat/sessions/{session_id}/messages` calls `AgentLoop.process_direct(..., channel="tui", chat_id=session_id, on_progress=...)`.
    - Test 3: `GET /chat/sessions/{session_id}` returns the persisted transcript and metadata for follow-up browser turns.
  </behavior>
  <action>Implement `ChatWorkspaceService` as a thin adapter over `SessionManager` and an injected `AgentLoop` factory. In `nanobot/tui/dependencies.py`, add an explicit browser-chat runtime seam such as `get_chat_runtime_factory()` that owns `MessageBus`, provider construction, `CronService`, and `AgentLoop` initialization using the existing config/runtime helpers, while explicitly excluding Typer command routing, channel-manager startup, and CLI-side template sync. Add a dedicated `nanobot/tui/routes/chat.py` router and opt it into `create_app(..., include_runtime_routes=True)` alongside the existing read-only routes. Keep all browser chat behavior inside `nanobot/tui`; do not change `nanobot/cli/commands.py`, do not introduce Typer subcommands, and do not absorb any Phase 20 UI shell work.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p18_chat.py::test_chat_routes_create_and_reuse_session_backed_conversations tests/test_tui_p18_chat.py::test_chat_history_route_reads_persisted_session_state tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py -q</automated>
  </verify>
  <done>Runtime-enabled TUI app instances expose chat create/read/send endpoints, the chat service reuses `SessionManager` and `AgentLoop.process_direct()`, and Phase 17 runtime/config regressions still pass unchanged.</done>
</task>

</tasks>

<verification>
Run the targeted Phase 18 chat tests plus the Phase 17 runtime/config slice to prove the new router stays inside the existing `nanobot/tui` boundary.
</verification>

<success_criteria>
`CHAT-01` is satisfied when a browser-oriented API can create a `tui:` session, send a follow-up message through the existing direct-chat runtime path, and retrieve the persisted transcript without any CLI command changes.
</success_criteria>

<output>
After completion, create `.planning/phases/18-chat-workspace/18-chat-workspace-01-SUMMARY.md`
</output>
