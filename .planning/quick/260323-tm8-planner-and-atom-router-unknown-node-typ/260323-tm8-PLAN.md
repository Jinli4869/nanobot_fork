# Quick Task 260323-tm8 Plan

**Description:** 修复 planner 输出大写 `AND/ATOM` 时 router 报 `Unknown node type` 的 bug，并补测试与提交。  
**Date:** 2026-03-23  
**Mode:** quick

## Objective

修复 planner / router 链路中对 plan node type 大小写敏感的问题，使 planner 即使返回 `AND`、`OR`、`ATOM` 也能被正确解析和执行，不再在运行时失败为 `Unknown node type: AND`。

## Constraints

- 保持修复最小，不改动无关执行逻辑。
- 必须补回归测试，覆盖解析与执行两个层面。
- 不处理无关未跟踪文件 `TEST.ipynb`。

## Tasks

1. 修复 `PlanNode.from_dict()` 的 node type 标准化与校验
2. 在 `TreeRouter` 中增加大小写无关兜底，兼容历史或外部构造的 `PlanNode`
3. 补回归测试并完成提交

## Verification

- `uv run pytest tests/test_opengui_p8_planning.py tests/test_opengui_agent_loop.py -q`
- `uv run python -m py_compile nanobot/agent/planner.py nanobot/agent/router.py tests/test_opengui_p8_planning.py tests/test_opengui_agent_loop.py`

## Deliverables

- `260323-tm8-PLAN.md`
- `260323-tm8-SUMMARY.md`
- 更新 `.planning/STATE.md`
