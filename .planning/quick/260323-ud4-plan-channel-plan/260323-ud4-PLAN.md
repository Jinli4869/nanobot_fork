# Quick Task 260323-ud4 Plan

**Description:** 在执行 plan 之后向当前 channel 发送 plan 预览消息，并补测试与提交。  
**Date:** 2026-03-23  
**Mode:** quick

## Objective

当任务进入 planner 分支时，在执行 tree router 之前先向当前消息所属的 channel 发送一条 plan 预览消息，让 TUI / Telegram / 其他 channel 用户能看到本轮执行计划。

## Constraints

- 只在 planning 分支发送，不影响直接 agent loop 路径。
- 预览消息应通过现有 bus/outbound 机制发送。
- 必须保留现有 `_format_plan_tree()` 输出风格，并补测试验证消息发送行为。

## Tasks

1. 在 `AgentLoop._plan_and_execute()` 中接收 channel 上下文
2. 在拿到 planner tree 后发送 plan preview outbound message
3. 补测试并完成提交

## Verification

- `uv run pytest tests/test_opengui_p8_planning.py tests/test_opengui_agent_loop.py -q`
- `uv run python -m py_compile nanobot/agent/loop.py tests/test_opengui_p8_planning.py`

## Deliverables

- `260323-ud4-PLAN.md`
- `260323-ud4-SUMMARY.md`
- 更新 `.planning/STATE.md`
