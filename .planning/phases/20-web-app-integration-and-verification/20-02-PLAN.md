---
phase: 20-web-app-integration-and-verification
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - tests/test_tui_p20_static.py
  - nanobot/tui/static.py
  - nanobot/tui/app.py
  - nanobot/tui/__main__.py
  - nanobot/tui/web/__init__.py
  - pyproject.toml
  - tests/test_tui_p17_config.py
autonomous: true
requirements: [SHIP-01]
must_haves:
  truths:
    - "Built frontend assets are discoverable and served from the local FastAPI app in packaged mode."
    - "Browser deep links such as `/chat/{session_id}` and `/operations?runId=...` fall back to the SPA shell while existing `/health`, `/chat`, `/runtime`, and `/tasks` routes keep their current API semantics."
    - "`python -m nanobot.tui` remains the canonical Python startup seam, and an optional packaged script launches the same app without replacing the existing `nanobot` CLI."
  artifacts:
    - path: "tests/test_tui_p20_static.py"
      provides: "Packaged static-serving and startup regression coverage for Phase 20"
      min_lines: 80
    - path: "nanobot/tui/static.py"
      provides: "Packaged frontend asset discovery and SPA fallback helper"
      contains: "importlib.resources"
    - path: "pyproject.toml"
      provides: "Packaged web asset inclusion and `nanobot-tui` startup script"
      contains: "nanobot-tui"
  key_links:
    - from: "nanobot/tui/app.py"
      to: "nanobot/tui/static.py"
      via: "frontend-serving opt-in for runtime apps"
      pattern: "serve_frontend"
    - from: "nanobot/tui/__main__.py"
      to: "nanobot/tui/app.py"
      via: "canonical module entry builds runtime app with frontend enabled"
      pattern: "create_app"
    - from: "pyproject.toml"
      to: "nanobot/tui/web/dist"
      via: "Hatch include rules and console script metadata"
      pattern: "nanobot/tui/web/dist"
---

<objective>
Wire the packaged and development startup seams for the Phase 20 web app so the React/Vite frontend can be served safely from FastAPI without breaking the existing CLI-first runtime boundary.

Purpose: Satisfy `SHIP-01` by connecting the Phase 20 SPA shell to the existing `python -m nanobot.tui` seam, packaged assets, and local development workflow.
Output: Static-serving regressions, asset discovery helpers, FastAPI SPA fallback wiring, and packaging metadata for the web workspace.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md
@.planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md
@.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-02-SUMMARY.md
@pyproject.toml
@nanobot/tui/app.py
@nanobot/tui/__main__.py
@nanobot/tui/config.py
@tests/test_tui_p17_runtime.py
@tests/test_tui_p17_config.py
@nanobot/tui/web/package.json
@nanobot/tui/web/vite.config.ts

<interfaces>
Current startup seam:
```python
def main(config_path: str | Path | None = None) -> None:
    config = load_config(resolved_config_path)
    runtime = resolve_tui_runtime_config(config)
    app = create_app(config=config, include_runtime_routes=True)
    uvicorn.run(app, host=runtime.host, port=runtime.port, reload=runtime.reload, log_level=runtime.log_level)
```

Constraints to preserve:
```text
- `python -m nanobot.tui` remains canonical
- existing API route paths stay unchanged
- the default `create_app()` path stays safe for health-only/import-safe tests
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add packaged static-serving and SPA fallback regression coverage</name>
  <files>tests/test_tui_p20_static.py</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, nanobot/tui/app.py, nanobot/tui/__main__.py, pyproject.toml, tests/test_tui_p17_runtime.py, tests/test_tui_p17_config.py</read_first>
  <behavior>
    - Test 1: `GET /` serves built `index.html` when packaged frontend assets are present.
    - Test 2: deep links such as `/chat/demo-session` and `/operations?runId=run-123` return the SPA shell instead of a raw 404.
    - Test 3: `/health`, `/chat`, `/runtime`, and `/tasks` still bypass the SPA fallback and keep their API behavior.
    - Test 4: missing build assets return deterministic startup guidance rather than a misleading blank HTML response.
  </behavior>
  <action>Create `tests/test_tui_p20_static.py` first with failing coverage for packaged root serving, SPA deep-link fallback, API-route bypass, and missing-assets guidance. Reuse `create_app(...)` in tests instead of booting uvicorn. Keep the expected route vocabulary rooted in the existing backend: do not introduce `/api/*` aliases, auth redirects, or remote-host assumptions.</action>
  <acceptance_criteria>
    - `tests/test_tui_p20_static.py` contains explicit assertions for `/`, `/chat/demo-session`, `/operations?runId=run-123`, and at least one existing API route.
    - The file asserts a deterministic missing-assets response rather than silent success.
    - The task leaves the current Phase 17 startup tests untouched until implementation lands.
  </acceptance_criteria>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p20_static.py -q</automated>
  </verify>
  <done>Wave 0 startup and static-serving coverage exists before backend packaging code is changed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement packaged asset discovery and FastAPI SPA fallback wiring</name>
  <files>nanobot/tui/static.py, nanobot/tui/app.py</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, tests/test_tui_p20_static.py, nanobot/tui/app.py, nanobot/tui/routes/__init__.py, nanobot/tui/routes/chat.py, nanobot/tui/routes/runtime.py, nanobot/tui/routes/tasks.py</read_first>
  <action>Implement `nanobot/tui/static.py` as the single packaged-asset helper. Resolve `nanobot/tui/web/dist` through `importlib.resources`, expose helpers for `dist` existence and `index.html`, and install static-file plus SPA-fallback routes in a way FastAPI can call without guessing the current working directory. Update `nanobot/tui/app.py` so the default `create_app()` behavior remains health-only and non-frontend, but runtime-enabled apps can opt into frontend serving through an explicit flag such as `serve_frontend=True`. The SPA fallback must serve `index.html` for `/`, `/chat/...`, and `/operations...` only when built assets exist; existing API routes must continue to win exact path matching. When assets are missing, return clear startup guidance that tells the operator to build the frontend or use the Vite dev server instead of returning a blank page.</action>
  <acceptance_criteria>
    - `nanobot/tui/static.py` contains `importlib.resources`.
    - `nanobot/tui/app.py` still exposes a default `create_app()` path that does not enable frontend serving implicitly.
    - `nanobot/tui/app.py` contains an explicit frontend opt-in flag and mounts the SPA fallback only for runtime-enabled apps.
    - `uv run --extra dev pytest tests/test_tui_p20_static.py::test_root_serves_built_index_html tests/test_tui_p20_static.py::test_api_routes_bypass_spa_fallback -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p20_static.py::test_root_serves_built_index_html tests/test_tui_p20_static.py::test_api_routes_bypass_spa_fallback -q</automated>
  </verify>
  <done>Packaged frontend assets can be served from FastAPI without changing any existing browser API routes or the health-only default app seam.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the canonical startup seam and package the built frontend assets</name>
  <files>nanobot/tui/__main__.py, nanobot/tui/web/__init__.py, pyproject.toml, tests/test_tui_p17_config.py</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, .planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-02-SUMMARY.md, nanobot/tui/__main__.py, nanobot/tui/config.py, pyproject.toml, tests/test_tui_p17_config.py, tests/test_tui_p20_static.py</read_first>
  <action>Keep `python -m nanobot.tui` as the canonical runtime seam, but update it to build the app with both runtime routes and packaged frontend serving enabled through the new explicit app flag. Add `nanobot/tui/web/__init__.py` so `importlib.resources` can resolve packaged assets safely. Update `pyproject.toml` to add `nanobot-tui = "nanobot.tui.__main__:main"` under `[project.scripts]` without touching the existing `nanobot` and `opengui` entries, and extend Hatch include rules so `nanobot/tui/web/dist/**`, `nanobot/tui/web/index.html`, and the minimum package files needed for asset lookup ship in wheel and sdist builds. Extend `tests/test_tui_p17_config.py` so the startup seam now asserts `create_app(... include_runtime_routes=True, serve_frontend=True ...)` while preserving the local-first host/port defaults from Phase 17.</action>
  <acceptance_criteria>
    - `pyproject.toml` contains the literal script entry `nanobot-tui = "nanobot.tui.__main__:main"`.
    - `pyproject.toml` includes `nanobot/tui/web/dist` in build metadata.
    - `nanobot/tui/__main__.py` still loads config through `resolve_tui_runtime_config` and still calls `uvicorn.run`.
    - `tests/test_tui_p17_config.py` asserts frontend serving is enabled through the canonical startup seam.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run build && uv run --extra dev pytest tests/test_tui_p20_static.py tests/test_tui_p17_config.py -q</automated>
  </verify>
  <done>The packaged app can serve built frontend assets through the same canonical Python startup seam, and CLI behavior remains separated from the new web launch path.</done>
</task>

</tasks>

<verification>
Use the `20-02` commands from `20-VALIDATION.md`. If the local environment still blocks `uv run --extra dev pytest ...`, use the documented fallback `.venv/bin/python -m pytest tests/test_tui_p20_static.py tests/test_tui_p17_config.py -q` after a successful frontend build.
</verification>

<success_criteria>
`SHIP-01` is implemented end to end: the SPA can be built, packaged assets are discoverable from the installed Python package, FastAPI serves the root shell and deep links, and `python -m nanobot.tui` remains the canonical runtime seam.
</success_criteria>

<output>
After completion, create `.planning/phases/20-web-app-integration-and-verification/20-web-app-integration-and-verification-02-SUMMARY.md`
</output>
