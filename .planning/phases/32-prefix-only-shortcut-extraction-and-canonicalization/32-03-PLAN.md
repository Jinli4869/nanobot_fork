---
phase: 32-prefix-only-shortcut-extraction-and-canonicalization
plan: "03"
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - tests/test_opengui_p28_shortcut_productionization.py
  - tests/test_opengui_p31_shortcut_observability.py
autonomous: true
requirements: [SXTR-05, SXTR-06, SXTR-07]
must_haves:
  truths:
    - "Canonicalized reusable-prefix shortcuts still execute successfully through the real shortcut executor path after Phase 32 cleanup and placeholder widening."
    - "Regression coverage proves prefix truncation, canonicalization, and placeholder emission behave deterministically on representative traces."
    - "Phase 28/31 shortcut promotion and execution seams remain green after the new extraction-quality rules land."
  artifacts:
    - path: "tests/test_opengui_p28_shortcut_productionization.py"
      provides: "Deterministic end-to-end promotion assertions after canonicalization and slot widening"
      contains: "test_promotion_pipeline_stores_canonicalized_prefix_with_parameter_slots"
    - path: "tests/test_opengui_p31_shortcut_observability.py"
      provides: "Execution seam asserting canonicalized promoted shortcuts still run with grounding"
      contains: "test_android_canonicalized_prefix_executes_with_grounded_placeholders"
  key_links:
    - from: "tests/test_opengui_p28_shortcut_productionization.py"
      to: "opengui/skills/shortcut_promotion.py"
      via: "Stored shortcut shape is asserted after canonicalization and prefix slicing"
      pattern: "canonicalized_prefix|parameter_slots"
    - from: "tests/test_opengui_p31_shortcut_observability.py"
      to: "opengui/skills/multi_layer_executor.py"
      via: "Promoted canonicalized shortcuts still execute through the real runtime seam"
      pattern: "execution_seam|GroundingResult"
---

<objective>
Prove the new Phase 32 extraction-quality rules are deterministic and runtime-safe by locking the promotion-to-execution seam with focused regressions.

Purpose: Phase 32 is only complete if concise canonicalized shortcuts still store correctly and execute through the real executor path after placeholder widening.
Output: end-to-end promotion and execution regressions covering canonicalized prefix storage, parameter-slot emission, and grounded execution compatibility.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-RESEARCH.md
@.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-VALIDATION.md
@opengui/skills/shortcut_promotion.py
@opengui/skills/shortcut_extractor.py
@opengui/skills/multi_layer_executor.py
@tests/test_opengui_p28_shortcut_productionization.py
@tests/test_opengui_p31_shortcut_observability.py

<interfaces>
End-to-end trace should prove:
```text
- stored shortcut source_step_indices reflect only the canonicalized reusable prefix
- stored parameter_slots include widened placeholder slots
- execution still succeeds when grounding provides live coordinates/selector params and step.parameters carry generalized payloads
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing promotion and execution seam regressions for canonicalized prefixes</name>
  <files>tests/test_opengui_p28_shortcut_productionization.py, tests/test_opengui_p31_shortcut_observability.py</files>
  <read_first>.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-RESEARCH.md, opengui/skills/shortcut_promotion.py, opengui/skills/shortcut_extractor.py, opengui/skills/multi_layer_executor.py, tests/test_opengui_p28_shortcut_productionization.py, tests/test_opengui_p31_shortcut_observability.py</read_first>
  <action>Add `test_promotion_pipeline_stores_canonicalized_prefix_with_parameter_slots` to `tests/test_opengui_p28_shortcut_productionization.py` and `test_android_canonicalized_prefix_executes_with_grounded_placeholders` to `tests/test_opengui_p31_shortcut_observability.py`. In the storage test, promote a successful Android trace containing setup steps, duplicate waits, a dynamic recipient selector, a payload `input_text`, and a final send step; assert the stored shortcut keeps only the setup prefix step indices, removes duplicate waits, and exposes parameter-slot names including `recipient` while excluding the send step. In the execution-seam test, promote a trace whose retained prefix contains a generalized selector-like field plus grounded tap coordinates, then execute the promoted shortcut through `ShortcutExecutor` with a deterministic fake grounder that returns live `x/y` values and any selector bindings needed by the runtime. Assert `ShortcutExecutionSuccess`, assert the executed tap uses the grounded coordinates, and assert the promoted shortcut’s `parameter_slots` still include the expected placeholder-backed slot names.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p28_shortcut_productionization.py` contains `test_promotion_pipeline_stores_canonicalized_prefix_with_parameter_slots`
    - `tests/test_opengui_p31_shortcut_observability.py` contains `test_android_canonicalized_prefix_executes_with_grounded_placeholders`
    - The new storage test asserts exact `source_step_indices` retained after promotion
    - The new execution test asserts grounded coordinates and checks `parameter_slots`
    - `uv run pytest tests/test_opengui_p28_shortcut_productionization.py tests/test_opengui_p31_shortcut_observability.py -k "canonicalized_prefix or grounded_placeholders" -q` exits non-zero before implementation and exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p28_shortcut_productionization.py tests/test_opengui_p31_shortcut_observability.py -k "canonicalized_prefix or grounded_placeholders" -q</automated>
  </verify>
  <done>The regressions lock the final stored shape and runtime seam expected from Phase 32.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Finalize deterministic regression coverage and rerun the shortcut extraction/execution slice</name>
  <files>tests/test_opengui_p28_shortcut_productionization.py, tests/test_opengui_p31_shortcut_observability.py</files>
  <read_first>.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-VALIDATION.md, tests/test_opengui_p28_shortcut_productionization.py, tests/test_opengui_p31_shortcut_observability.py</read_first>
  <action>Implement whatever small fixture or assertion updates are required so the new end-to-end canonicalized-prefix tests pass without weakening prior Phase 28 or Phase 31 guarantees. Keep the fake-grounder and fake-backend patterns already used by `tests/test_opengui_p31_shortcut_observability.py`. After the new tests pass, run the exact regression slice `uv run pytest tests/test_opengui_p26_quality_gated_extraction.py tests/test_opengui_p28_shortcut_productionization.py tests/test_opengui_p30_stable_shortcut_execution.py tests/test_opengui_p31_shortcut_observability.py -q` and update any broken fixture assumptions inside the touched test files only; do not move Phase 32 logic into unrelated runtime/store modules during closeout.</action>
  <acceptance_criteria>
    - `uv run pytest tests/test_opengui_p28_shortcut_productionization.py tests/test_opengui_p31_shortcut_observability.py -k "canonicalized_prefix or grounded_placeholders" -q` exits 0
    - `uv run pytest tests/test_opengui_p26_quality_gated_extraction.py tests/test_opengui_p28_shortcut_productionization.py tests/test_opengui_p30_stable_shortcut_execution.py tests/test_opengui_p31_shortcut_observability.py -q` exits 0
    - No new Phase 32 assertions depend on full-tail trace storage or raw duplicate wait steps
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p26_quality_gated_extraction.py tests/test_opengui_p28_shortcut_productionization.py tests/test_opengui_p30_stable_shortcut_execution.py tests/test_opengui_p31_shortcut_observability.py -q</automated>
  </verify>
  <done>Phase 32 closes with deterministic coverage that the concise stored shortcuts still execute safely through the existing runtime seam.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_opengui_p26_quality_gated_extraction.py tests/test_opengui_p28_shortcut_productionization.py tests/test_opengui_p30_stable_shortcut_execution.py tests/test_opengui_p31_shortcut_observability.py -q` and confirm canonicalized reusable-prefix shortcuts persist with the right slots and still execute successfully.
</verification>

<success_criteria>
Phase 32 is complete when canonicalized reusable-prefix shortcuts are stored deterministically, expose placeholder-backed parameter slots for task-varying inputs, and still run through the real executor seam with grounded live values.
</success_criteria>

<output>
After completion, create `.planning/phases/32-prefix-only-shortcut-extraction-and-canonicalization/32-03-SUMMARY.md`
</output>
