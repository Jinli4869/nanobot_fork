---
phase: 15-intervention-safety-and-handoff
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - opengui/action.py
  - opengui/prompts/system.py
  - opengui/agent.py
  - tests/test_opengui_p15_intervention.py
autonomous: true
requirements:
  - SAFE-01

must_haves:
  truths:
    - "Phase 15 introduces an explicit intervention request contract instead of overloading `done` or assistant free text."
    - "The model-facing tool schema and prompt both advertise `request_intervention` as a first-class action."
    - "Wave 0 tests lock the intervention contract before deeper pause/resume orchestration begins."
  artifacts:
    - path: "opengui/action.py"
      provides: "Validated action parsing for `request_intervention`"
      contains: "request_intervention"
    - path: "opengui/prompts/system.py"
      provides: "System prompt tool schema that teaches the model when to request intervention"
      contains: "request_intervention"
    - path: "opengui/agent.py"
      provides: "Agent tool schema that accepts the intervention action"
      contains: "request_intervention"
    - path: "tests/test_opengui_p15_intervention.py"
      provides: "Wave 0 regression coverage for the intervention action contract"
      contains: "test_parse_action_accepts_request_intervention"
  key_links:
    - from: "opengui/prompts/system.py::_default_tool_definition"
      to: "opengui/action.py::parse_action"
      via: "the model-visible tool schema and parser agree on the action vocabulary"
      pattern: "request_intervention"
    - from: "opengui/agent.py::_COMPUTER_USE_TOOL"
      to: "tests/test_opengui_p15_intervention.py"
      via: "the runtime tool schema stays aligned with the new Phase 15 red tests"
      pattern: "request_intervention"
---

<objective>
Establish the Phase 15 contract by teaching the model and parser about an explicit intervention request action, and lock that contract with Wave 0 tests.

Purpose: The rest of the phase depends on a deterministic "pause for human help" branch. This plan creates that branch at the action/schema level before pause/resume lifecycle work begins.
Output: a red/green test file for the intervention contract plus production schemas/parsing that accept `request_intervention` with a required reason.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-intervention-safety-and-handoff/15-RESEARCH.md
@.planning/phases/15-intervention-safety-and-handoff/15-VALIDATION.md
@opengui/action.py
@opengui/prompts/system.py
@opengui/agent.py
@tests/test_opengui.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 tests for the explicit intervention action contract</name>
  <files>tests/test_opengui_p15_intervention.py</files>
  <read_first>
    - .planning/phases/15-intervention-safety-and-handoff/15-RESEARCH.md
    - .planning/phases/15-intervention-safety-and-handoff/15-VALIDATION.md
    - tests/test_opengui_p15_intervention.py
    - opengui/action.py
    - opengui/prompts/system.py
    - opengui/agent.py
    - tests/test_opengui.py
  </read_first>
  <behavior>
    - Test 1: `parse_action()` accepts `{"action_type": "request_intervention", "text": "..."}` and preserves the reason text.
    - Test 2: `parse_action()` rejects `request_intervention` when `text` is missing or blank.
    - Test 3: `build_system_prompt()` exposes `request_intervention` in the tool schema and safety guidance.
    - Test 4: `GuiAgent` runtime tool schema includes `request_intervention` so model/tool parity cannot drift.
  </behavior>
  <action>
Create `tests/test_opengui_p15_intervention.py` with these exact tests:

- `test_parse_action_accepts_request_intervention`
- `test_request_intervention_requires_reason_text`
- `test_system_prompt_lists_request_intervention_action`
- `test_agent_tool_schema_lists_request_intervention`

Use direct assertions against:
- `opengui.action.parse_action(...)`
- `opengui.prompts.system.build_system_prompt(...)`
- `opengui.agent._COMPUTER_USE_TOOL`

The prompt assertions must check both:
- the tool enum contains `request_intervention`
- the system instructions mention that the agent should request intervention for sensitive or unsafe states instead of continuing normally
  </action>
  <acceptance_criteria>
    - `rg -n "def test_parse_action_accepts_request_intervention" tests/test_opengui_p15_intervention.py` returns one match.
    - `rg -n "def test_request_intervention_requires_reason_text" tests/test_opengui_p15_intervention.py` returns one match.
    - `rg -n "def test_system_prompt_lists_request_intervention_action" tests/test_opengui_p15_intervention.py` returns one match.
    - `rg -n "def test_agent_tool_schema_lists_request_intervention" tests/test_opengui_p15_intervention.py` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p15_intervention.py -k "parse_action_accepts_request_intervention or request_intervention_requires_reason_text or system_prompt_lists_request_intervention_action or agent_tool_schema_lists_request_intervention" -q</automated>
  </verify>
  <done>The repo has Wave 0 red tests that pin the intervention action vocabulary before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the explicit intervention action in parser and tool schemas</name>
  <files>opengui/action.py, opengui/prompts/system.py, opengui/agent.py, tests/test_opengui_p15_intervention.py</files>
  <read_first>
    - opengui/action.py
    - opengui/prompts/system.py
    - opengui/agent.py
    - tests/test_opengui_p15_intervention.py
    - .planning/phases/15-intervention-safety-and-handoff/15-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The parser recognizes `request_intervention` as a valid action type and requires a non-empty reason string in `text`.
    - Test 2: The system prompt and runtime tool schema both advertise the same new action type.
    - Test 3: The safety instructions tell the model to use `request_intervention` rather than `done` when the user must take over.
  </behavior>
  <action>
Update the intervention contract with these exact target states:

1. In `opengui/action.py`:
   - add `request_intervention` to `VALID_ACTION_TYPES`
   - treat `request_intervention` like `input_text`/`open_app` for required-text validation, but require the text to be a non-empty human-readable reason
   - add a `describe_action()` branch that returns `request intervention: <reason-preview>`

2. In `opengui/prompts/system.py`:
   - add `request_intervention` to the tool enum
   - add an explicit rule that if the task reaches a sensitive, blocked, or unsafe state, the model must call `computer_use` with `action_type="request_intervention"` instead of continuing or using `done`

3. In `opengui/agent.py`:
   - add `request_intervention` to `_COMPUTER_USE_TOOL`
   - do not implement the full pause/resume handler yet in this plan; only ensure the runtime schema stays aligned with the parser and prompt

Do not change CLI or nanobot behavior in this plan.
  </action>
  <acceptance_criteria>
    - `rg -n "request_intervention" opengui/action.py opengui/prompts/system.py opengui/agent.py` returns at least five total matches.
    - `rg -n "sensitive, blocked, or unsafe|request_intervention" opengui/prompts/system.py` returns at least two matches.
    - `uv run pytest tests/test_opengui_p15_intervention.py -k "parse_action_accepts_request_intervention or request_intervention_requires_reason_text or system_prompt_lists_request_intervention_action or agent_tool_schema_lists_request_intervention" -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p15_intervention.py -k "parse_action_accepts_request_intervention or request_intervention_requires_reason_text or system_prompt_lists_request_intervention_action or agent_tool_schema_lists_request_intervention" -q</automated>
  </verify>
  <done>`request_intervention` is now a real Phase 15 action across parser, prompt, and runtime tool schema.</done>
</task>

</tasks>

<verification>
Keep this plan scoped to the action/schema contract. Do not start pause/resume lifecycle work until the new action is locked in with green Wave 0 tests.
</verification>

<success_criteria>
The model, parser, and runtime all agree on a first-class `request_intervention` action, and the repo has red/green tests that prevent the contract from regressing.
</success_criteria>

<output>
After completion, create `.planning/phases/15-intervention-safety-and-handoff/15-intervention-safety-and-handoff-01-SUMMARY.md`
</output>
