---
phase: quick
plan: 260402-q5f
type: execute
wave: 1
depends_on: []
files_modified:
  - opengui/agent.py
  - nanobot/utils/gui_evaluation.py
  - eval/eval.py
  - tests/test_opengui.py
  - tests/test_opengui_p8_trajectory.py
autonomous: true
must_haves:
  truths:
    - "GUI agent captures the next screenshot only after the action execution path has fully returned and the post-action settle delay has elapsed"
    - "The post-run GUI evaluation step count only includes trajectory events whose type is step"
    - "Batch eval/eval.py and the runtime GUI evaluation helper use the same step-only counting rule"
  artifacts:
    - path: "opengui/agent.py"
      provides: "post-action settle-before-observe behavior"
      contains: "_run_step"
    - path: "nanobot/utils/gui_evaluation.py"
      provides: "step-only GUI evaluation counting"
      contains: "evaluate_gui_trajectory_sync"
    - path: "eval/eval.py"
      provides: "batch evaluation step counting aligned with runtime helper"
      contains: "run_eval"
---

<objective>
修复两个小而关键的 GUI 行为问题：
1. GUI 动作执行后，不要立刻抢截图；在动作返回后做短暂 settle，再抓取下一张截图。
2. GUI evaluation 的步数统计只计算 `type == "step"` 的轨迹事件，避免把 metadata / attempt / result 等生命周期事件误计入步数。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Lock GUI screenshot timing after action settlement</name>
  <files>tests/test_opengui.py opengui/agent.py</files>
  <action>
在 `tests/test_opengui.py` 先补回归测试，验证 GUI agent 在一次非终止动作后按 `execute -> settle delay -> observe` 的顺序推进，再在 `opengui/agent.py` 中实现最小修复。
  </action>
  <verify>
    <automated>uv run pytest tests/test_opengui.py -q -k settle</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Count only step events in GUI evaluation</name>
  <files>tests/test_opengui_p8_trajectory.py nanobot/utils/gui_evaluation.py eval/eval.py</files>
  <action>
先补评测回归测试，锁定 evaluation 结果和 judge 输入都只基于 `type == "step"` 的轨迹事件；然后在运行时 helper 与 batch eval 脚本中统一实现这条规则。
  </action>
  <verify>
    <automated>uv run pytest tests/test_opengui_p8_trajectory.py -q -k step_count</automated>
  </verify>
</task>

</tasks>
