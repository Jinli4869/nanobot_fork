---
phase: 20-web-app-integration-and-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - nanobot/tui/web/package.json
  - nanobot/tui/web/package-lock.json
  - nanobot/tui/web/tsconfig.json
  - nanobot/tui/web/vite.config.ts
  - nanobot/tui/web/index.html
  - nanobot/tui/web/src/main.tsx
  - nanobot/tui/web/src/app/providers.tsx
  - nanobot/tui/web/src/app/router.tsx
  - nanobot/tui/web/src/app/shell.tsx
  - nanobot/tui/web/src/lib/workspace-state.ts
  - nanobot/tui/web/src/lib/api/client.ts
  - nanobot/tui/web/src/lib/chat-events.ts
  - nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx
  - nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx
  - nanobot/tui/web/src/app/shell.test.tsx
  - nanobot/tui/web/src/features/workspace-routes.test.tsx
autonomous: true
requirements: [WEB-01, WEB-02]
must_haves:
  truths:
    - "A local user can open a single browser workspace shell under `nanobot/tui` instead of stitching together separate chat and operations pages."
    - "Switching between chat and operations keeps the active workspace identity in the URL so the current session and selected run detail survive navigation."
    - "The frontend consumes the Phase 18 and Phase 19 HTTP and SSE contracts as-is without reopening backend route design."
  artifacts:
    - path: "nanobot/tui/web/package.json"
      provides: "Frontend toolchain, scripts, and dependency contract for the isolated React/Vite app"
      contains: "\"build\""
    - path: "nanobot/tui/web/src/app/router.tsx"
      provides: "URL-backed app router for chat and operations navigation"
      contains: "createBrowserRouter"
    - path: "nanobot/tui/web/src/features/workspace-routes.test.tsx"
      provides: "Regression coverage for navigation and shared workspace context"
      min_lines: 50
  key_links:
    - from: "nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx"
      to: "nanobot/tui/routes/chat.py"
      via: "fetch and EventSource against existing /chat endpoints"
      pattern: "/chat/sessions"
    - from: "nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx"
      to: "nanobot/tui/routes/runtime.py"
      via: "runtime polling and run-detail inspection"
      pattern: "/runtime"
    - from: "nanobot/tui/web/src/app/router.tsx"
      to: "nanobot/tui/web/src/lib/workspace-state.ts"
      via: "route params and search params as durable workspace state"
      pattern: "sessionId|runId"
---

<objective>
Build the isolated React/Vite workspace shell for Phase 20 so chat and operations live inside one browser app without reopening the backend contracts already shipped in Phases 17-19.

Purpose: Satisfy `WEB-01` and `WEB-02` first by creating the frontend boundary, route model, and shared workspace state that later packaged startup work can serve.
Output: A frontend workspace under `nanobot/tui/web`, route-backed chat and operations views, typed API helpers, and frontend regression tests for navigation/context retention.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md
@.planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md
@.planning/phases/17-web-runtime-boundary/17-web-runtime-boundary-02-SUMMARY.md
@.planning/phases/18-chat-workspace/18-chat-workspace-01-SUMMARY.md
@.planning/phases/18-chat-workspace/18-chat-workspace-02-SUMMARY.md
@.planning/phases/18-chat-workspace/18-chat-workspace-03-SUMMARY.md
@.planning/phases/19-operations-console/19-operations-console-01-SUMMARY.md
@.planning/phases/19-operations-console/19-operations-console-02-SUMMARY.md
@.planning/phases/19-operations-console/19-operations-console-03-SUMMARY.md
@pyproject.toml
@nanobot/tui/app.py
@nanobot/tui/routes/chat.py
@nanobot/tui/routes/runtime.py
@nanobot/tui/routes/tasks.py
@nanobot/tui/routes/traces.py
@tests/test_tui_p18_chat.py
@tests/test_tui_p18_streaming.py
@tests/test_tui_p19_runtime.py
@tests/test_tui_p19_tasks.py
@tests/test_tui_p19_traces.py

<interfaces>
Existing backend endpoints to consume directly:
```text
POST /chat/sessions
GET /chat/sessions/{session_id}
POST /chat/sessions/{session_id}/messages
GET /chat/sessions/{session_id}/events
GET /runtime
GET /runtime/runs/{run_id}
GET /runtime/runs/{run_id}/trace
GET /runtime/runs/{run_id}/logs
POST /tasks/runs
```

From `nanobot/tui/app.py`:
```python
def create_app(*, config: Config | None = None, include_runtime_routes: bool = False) -> FastAPI: ...
```

Frontend constraints from research:
```text
- Keep the web stack primarily under nanobot/tui
- Preserve one SPA shell with route-backed chat and operations views
- Use URL-backed workspace context; do not add auth, hosting, or backend rewrites
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the isolated React/Vite workspace and baseline build</name>
  <files>nanobot/tui/web/package.json, nanobot/tui/web/package-lock.json, nanobot/tui/web/tsconfig.json, nanobot/tui/web/vite.config.ts, nanobot/tui/web/index.html, nanobot/tui/web/src/main.tsx</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, pyproject.toml, nanobot/tui/app.py, nanobot/tui/__main__.py</read_first>
  <action>Create `nanobot/tui/web/` as a TypeScript React workspace using the versions and libraries already selected in `20-RESEARCH.md`: React 19, Vite 8, `@vitejs/plugin-react`, `react-router`, `@tanstack/react-query`, Vitest, `@testing-library/react`, and `jsdom`. Add scripts named exactly `dev`, `build`, and `test`, commit the generated `package-lock.json`, and configure the Vite dev server to bind `127.0.0.1` with a fixed local port and proxy `/health`, `/sessions`, `/chat`, `/runtime`, and `/tasks` to `http://127.0.0.1:18791`. Add `index.html` plus a minimal `src/main.tsx` so `npm --prefix nanobot/tui/web run build` emits a real `dist/index.html` immediately. Keep all new frontend files under `nanobot/tui/web`; do not add Python runtime code in this task.</action>
  <acceptance_criteria>
    - `nanobot/tui/web/package.json` contains `dev`, `build`, and `test` scripts.
    - `nanobot/tui/web/vite.config.ts` contains proxy entries for `/chat`, `/runtime`, and `/tasks` pointing at `http://127.0.0.1:18791`.
    - `npm --prefix nanobot/tui/web run build` succeeds.
    - `nanobot/tui/web/dist/index.html` exists after the build command.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run build</automated>
  </verify>
  <done>The frontend workspace exists, the toolchain is pinned in-repo, and a baseline production build can be created before feature routes are added.</done>
</task>

<task type="auto">
  <name>Task 2: Build the route-backed shell and durable workspace state</name>
  <files>nanobot/tui/web/src/app/providers.tsx, nanobot/tui/web/src/app/router.tsx, nanobot/tui/web/src/app/shell.tsx, nanobot/tui/web/src/lib/workspace-state.ts, nanobot/tui/web/src/app/shell.test.tsx</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, .planning/phases/18-chat-workspace/18-chat-workspace-03-SUMMARY.md, .planning/phases/19-operations-console/19-operations-console-03-SUMMARY.md, nanobot/tui/web/src/main.tsx, nanobot/tui/routes/chat.py, nanobot/tui/routes/runtime.py, nanobot/tui/routes/tasks.py, nanobot/tui/routes/traces.py</read_first>
  <action>Implement one `WorkspaceShell` with explicit navigation tabs labeled `Chat` and `Operations`, and route definitions for `/`, `/chat/:sessionId`, and `/operations`. Add a small shared workspace-state helper that treats the active chat session id as a route param and the selected operations context as search params such as `runId` and `panel`. Keep shared identity in the URL instead of transient React-only state so navigation does not reset the active workspace. Use `@tanstack/react-query` for server-state providers, but keep shell chrome and route state lightweight. Add `nanobot/tui/web/src/app/shell.test.tsx` to assert that a seeded `sessionId` remains present after navigating from chat to operations and back. Keep the shell practical and restrained; do not add speculative design polish, auth screens, or hosting controls.</action>
  <acceptance_criteria>
    - `nanobot/tui/web/src/app/router.tsx` contains `createBrowserRouter`.
    - The router defines `/chat/:sessionId` and `/operations`.
    - `nanobot/tui/web/src/lib/workspace-state.ts` reads and writes `sessionId` and `runId` from URL state.
    - `nanobot/tui/web/src/app/shell.test.tsx` contains an assertion that the same `sessionId` survives route changes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run test -- --run src/app/shell.test.tsx --reporter=dot</automated>
  </verify>
  <done>The SPA has one durable shell and route model, and navigation no longer depends on browser-only ephemeral state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement chat and operations routes against the existing backend contracts</name>
  <files>nanobot/tui/web/src/lib/api/client.ts, nanobot/tui/web/src/lib/chat-events.ts, nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx, nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx, nanobot/tui/web/src/features/workspace-routes.test.tsx</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, nanobot/tui/routes/chat.py, nanobot/tui/routes/runtime.py, nanobot/tui/routes/tasks.py, nanobot/tui/routes/traces.py, tests/test_tui_p18_chat.py, tests/test_tui_p18_streaming.py, tests/test_tui_p19_runtime.py, tests/test_tui_p19_tasks.py, tests/test_tui_p19_traces.py</read_first>
  <behavior>
    - Test 1: The chat route can create or resume a session, post a message, and consume SSE events from `GET /chat/sessions/{session_id}/events`.
    - Test 2: The operations route can fetch `/runtime`, surface active runs and recent failures, and request run detail, trace, and log data by `run_id`.
    - Test 3: Switching between the two route views preserves the active `sessionId` and selected `runId` in the URL-backed workspace state.
  </behavior>
  <action>Create one typed frontend API layer in `nanobot/tui/web/src/lib/api/client.ts` for exactly these backend calls: `POST /chat/sessions`, `GET /chat/sessions/{session_id}`, `POST /chat/sessions/{session_id}/messages`, `GET /runtime`, `GET /runtime/runs/{run_id}`, `GET /runtime/runs/{run_id}/trace`, `GET /runtime/runs/{run_id}/logs`, and `POST /tasks/runs`. Add `nanobot/tui/web/src/lib/chat-events.ts` as a thin `EventSource` wrapper for the existing SSE endpoint and keep Last-Event-ID/reconnect behavior aligned with the Phase 18 contract rather than inventing WebSocket handling. Implement `ChatWorkspaceRoute.tsx` and `OperationsWorkspaceRoute.tsx` on top of those helpers so the chat screen can create/resume a session, render transcript and streamed progress, and the operations screen can show runtime summaries, launches, and diagnostics. Add `nanobot/tui/web/src/features/workspace-routes.test.tsx` to lock the navigation and shared-context behavior. Do not add new backend paths, `/api` aliases, auth flows, or generic command execution.</action>
  <acceptance_criteria>
    - `nanobot/tui/web/src/lib/api/client.ts` contains the literal backend paths `/chat/sessions`, `/runtime`, `/tasks/runs`, and `/runtime/runs/`.
    - `nanobot/tui/web/src/lib/chat-events.ts` constructs `EventSource` against `/chat/sessions/${sessionId}/events`.
    - `nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx` references session creation, message submission, and streamed updates.
    - `nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx` references runtime summary, run detail, trace, and log loading.
    - `npm --prefix nanobot/tui/web run test -- --run --reporter=dot` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run test -- --run --reporter=dot</automated>
  </verify>
  <done>The frontend is a working SPA shell over the existing chat and operations contracts, and `WEB-01`/`WEB-02` are implemented without backend scope creep.</done>
</task>

</tasks>

<verification>
Use the `20-VALIDATION.md` `20-01` slice after each task. If the environment blocks `npm` tests until install is complete, complete Task 1 first so `package-lock.json` and the buildable workspace exist before rerunning the documented test command.
</verification>

<success_criteria>
The isolated frontend exists under `nanobot/tui/web`, one SPA shell can navigate between chat and operations without losing workspace identity, and the Phase 18-19 backend contracts are consumed directly rather than redefined.
</success_criteria>

<output>
After completion, create `.planning/phases/20-web-app-integration-and-verification/20-web-app-integration-and-verification-01-SUMMARY.md`
</output>
