---
phase: 24-schema-and-grounding
plan: "03"
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - opengui/grounding/__init__.py
  - opengui/grounding/protocol.py
  - opengui/grounding/llm.py
  - tests/test_opengui_p24_schema_grounding.py
autonomous: true
requirements: [GRND-01, GRND-02, GRND-03]
must_haves:
  truths:
    - "GrounderProtocol is a runtime-checkable async protocol that resolves semantic targets into a structured GroundingResult instead of executing actions directly."
    - "LLMGrounder can be instantiated with the existing `LLMProvider` contract and returns `GroundingResult` carrying `grounder_id`, `confidence`, `resolved_params`, and optional `fallback_metadata`."
    - "The new grounding package imports cleanly without dragging in executor or host-specific modules, and the new modules pass a compile/import sanity gate."
  artifacts:
    - path: "opengui/grounding/protocol.py"
      provides: "GrounderProtocol, GroundingContext, and GroundingResult contracts"
      contains: "class GrounderProtocol"
    - path: "opengui/grounding/llm.py"
      provides: "LLMGrounder implementation backed by `LLMProvider`"
      contains: "class LLMGrounder"
    - path: "tests/test_opengui_p24_schema_grounding.py"
      provides: "Grounding protocol/result and import-safety regression coverage"
      contains: "test_grounder"
  key_links:
    - from: "opengui/grounding/protocol.py"
      to: "opengui/interfaces.py"
      via: "mirror the existing `@runtime_checkable Protocol` style"
      pattern: "runtime_checkable"
    - from: "opengui/grounding/llm.py"
      to: "opengui/agent.py"
      via: "reuse the current vision-LLM grounding style without wiring executors in this phase"
      pattern: "_COMPUTER_USE_TOOL"
---

<objective>
Add the pluggable grounding package and result contracts that later executors can call without coupling to the current agent-side grounding implementation.

Purpose: satisfy `GRND-01` through `GRND-03` and the Phase 24 import/type-safety success criterion.
Output: `opengui/grounding` package with protocol/result/context types, `LLMGrounder`, and tests covering protocol conformance, result shape, and compile/import sanity.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-schema-and-grounding/24-CONTEXT.md
@.planning/phases/24-schema-and-grounding/24-RESEARCH.md
@.planning/phases/24-schema-and-grounding/24-VALIDATION.md
@opengui/interfaces.py
@opengui/observation.py
@opengui/agent.py
@opengui/skills/shortcut.py
@tests/test_opengui_p24_schema_grounding.py

<interfaces>
Locked grounding shape:
```python
@runtime_checkable
class GrounderProtocol(Protocol):
    async def ground(self, target: str, context: GroundingContext) -> GroundingResult: ...

@dataclass(frozen=True)
class GroundingContext:
    screenshot_path: Path
    observation: Observation
    parameter_slots: tuple[ParameterSlot, ...] = ()
    task_hint: str | None = None

@dataclass(frozen=True)
class GroundingResult:
    grounder_id: str
    confidence: float
    resolved_params: dict[str, Any]
    fallback_metadata: dict[str, Any] | None = None
```

LLM implementation target:
```text
- `LLMGrounder.__init__(llm: LLMProvider, model: str = "...")`
- `LLMGrounder.ground()` returns `GroundingResult`, not `Action`
- keep the class import-safe; do not instantiate GuiAgent or SkillExecutor here
```

Phase 24 constraints:
```text
- use stdlib dataclasses and protocol types
- keep `GroundingContext` dependent only on `Observation`, `ParameterSlot`, and pathlib/std types
- no executor wiring changes in this phase
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for grounding protocol, result DTOs, and import safety</name>
  <files>tests/test_opengui_p24_schema_grounding.py</files>
  <read_first>.planning/phases/24-schema-and-grounding/24-CONTEXT.md, .planning/phases/24-schema-and-grounding/24-RESEARCH.md, .planning/phases/24-schema-and-grounding/24-VALIDATION.md, opengui/interfaces.py, opengui/observation.py, opengui/agent.py, opengui/skills/shortcut.py, tests/test_opengui_p24_schema_grounding.py</read_first>
  <behavior>
    - Test 1: `GroundingResult` round-trips through `to_dict()` / `from_dict()` while preserving `grounder_id`, `confidence`, `resolved_params`, and `fallback_metadata`.
    - Test 2: `GrounderProtocol` accepts a fake async implementation under `isinstance(fake, GrounderProtocol)`.
    - Test 3: `LLMGrounder.ground()` called with a fake `LLMProvider` returns a `GroundingResult` whose `grounder_id` starts with `llm:` and whose `resolved_params` is a dict.
    - Test 4: importing `opengui.grounding`, `opengui.grounding.protocol`, and `opengui.grounding.llm` succeeds without importing `SkillExecutor` or raising circular import errors.
  </behavior>
  <action>Extend `tests/test_opengui_p24_schema_grounding.py` with failing tests named `test_grounding_result_round_trip`, `test_fake_grounder_conforms_to_protocol`, `test_llm_grounder_returns_grounding_result`, and `test_grounding_modules_import_cleanly`. Use a small fake `LLMProvider` that returns an `LLMResponse` containing JSON or a tool-call payload sufficient for `LLMGrounder` to produce `resolved_params={"text": "wifi"}` or similar deterministic data. In the import-safety test, import the three grounding modules directly with `importlib.import_module` and assert the imported modules expose `GrounderProtocol` and `LLMGrounder`. Keep the fake provider local to the test file.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p24_schema_grounding.py` contains the exact test names `test_grounding_result_round_trip`, `test_fake_grounder_conforms_to_protocol`, and `test_llm_grounder_returns_grounding_result`
    - `tests/test_opengui_p24_schema_grounding.py` imports `LLMResponse` from `opengui.interfaces`
    - `tests/test_opengui_p24_schema_grounding.py` contains `importlib.import_module("opengui.grounding")`
    - `uv run pytest -q tests/test_opengui_p24_schema_grounding.py -k "grounding_result or fake_grounder or llm_grounder or import_cleanly"` exits non-zero before implementation and exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_opengui_p24_schema_grounding.py -k "grounding_result or fake_grounder or llm_grounder or import_cleanly"</automated>
  </verify>
  <done>The tests define the exact grounding protocol/result surface and enforce import safety before production code is added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the grounding package and compile/import sanity gate</name>
  <files>opengui/grounding/__init__.py, opengui/grounding/protocol.py, opengui/grounding/llm.py, tests/test_opengui_p24_schema_grounding.py</files>
  <read_first>.planning/phases/24-schema-and-grounding/24-CONTEXT.md, .planning/phases/24-schema-and-grounding/24-RESEARCH.md, opengui/interfaces.py, opengui/observation.py, opengui/agent.py, opengui/skills/shortcut.py, tests/test_opengui_p24_schema_grounding.py</read_first>
  <behavior>
    - `GroundingContext` uses the exact fields `screenshot_path`, `observation`, `parameter_slots`, and `task_hint`.
    - `GroundingResult.to_dict()` and `from_dict()` preserve `fallback_metadata=None` and arbitrary `resolved_params` dict content.
    - `LLMGrounder.ground()` accepts `target: str` and `context: GroundingContext`, calls the provided `LLMProvider`, and returns a `GroundingResult` rather than an `Action`.
    - `opengui/grounding/__init__.py` exports `GrounderProtocol`, `GroundingContext`, `GroundingResult`, and `LLMGrounder`.
  </behavior>
  <action>Create `opengui/grounding/protocol.py` with stdlib imports, `Path` from `pathlib`, `Any` from `typing`, and imports of `Observation`, `LLMProvider`, and `ParameterSlot`. Define frozen dataclasses `GroundingContext` and `GroundingResult` with explicit `to_dict()` / `from_dict()` methods. Define `GrounderProtocol` as `@runtime_checkable` and `Protocol` with the exact async method signature `ground(self, target: str, context: GroundingContext) -> GroundingResult`. Create `opengui/grounding/llm.py` with a concrete `LLMGrounder` that takes `llm: LLMProvider` and optional `grounder_id: str = "llm:default"`. Implement `ground()` so it builds an LLM prompt from `target`, `context.task_hint`, and `context.parameter_slots`, parses either JSON content or the first tool-call argument dict from the response, and returns `GroundingResult(grounder_id=..., confidence=1.0 or parsed value, resolved_params=parsed_params, fallback_metadata=None or parsed metadata)`. Create `opengui/grounding/__init__.py` exporting the four public symbols. After implementation, run the exact compile gate `uv run python -m py_compile opengui/skills/data.py opengui/skills/shortcut.py opengui/skills/task_skill.py opengui/grounding/__init__.py opengui/grounding/protocol.py opengui/grounding/llm.py` and keep the plan’s test file updated so that command is also exercised during execution.</action>
  <acceptance_criteria>
    - `opengui/grounding/protocol.py` contains the exact class names `GroundingContext`, `GroundingResult`, and `GrounderProtocol`
    - `opengui/grounding/protocol.py` contains `async def ground(self, target: str, context: GroundingContext) -> GroundingResult`
    - `opengui/grounding/llm.py` contains `class LLMGrounder`
    - `opengui/grounding/__init__.py` contains the strings `"GrounderProtocol"`, `"GroundingContext"`, `"GroundingResult"`, and `"LLMGrounder"` in `__all__`
    - `uv run pytest -q tests/test_opengui_p24_schema_grounding.py -k "grounding_result or fake_grounder or llm_grounder or import_cleanly"` exits 0
    - `uv run python -m py_compile opengui/skills/data.py opengui/skills/shortcut.py opengui/skills/task_skill.py opengui/grounding/__init__.py opengui/grounding/protocol.py opengui/grounding/llm.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_opengui_p24_schema_grounding.py -k "grounding_result or fake_grounder or llm_grounder or import_cleanly"</automated>
    <automated>uv run python -m py_compile opengui/skills/data.py opengui/skills/shortcut.py opengui/skills/task_skill.py opengui/grounding/__init__.py opengui/grounding/protocol.py opengui/grounding/llm.py</automated>
  </verify>
  <done>The grounding package exists as a clean reusable contract layer, `LLMGrounder` returns structured result DTOs, and the new modules pass both unit and compile/import sanity checks.</done>
</task>

</tasks>

<verification>
Run the Phase 24 quick slice from `24-VALIDATION.md`: `uv run pytest -q tests/test_opengui_p1_skills.py tests/test_opengui_p1_memory.py tests/test_opengui_p24_schema_grounding.py`. Then run the compile gate from the validation doc to confirm import safety for the new modules.
</verification>

<success_criteria>
`GRND-01` through `GRND-03` are satisfied when the repo exposes a runtime-checkable grounding protocol, a structured context/result DTO pair, and an `LLMGrounder` implementation that returns metadata-rich parameter-resolution results without coupling to executors.
</success_criteria>

<output>
After completion, create `.planning/phases/24-schema-and-grounding/24-03-SUMMARY.md`
</output>
