---
phase: quick
plan: 260324-p41
type: execute
wave: 1
depends_on: []
files_modified:
  - nanobot/agent/tools/gui.py
  - tests/test_opengui_p8_trajectory.py
  - tests/test_opengui_p3_nanobot.py
  - tests/test_opengui_p11_integration.py
autonomous: true
must_haves:
  truths:
    - "GUI agent result returns to the main agent without waiting for trajectory summarization or skill extraction"
    - "Trajectory postprocessing still runs in the background when a trace file exists"
    - "Background postprocessing failures are contained to logs and do not leak unhandled task warnings"
  artifacts:
    - path: "nanobot/agent/tools/gui.py"
      provides: "Background postprocessing scheduler for GUI trajectory follow-up"
      contains: "_schedule_trajectory_postprocessing"
    - path: "tests/test_opengui_p11_integration.py"
      provides: "Regression coverage that execute returns before background postprocessing completes"
      contains: "test_gui_tool_returns_before_background_postprocessing_finishes"
---

<objective>
Decouple GUI trajectory summarization and skill extraction from the synchronous GUI tool response path.

Goal: Once the GUI agent finishes and a trace exists, return the result payload to the main agent immediately. Run trajectory summarization and skill extraction afterward in a background asyncio task so the main agent is not blocked by LLM-heavy postprocessing.
</objective>

<tasks>

<task type="auto">
  <name>Move GUI trajectory postprocessing off the synchronous return path</name>
  <files>nanobot/agent/tools/gui.py</files>
  <action>
Introduce a background scheduling helper that creates an asyncio task for trajectory postprocessing when a trace file exists.
Keep the existing postprocessing sequence intact inside the background worker:
1. summarize trajectory
2. log summary when present
3. extract and merge skill

Track pending tasks on the tool instance so tests and future callers can await them explicitly if needed. Add a done callback that both removes completed tasks from the tracking set and consumes/logs exceptions so there are no "Task exception was never retrieved" warnings.
  </action>
  <verify>
    <automated>GUI tool tests pass and the new integration test proves the response returns before background postprocessing completes.</automated>
  </verify>
</task>

<task type="auto">
  <name>Update tests for explicit background postprocessing waits</name>
  <files>tests/test_opengui_p8_trajectory.py, tests/test_opengui_p3_nanobot.py, tests/test_opengui_p11_integration.py</files>
  <action>
Adjust tests that previously assumed postprocessing completed before execute() returned. After awaiting tool.execute(...), explicitly await the helper that drains pending background postprocessing tasks before asserting summarizer or skill extraction side effects.

Add one regression test that blocks the postprocessing coroutine on an asyncio.Event and verifies execute() still returns a successful payload immediately while the background task remains pending.
  </action>
  <verify>
    <automated>`uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p3_nanobot.py tests/test_opengui_p11_integration.py`</automated>
  </verify>
</task>

</tasks>
