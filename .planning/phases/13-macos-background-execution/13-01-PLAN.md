---
phase: 13-macos-background-execution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_opengui_p13_macos_display.py
  - opengui/backends/background_runtime.py
  - opengui/backends/displays/cgvirtualdisplay.py
  - opengui/backends/displays/__init__.py
  - pyproject.toml
autonomous: true
requirements:
  - MAC-01
  - MAC-02

must_haves:
  truths:
    - "macOS isolated background support is decided before any background backend starts."
    - "Supported macOS runs resolve through the shared runtime contract with backend_name set to cgvirtualdisplay."
    - "Unsupported macOS versions or missing permissions map to stable reason codes with actionable remediation."
    - "The macOS virtual-display manager returns DisplayInfo with width, height, offset_x, offset_y, and monitor_index metadata."
    - "Linux Xvfb probing behavior remains unchanged."
  artifacts:
    - path: "opengui/backends/background_runtime.py"
      provides: "Shared macOS capability probing and remediation mapping"
      contains: "macos_virtual_display_available"
    - path: "opengui/backends/displays/cgvirtualdisplay.py"
      provides: "macOS CGVirtualDisplay lifecycle manager"
      contains: "class CGVirtualDisplayManager"
    - path: "tests/test_opengui_p13_macos_display.py"
      provides: "Unit coverage for Phase 13 probe and manager contracts"
      contains: "test_probe_macos_virtual_display_available"
  key_links:
    - from: "opengui/backends/background_runtime.py::probe_isolated_background_support"
      to: "opengui/backends/displays/cgvirtualdisplay.py::CGVirtualDisplayManager"
      via: "shared runtime resolves backend_name=cgvirtualdisplay before isolated startup"
      pattern: "cgvirtualdisplay"
    - from: "opengui/backends/displays/cgvirtualdisplay.py::CGVirtualDisplayManager.start"
      to: "opengui/backends/virtual_display.py::DisplayInfo"
      via: "manager returns target-surface metadata consumed by the wrapper and desktop backend"
      pattern: "DisplayInfo"
---

<objective>
Establish the macOS isolated-capability contract and the concrete `CGVirtualDisplayManager` lifecycle surface.

Purpose: Phase 13 needs one real macOS isolated backend plus shared runtime reason codes before any routing or host-entry wiring can be trusted.
Output: a new macOS display manager, shared probe/remediation support in `background_runtime.py`, macOS dependency markers, and green unit coverage for MAC-01/MAC-02 contract behavior.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-macos-background-execution/13-RESEARCH.md
@.planning/phases/13-macos-background-execution/13-VALIDATION.md
@opengui/backends/background_runtime.py
@opengui/backends/virtual_display.py
@opengui/backends/displays/__init__.py
@opengui/backends/displays/xvfb.py
@pyproject.toml

<interfaces>
Existing shared runtime seam:
```python
@dataclasses.dataclass(frozen=True)
class IsolationProbeResult:
    supported: bool
    reason_code: str
    retryable: bool
    host_platform: HostPlatform
    backend_name: str | None
    sys_platform: str

def probe_isolated_background_support(
    *, sys_platform: str | None = None, xvfb_binary: str = "Xvfb"
) -> IsolationProbeResult: ...
```

Existing virtual display contract:
```python
@dataclasses.dataclass(frozen=True)
class DisplayInfo:
    display_id: str
    width: int
    height: int
    offset_x: int = 0
    offset_y: int = 0
    monitor_index: int = 1

class VirtualDisplayManager(typing.Protocol):
    async def start(self) -> DisplayInfo: ...
    async def stop(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 macOS contract tests before production edits</name>
  <files>tests/test_opengui_p13_macos_display.py</files>
  <read_first>
    - tests/test_opengui_p12_runtime_contracts.py
    - tests/test_opengui_p10_background.py
    - .planning/phases/13-macos-background-execution/13-RESEARCH.md
    - .planning/phases/13-macos-background-execution/13-VALIDATION.md
  </read_first>
  <action>
Create `tests/test_opengui_p13_macos_display.py` first, before production code changes. Add these exact test names as Wave 0 placeholders using the same import-guard pattern used in earlier phases:
- `test_probe_macos_virtual_display_available`
- `test_probe_reports_macos_version_unsupported`
- `test_probe_reports_actionable_permission_remediation`
- `test_cgvirtualdisplay_manager_returns_display_info`
- `test_cgvirtualdisplay_manager_stop_is_idempotent`

Wrap imports from `opengui.backends.background_runtime` and `opengui.backends.displays.cgvirtualdisplay` in `try/except ImportError`; set `_IMPORTS_OK = False` on failure; mark each placeholder with `@pytest.mark.xfail(strict=True, reason="Phase 13 macOS background contracts not implemented yet")`.

Each placeholder must target one concrete contract value instead of `pass`:
- supported probe must mention `backend_name="cgvirtualdisplay"`
- unsupported-version probe must mention `macos_version_unsupported`
- permission remediation probe must mention `macos_screen_recording_denied` and `System Settings`
- manager start must mention `DisplayInfo`
- manager stop must mention idempotent cleanup

Use `pytest.fail("phase 13 placeholder")` inside each xfail body so the scaffold is visibly red until promoted.
  </action>
  <acceptance_criteria>
    - `rg -n "def test_probe_macos_virtual_display_available" tests/test_opengui_p13_macos_display.py` returns one match.
    - `rg -n "macos_version_unsupported" tests/test_opengui_p13_macos_display.py` returns at least one match.
    - `rg -n "pytest.mark.xfail\\(strict=True, reason=\"Phase 13 macOS background contracts not implemented yet\"" tests/test_opengui_p13_macos_display.py` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p13_macos_display.py -q</automated>
  </verify>
  <done>Wave 0 placeholders exist for every Phase 13 probe and manager contract named in `13-VALIDATION.md`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement macOS probe taxonomy, dependency markers, and CGVirtualDisplayManager</name>
  <files>opengui/backends/background_runtime.py, opengui/backends/displays/cgvirtualdisplay.py, opengui/backends/displays/__init__.py, pyproject.toml, tests/test_opengui_p13_macos_display.py</files>
  <read_first>
    - opengui/backends/background_runtime.py
    - opengui/backends/displays/xvfb.py
    - opengui/backends/virtual_display.py
    - pyproject.toml
    - tests/test_opengui_p13_macos_display.py
    - .planning/phases/13-macos-background-execution/13-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `probe_isolated_background_support(sys_platform="darwin")` returns `supported=True`, `backend_name="cgvirtualdisplay"`, and `reason_code="macos_virtual_display_available"` when macOS support helpers report an eligible host with granted permissions.
    - Test 2: unsupported macOS support-floor probes return `supported=False`, `reason_code="macos_version_unsupported"`, and a remediation-bearing resolved mode.
    - Test 3: missing Screen Recording / Accessibility / event-post capability returns a stable macOS reason code with actionable remediation mentioning System Settings.
    - Test 4: `CGVirtualDisplayManager.start()` returns `DisplayInfo` with non-default `monitor_index` and offset metadata sourced from a mocked Quartz boundary.
    - Test 5: `CGVirtualDisplayManager.stop()` is safe to call more than once.
  </behavior>
  <action>
Promote the Wave 0 tests to green and implement the production path with these exact concrete choices:

1. In `opengui/backends/background_runtime.py`, extend `probe_isolated_background_support()` for normalized `host_platform == "macos"` by delegating to a macOS helper and preserving the existing Linux branch exactly as-is.
2. Add macOS reason codes and remediation strings for:
   - `macos_virtual_display_available`
   - `macos_version_unsupported`
   - `macos_pyobjc_missing`
   - `macos_virtual_display_api_missing`
   - `macos_screen_recording_denied`
   - `macos_accessibility_denied`
   - `macos_event_post_denied`
3. For the supported macOS case, return:
   - `supported=True`
   - `retryable=False`
   - `host_platform="macos"`
   - `backend_name="cgvirtualdisplay"`
   - `sys_platform="darwin"`
4. Create `opengui/backends/displays/cgvirtualdisplay.py` exporting `CGVirtualDisplayManager` and any private helpers it needs. Keep all PyObjC / Quartz imports inside macOS-gated code paths so Linux test collection remains safe.
5. `CGVirtualDisplayManager.start()` must return a real `DisplayInfo` object with these fields populated from the mocked/display-discovery boundary:
   - `display_id` as a stable macOS-prefixed identifier such as `macos:<id>`
   - `width`
   - `height`
   - `offset_x`
   - `offset_y`
   - `monitor_index`
6. `CGVirtualDisplayManager.stop()` must be idempotent and must clear the stored display handle/reference even when the backing stop call raises.
7. Update `opengui/backends/displays/__init__.py` so `CGVirtualDisplayManager` is importable alongside the existing display implementations.
8. Update `pyproject.toml` to add PyObjC packages behind macOS environment markers inside the `desktop` and `dev` extras so the new path is packaged without breaking non-macOS installs.
9. Replace the xfail placeholders in `tests/test_opengui_p13_macos_display.py` with real green tests that patch the macOS helper boundary instead of requiring a real virtual display.
  </action>
  <acceptance_criteria>
    - `rg -n "macos_virtual_display_available" opengui/backends/background_runtime.py` returns at least one match.
    - `rg -n "backend_name=\"cgvirtualdisplay\"" opengui/backends/background_runtime.py tests/test_opengui_p13_macos_display.py` returns at least one match.
    - `rg -n "class CGVirtualDisplayManager" opengui/backends/displays/cgvirtualdisplay.py` returns one match.
    - `rg -n "pyobjc-core" pyproject.toml` returns at least one match with a macOS marker.
    - `rg -n "def test_cgvirtualdisplay_manager_returns_display_info" tests/test_opengui_p13_macos_display.py` returns one match without an xfail marker on that test.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p13_macos_display.py -q</automated>
  </verify>
  <done>The codebase can now distinguish supported macOS isolated execution from concrete permission/capability failures and has a first-class macOS display manager behind the shared contracts.</done>
</task>

</tasks>

<verification>
Run the new Phase 13 unit file only. No test should require an actual macOS virtual display or PyObjC installation on Linux CI.
</verification>

<success_criteria>
The shared runtime contract can represent macOS isolated support and remediation cleanly, and the codebase contains a concrete `CGVirtualDisplayManager` implementation surface ready for routing integration.
</success_criteria>

<output>
After completion, create `.planning/phases/13-macos-background-execution/13-macos-background-execution-01-SUMMARY.md`
</output>
