---
phase: 16-host-integration-and-verification
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - nanobot/agent/tools/gui.py
  - tests/test_opengui_p11_integration.py
autonomous: true
requirements:
  - INTG-06
  - TEST-V12-01

must_haves:
  truths:
    - "Nanobot preserves the same shared runtime decisions and reason codes as CLI while keeping the existing JSON host contract stable."
    - "Nanobot payloads retain cleanup and handoff evidence needed to diagnose lifecycle behavior."
    - "Nanobot intervention results stay scrubbed and structured instead of leaking raw sensitive reason text."
  artifacts:
    - path: "nanobot/agent/tools/gui.py"
      provides: "Structured nanobot host-entry behavior for Phase 16 parity"
      contains: "acknowledge_background_fallback"
    - path: "tests/test_opengui_p11_integration.py"
      provides: "Nanobot regression coverage for reason-token and cleanup-token parity"
      contains: "test_gui_tool_background_decision_tokens_stay_consistent_across_supported_hosts"
  key_links:
    - from: "nanobot/agent/tools/gui.py::execute"
      to: "opengui/backends/background_runtime.py::resolve_run_mode"
      via: "Nanobot consumes the same shared runtime decision object as CLI before agent execution"
      pattern: "reason_code"
    - from: "nanobot/agent/tools/gui.py::_GuiToolInterventionHandler"
      to: "opengui/agent.py::GuiAgent._scrub_for_log"
      via: "Structured nanobot intervention output stays scrubbed while preserving safe target metadata"
      pattern: "resume_not_confirmed"
---

<objective>
Tighten the nanobot GUI host path so it exposes the same background capability and lifecycle semantics as the CLI path while preserving nanobot's structured JSON contract.

Purpose: Phase 16 requires host parity, but the nanobot surface is intentionally non-interactive and JSON-shaped. This plan locks that parity without flattening nanobot into CLI behavior.
Output: nanobot regression tests plus any targeted `nanobot/agent/tools/gui.py` updates needed to preserve shared reason codes, remediation copy, cleanup tokens, and scrubbed intervention outcomes.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-host-integration-and-verification/16-RESEARCH.md
@.planning/phases/16-host-integration-and-verification/16-VALIDATION.md
@opengui/backends/background_runtime.py
@nanobot/agent/tools/gui.py
@tests/test_opengui_p11_integration.py
@tests/test_opengui_p14_windows_desktop.py
@tests/test_opengui_p15_intervention.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nanobot parity tests for shared decision tokens and structured lifecycle evidence</name>
  <files>tests/test_opengui_p11_integration.py</files>
  <read_first>
    - tests/test_opengui_p11_integration.py
    - nanobot/agent/tools/gui.py
    - opengui/backends/background_runtime.py
    - tests/test_opengui_p14_windows_desktop.py
    - tests/test_opengui_p15_intervention.py
    - .planning/phases/16-host-integration-and-verification/16-RESEARCH.md
    - .planning/phases/16-host-integration-and-verification/16-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: Nanobot reports the same shared decision tokens and reason codes as CLI for supported, fallback, and blocked background paths.
    - Test 2: Structured JSON failure or success payloads preserve cleanup and target-surface tokens such as `cleanup_reason=...`, `display_id`, or `desktop_name`.
    - Test 3: Intervention-related structured output stays scrubbed and does not surface the raw reason text.
  </behavior>
  <action>
Extend `tests/test_opengui_p11_integration.py` with these exact tests:

- `test_gui_tool_background_decision_tokens_stay_consistent_across_supported_hosts`
- `test_gui_tool_preserves_cleanup_and_intervention_tokens_in_structured_payloads`

The first test must cover at least:
- a supported or isolated path whose summary/log behavior still reflects the shared runtime decision
- a fallback path that keeps the existing `acknowledge_background_fallback=true` guidance
- a blocked Windows unsupported-app-class path whose payload still includes `windows_app_class_unsupported`

The second test must assert:
- Windows lifecycle evidence such as `cleanup_reason=startup_failed` is preserved in the structured payload
- safe handoff metadata such as `display_id` or `desktop_name` remains available when appropriate
- raw intervention reason text does not appear in the structured output, while the scrubbed placeholders remain acceptable

Do not add a new nanobot-specific utility module in this task.
  </action>
  <acceptance_criteria>
    - `rg -n "def test_gui_tool_background_decision_tokens_stay_consistent_across_supported_hosts" tests/test_opengui_p11_integration.py` returns one match.
    - `rg -n "def test_gui_tool_preserves_cleanup_and_intervention_tokens_in_structured_payloads" tests/test_opengui_p11_integration.py` returns one match.
    - `rg -n "windows_app_class_unsupported|acknowledge_background_fallback=true|cleanup_reason=startup_failed|display_id|desktop_name" tests/test_opengui_p11_integration.py` returns at least five total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p11_integration.py::test_gui_tool_background_decision_tokens_stay_consistent_across_supported_hosts tests/test_opengui_p11_integration.py::test_gui_tool_preserves_cleanup_and_intervention_tokens_in_structured_payloads -q</automated>
  </verify>
  <done>The nanobot suite now pins the Phase 16 structured-contract parity requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align nanobot structured results with the shared runtime contract</name>
  <files>nanobot/agent/tools/gui.py, tests/test_opengui_p11_integration.py</files>
  <read_first>
    - nanobot/agent/tools/gui.py
    - opengui/backends/background_runtime.py
    - tests/test_opengui_p11_integration.py
    - .planning/phases/16-host-integration-and-verification/16-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `GuiSubagentTool.execute()` consumes the same probe, reason, and remediation semantics as CLI before agent execution.
    - Test 2: Structured JSON payloads preserve lifecycle evidence and fallback guidance instead of collapsing them into generic messages.
    - Test 3: Intervention-related output stays scrubbed and stable in the existing JSON response shape.
  </behavior>
  <action>
Update `nanobot/agent/tools/gui.py` to preserve these exact target states:

1. Keep `_resolve_probe_target_app_class(...)` returning:
   - `None` for non-background, non-local, or non-Windows paths
   - `target_app_class or "classic-win32"` for Windows background-local runs

2. In `GuiSubagentTool.execute()`:
   - keep the call ordering `probe -> resolve_run_mode -> log_mode_resolution -> branch`
   - preserve the shared runtime `decision.message` and `reason_code` semantics in structured payloads instead of replacing them with nanobot-local wording
   - preserve lifecycle evidence such as `cleanup_reason=...`, `display_id`, `desktop_name`, and fallback acknowledgement guidance when they are part of the returned summary/error

3. In `_GuiToolInterventionHandler` and `_run_task(...)`:
   - keep the existing JSON response shape
   - preserve the normalized `intervention_cancelled` outcome instead of raw free-text errors
   - ensure no raw intervention reason text is logged or returned

Do not add interactive resume behavior to nanobot in this phase. Preserve the current non-interactive host semantics while keeping the underlying contract aligned with CLI.
  </action>
  <acceptance_criteria>
    - `rg -n "_resolve_probe_target_app_class|acknowledge_background_fallback|intervention_cancelled|cleanup_reason" nanobot/agent/tools/gui.py` returns at least four total matches.
    - `rg -n "classic-win32|windows_app_class_unsupported|cleanup_reason=startup_failed|resume_not_confirmed" nanobot/agent/tools/gui.py tests/test_opengui_p11_integration.py` returns at least six total matches.
    - `uv run pytest tests/test_opengui_p11_integration.py::test_gui_tool_background_decision_tokens_stay_consistent_across_supported_hosts tests/test_opengui_p11_integration.py::test_gui_tool_preserves_cleanup_and_intervention_tokens_in_structured_payloads tests/test_opengui_p11_integration.py::test_gui_tool_blocks_windows_unsupported_app_class_before_agent_start -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p11_integration.py::test_gui_tool_background_decision_tokens_stay_consistent_across_supported_hosts tests/test_opengui_p11_integration.py::test_gui_tool_preserves_cleanup_and_intervention_tokens_in_structured_payloads tests/test_opengui_p11_integration.py::test_gui_tool_blocks_windows_unsupported_app_class_before_agent_start -q</automated>
  </verify>
  <done>Nanobot now preserves Phase 16 host parity at the structured payload boundary without breaking its existing host contract.</done>
</task>

</tasks>

<verification>
Keep nanobot on the shared runtime contract. Do not "fix parity" by removing its JSON result shape or by forcing interactive CLI behavior into the nanobot host path.
</verification>

<success_criteria>
Nanobot exposes the same capability decisions and lifecycle evidence as CLI, but through its stable structured JSON interface.
</success_criteria>

<output>
After completion, create `.planning/phases/16-host-integration-and-verification/16-host-integration-and-verification-02-SUMMARY.md`
</output>

