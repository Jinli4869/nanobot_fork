---
phase: 28-shortcut-extraction-productionization
plan: "03"
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - tests/test_opengui_p28_shortcut_productionization.py
  - tests/test_opengui_p8_trajectory.py
  - tests/test_opengui_p11_integration.py
  - tests/test_opengui_p27_storage_search_agent.py
autonomous: true
requirements: [SXTR-01, SXTR-02, SXTR-03, SXTR-04]
must_haves:
  truths:
    - "Regression coverage proves summary/result noise, malformed traces, retry noise, and low-value candidates do not become promoted shortcuts."
    - "Adjacent seam tests prove GUI task completion remains decoupled from background promotion and that postprocessing failures stay non-fatal."
    - "The full extraction/store/postprocessing test slice stays green after the new promotion lifecycle is in place."
  artifacts:
    - path: "tests/test_opengui_p28_shortcut_productionization.py"
      provides: "Phase-local regression matrix for promotion lifecycle and rejection behavior"
      contains: "test_promotion_pipeline_rejects_summary_result_noise"
    - path: "tests/test_opengui_p8_trajectory.py"
      provides: "Background postprocessing behavior lock after production cutover"
      contains: "test_gui_tool_returns_before_background_postprocessing_finishes"
    - path: "tests/test_opengui_p11_integration.py"
      provides: "GUI tool integration confidence for trace-path and postprocessing wiring"
      contains: "test_gui_tool_returns_before_background_postprocessing_finishes"
  key_links:
    - from: "tests/test_opengui_p28_shortcut_productionization.py"
      to: "opengui/skills/shortcut_promotion.py"
      via: "phase-local regression locks the production promotion seam"
      pattern: "ShortcutPromotionPipeline"
    - from: "tests/test_opengui_p27_storage_search_agent.py"
      to: "opengui/skills/shortcut_store.py"
      via: "round-trip/search compatibility remains green with lineage metadata"
      pattern: "ShortcutSkillStore"
    - from: "tests/test_opengui_p8_trajectory.py"
      to: "nanobot/agent/tools/gui.py"
      via: "postprocessing remains asynchronous and non-fatal"
      pattern: "_run_trajectory_postprocessing|_promote_shortcut"
---

<objective>
Lock the productionized extraction path with focused regression coverage.

Purpose: prove the new promotion lifecycle is trustworthy by covering malformed traces, summary/result noise, retry noise, low-value evidence, dedup/version behavior, and the non-blocking GUI postprocessing seam.
Output: a complete Phase 28 regression matrix plus adjacent seam coverage that keeps the new extraction path stable across future refactors.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-shortcut-extraction-productionization/28-RESEARCH.md
@.planning/phases/28-shortcut-extraction-productionization/28-VALIDATION.md
@opengui/skills/shortcut_promotion.py
@opengui/skills/shortcut_store.py
@tests/test_opengui_p8_trajectory.py
@tests/test_opengui_p11_integration.py
@tests/test_opengui_p27_storage_search_agent.py
@tests/test_opengui_p28_shortcut_productionization.py

<interfaces>
Required regression behaviors to prove:
```text
- `metadata`, `phase_change`, `result`, and malformed rows never become promoted shortcut steps
- earlier failed-attempt steps are excluded when a later attempt succeeds
- unsupported or low-evidence candidates do not write to `shortcut_skills.json`
- duplicate promotions merge/version instead of creating repeated entries
- GUI tool returns before background promotion finishes and promotion failure is non-fatal
```

Full phase validation commands from `28-VALIDATION.md`:
```text
Quick: uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py
Full:  uv run pytest tests/test_opengui_p26_quality_gated_extraction.py tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p8_trajectory.py tests/test_opengui_p11_integration.py tests/test_opengui_p28_shortcut_productionization.py
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the phase-local regression matrix for noise rejection, gating, and duplicate handling</name>
  <files>tests/test_opengui_p28_shortcut_productionization.py, tests/test_opengui_p27_storage_search_agent.py</files>
  <read_first>.planning/phases/28-shortcut-extraction-productionization/28-VALIDATION.md, opengui/skills/shortcut_promotion.py, opengui/skills/shortcut_store.py, tests/test_opengui_p28_shortcut_productionization.py, tests/test_opengui_p27_storage_search_agent.py</read_first>
  <behavior>
    - Add `test_promotion_pipeline_rejects_summary_result_noise` covering traces where only lifecycle or terminal rows carry content; assert no shortcut write occurs.
    - Add `test_promotion_pipeline_ignores_retry_noise_before_final_success` covering failed attempt rows before a later successful attempt; assert only the final attempt is promoted.
    - Add `test_duplicate_promotions_do_not_increase_shortcut_store_count` covering repeated promotion of the same effective shortcut; assert store count remains stable and `shortcut_version` changes instead.
    - Keep Phase 27 search compatibility covered after lineage metadata and merge semantics exist.
  </behavior>
  <action>Extend `tests/test_opengui_p28_shortcut_productionization.py` with the exact test names `test_promotion_pipeline_rejects_summary_result_noise`, `test_promotion_pipeline_ignores_retry_noise_before_final_success`, and `test_duplicate_promotions_do_not_increase_shortcut_store_count`. In the duplicate-count test, assert the exact numeric condition `len(store.list_all(platform=\"android\")) == 1` after two promotions of the same effective shortcut. Extend `tests/test_opengui_p27_storage_search_agent.py` with one compatibility test that performs a search after a merged shortcut reaches `shortcut_version == 2` and asserts the search result still returns the canonical old `skill_id`.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p28_shortcut_productionization.py` contains the exact test names `test_promotion_pipeline_rejects_summary_result_noise` and `test_duplicate_promotions_do_not_increase_shortcut_store_count`
    - `tests/test_opengui_p28_shortcut_productionization.py` contains the exact assertion `len(store.list_all(platform=\"android\")) == 1`
    - `tests/test_opengui_p27_storage_search_agent.py` contains a search assertion that checks the canonical old `skill_id` after a merge/version update
    - `uv run pytest tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py -k "summary_result_noise or retry_noise or duplicate_promotions or canonical"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py -k "summary_result_noise or retry_noise or duplicate_promotions or canonical"</automated>
  </verify>
  <done>The phase-local regression matrix proves the production promotion path rejects noise and keeps shortcut-store growth under control.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend adjacent seam coverage and run the full Phase 28 validation slice</name>
  <files>tests/test_opengui_p8_trajectory.py, tests/test_opengui_p11_integration.py, tests/test_opengui_p28_shortcut_productionization.py</files>
  <read_first>.planning/phases/28-shortcut-extraction-productionization/28-VALIDATION.md, nanobot/agent/tools/gui.py, tests/test_opengui_p8_trajectory.py, tests/test_opengui_p11_integration.py, tests/test_opengui_p28_shortcut_productionization.py</read_first>
  <behavior>
    - Preserve `GuiSubagentTool`'s background return semantics after the promotion cutover.
    - Preserve non-fatal handling when promotion or summarization fails in background postprocessing.
    - Exercise the full validation slice from `28-VALIDATION.md` so Phase 28 closes with green evidence rather than only phase-local tests.
  </behavior>
  <action>Update `tests/test_opengui_p8_trajectory.py` and `tests/test_opengui_p11_integration.py` so they patch the new promotion seam (`_promote_shortcut` or `ShortcutPromotionPipeline`) rather than the removed legacy extractor assumptions, while keeping the exact postprocessing guarantees intact: returned payload still arrives before background work finishes, and promotion failure does not change `success`, `summary`, `steps_taken`, or `trace_path`. After the test updates, run the exact commands from `28-VALIDATION.md`: `uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py` and `uv run pytest tests/test_opengui_p26_quality_gated_extraction.py tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p8_trajectory.py tests/test_opengui_p11_integration.py tests/test_opengui_p28_shortcut_productionization.py`.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p8_trajectory.py` patches `_promote_shortcut` or `ShortcutPromotionPipeline` instead of `SkillExtractor.extract_from_file`
    - `tests/test_opengui_p11_integration.py` preserves assertions that GUI tool results remain available while background postprocessing is still pending
    - `uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py` exits 0
    - `uv run pytest tests/test_opengui_p26_quality_gated_extraction.py tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p8_trajectory.py tests/test_opengui_p11_integration.py tests/test_opengui_p28_shortcut_productionization.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p8_trajectory.py tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py</automated>
    <automated>uv run pytest tests/test_opengui_p26_quality_gated_extraction.py tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p8_trajectory.py tests/test_opengui_p11_integration.py tests/test_opengui_p28_shortcut_productionization.py</automated>
  </verify>
  <done>The full Phase 28 validation slice is encoded in tests and run commands, giving the new promotion lifecycle durable regression protection.</done>
</task>

</tasks>

<verification>
Run both commands from `28-VALIDATION.md` and confirm they exit 0. The phase is only ready for execution when the phase-local noise/merge tests and the adjacent GUI-postprocessing seam tests are green together.
</verification>

<success_criteria>
Phase 28 is locked when focused automated coverage proves that malformed traces, lifecycle noise, retry noise, low-value evidence, and duplicate promotions cannot silently corrupt the new shortcut store, and the GUI tool still behaves correctly while background promotion runs.
</success_criteria>

<output>
After completion, create `.planning/phases/28-shortcut-extraction-productionization/28-03-SUMMARY.md`
</output>
