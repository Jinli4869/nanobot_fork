---
phase: 14-windows-isolated-desktop-execution
plan: "04"
type: execute
wave: 4
depends_on:
  - 14-01
  - 14-02
  - 14-03
files_modified:
  - .planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md
  - tests/test_opengui_p14_windows_desktop.py
  - tests/test_opengui_p5_cli.py
  - tests/test_opengui_p11_integration.py
  - tests/test_opengui_p12_runtime_contracts.py
autonomous: true
requirements:
  - WIN-01
  - WIN-02
  - WIN-03

must_haves:
  truths:
    - "The full Phase 14 automated regression slice is green without regressing the shared runtime contract."
    - "The phase leaves behind a concrete Windows-host checklist for supported apps, blocked contexts, unsupported app classes, and cleanup paths."
    - "Any regression discovered during closeout is fixed in the same wave instead of being deferred."
  artifacts:
    - path: ".planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md"
      provides: "Real-host Windows verification checklist"
      contains: "## Supported Win32 App"
    - path: "tests/test_opengui_p14_windows_desktop.py"
      provides: "Final Phase 14 unit coverage"
      contains: "test_windows_isolated_backend_cleans_up_on_cancellation"
  key_links:
    - from: ".planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md::Cleanup Paths"
      to: "opengui/backends/windows_isolated.py::shutdown"
      via: "manual validation exercises success, failure, and cancellation cleanup behavior"
      pattern: "Cleanup Paths"
---

<objective>
Close Phase 14 with the full regression slice and an honest Windows-host smoke checklist.

Purpose: CI can prove control flow, but not real alternate-desktop rendering or handle cleanup on a live Windows host. Phase 14 needs both automated closeout and a real-host checklist.
Output: a green Phase 14 regression slice and `14-MANUAL-SMOKE.md` covering supported, blocked, unsupported-app, and cleanup scenarios.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-windows-isolated-desktop-execution/14-RESEARCH.md
@.planning/phases/14-windows-isolated-desktop-execution/14-VALIDATION.md
@tests/test_opengui_p14_windows_desktop.py
@tests/test_opengui_p5_cli.py
@tests/test_opengui_p11_integration.py
@tests/test_opengui_p12_runtime_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and stabilize the full Phase 14 automated regression slice</name>
  <files>tests/test_opengui_p14_windows_desktop.py, tests/test_opengui_p5_cli.py, tests/test_opengui_p11_integration.py, tests/test_opengui_p12_runtime_contracts.py</files>
  <read_first>
    - .planning/phases/14-windows-isolated-desktop-execution/14-VALIDATION.md
    - tests/test_opengui_p14_windows_desktop.py
    - tests/test_opengui_p5_cli.py
    - tests/test_opengui_p11_integration.py
    - tests/test_opengui_p12_runtime_contracts.py
  </read_first>
  <action>
Run the full Phase 14 regression slice exactly as specified in `14-VALIDATION.md`:

`uv run pytest tests/test_opengui_p14_windows_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q`

If anything fails, fix it in this same wave. Keep these constraints explicit:
- preserve Phase 12 shared runtime behavior for Linux and macOS
- do not weaken assertions that protect `windows_non_interactive_session`, `windows_app_class_unsupported`, or `cleanup_reason=...`
- keep the regression slice under the same phase-local files unless a real defect requires a source update

If the full slice is already green, leave the files untouched and record the clean pass in the summary.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_opengui_p14_windows_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q` exits 0.
    - `rg -n "win32desktop|windows_non_interactive_session|cleanup_reason=" tests/test_opengui_p14_windows_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py` returns at least five total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p14_windows_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q</automated>
  </verify>
  <done>The automated Phase 14 closeout slice is green and still protects the shared runtime and Windows cleanup contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Create the real-host Windows smoke checklist</name>
  <files>.planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md</files>
  <read_first>
    - .planning/phases/14-windows-isolated-desktop-execution/14-RESEARCH.md
    - .planning/phases/14-windows-isolated-desktop-execution/14-VALIDATION.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
Create `.planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md` with these exact sections and verification points:

1. `## Supported Win32 App`
   - launch from an interactive signed-in Windows session
   - use a classic Win32/GDI app such as Notepad or File Explorer
   - expected log/result includes `windows_isolated_desktop_available`
   - expected target-surface metadata includes `backend_name=win32desktop` and `display_id=win32desktop:`
   - confirm the foreground user desktop does not switch away

2. `## Non-Interactive Launch Context`
   - attempt the run from a service, Session 0, or other non-interactive context
   - expected result blocks before the first agent step
   - expected message includes `windows_non_interactive_session` and `Session 0 and service contexts`

3. `## Unsupported App Class`
   - attempt an isolated run against a UWP, DirectX, or GPU-heavy surface
   - expected result warns or blocks before automation begins
   - expected message includes `windows_app_class_unsupported` and `classic Win32/GDI`

4. `## Cleanup Paths`
   - verify normal success cleanup
   - force a startup failure after desktop creation
   - cancel a run mid-flight
   - expected evidence includes `cleanup_reason=normal`, `cleanup_reason=startup_failed`, and `cleanup_reason=cancelled`
   - confirm no stale OpenGUI desktop handles or orphaned child processes remain

Keep the checklist phase-local. Do not add user-facing product docs outside `.planning/` in this wave.
  </action>
  <acceptance_criteria>
    - `.planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md` exists.
    - `rg -n "^## Supported Win32 App|^## Non-Interactive Launch Context|^## Unsupported App Class|^## Cleanup Paths" .planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md` returns four matches.
    - `rg -n "windows_isolated_desktop_available|windows_non_interactive_session|windows_app_class_unsupported|cleanup_reason=cancelled" .planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md` returns at least four matches.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^## Supported Win32 App|^## Non-Interactive Launch Context|^## Unsupported App Class|^## Cleanup Paths|windows_isolated_desktop_available|windows_non_interactive_session|windows_app_class_unsupported|cleanup_reason=cancelled" .planning/phases/14-windows-isolated-desktop-execution/14-MANUAL-SMOKE.md</automated>
  </verify>
  <done>The phase now has an explicit Windows-host verification checklist for the behaviors mocks cannot prove honestly.</done>
</task>

</tasks>

<verification>
Run the full automated regression command first. Only after that passes should the Windows smoke checklist be added or updated.
</verification>

<success_criteria>
Phase 14 closes with both automated regression confidence and a concrete Windows-host verification path for supported execution, blocked contexts, unsupported app classes, and cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/14-windows-isolated-desktop-execution/14-windows-isolated-desktop-execution-04-SUMMARY.md`
</output>
