---
phase: 13-macos-background-execution
plan: "02"
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - opengui/backends/background.py
  - opengui/backends/desktop.py
  - tests/test_opengui_p13_macos_display.py
  - tests/test_opengui_p4_desktop.py
autonomous: true
requirements:
  - MAC-03
  - MAC-01

must_haves:
  truths:
    - "The inner desktop backend can be told which target display to observe."
    - "BackgroundDesktopBackend injects the active DisplayInfo into the inner backend before inner.preflight()."
    - "Absolute coordinate offset translation stays wrapper-owned; the desktop backend does not re-apply offsets."
    - "observe() and execute() share the same target-surface metadata."
    - "Linux behavior remains unchanged when no target display is configured."
  artifacts:
    - path: "opengui/backends/desktop.py"
      provides: "Target-display-aware desktop capture path"
      contains: "configure_target_display"
    - path: "opengui/backends/background.py"
      provides: "Wrapper injection of DisplayInfo into the inner backend"
      contains: "configure_target_display"
    - path: "tests/test_opengui_p4_desktop.py"
      provides: "Non-primary monitor observe coverage"
      contains: "test_observe_uses_configured_monitor_index"
  key_links:
    - from: "opengui/backends/background.py::preflight"
      to: "opengui/backends/desktop.py::configure_target_display"
      via: "wrapper injects the active target surface before desktop preflight"
      pattern: "configure_target_display"
    - from: "opengui/backends/desktop.py::observe"
      to: "opengui/backends/background.py::_apply_offset"
      via: "capture and input stay aligned against the same DisplayInfo metadata"
      pattern: "monitor_index|offset_x"
---

<objective>
Make observation and input target the same macOS isolated surface by adding an explicit target-display configuration seam.

Purpose: Phase 13 is not complete when the display manager exists; MAC-03 requires the actual desktop IO path to honor the selected monitor and existing offset contract.
Output: a target-display-aware `LocalDesktopBackend`, wrapper injection of `DisplayInfo`, and green routing tests proving capture/input alignment.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-macos-background-execution/13-RESEARCH.md
@.planning/phases/13-macos-background-execution/13-VALIDATION.md
@opengui/backends/background.py
@opengui/backends/desktop.py
@opengui/backends/virtual_display.py
@tests/test_opengui_p4_desktop.py
@tests/test_opengui_p10_background.py
@tests/test_opengui_p13_macos_display.py

<interfaces>
Current wrapper seam:
```python
class BackgroundDesktopBackend:
    async def preflight(self) -> None: ...
    async def observe(self, screenshot_path: pathlib.Path, timeout: float = 5.0) -> Observation: ...
    async def execute(self, action: Action, timeout: float = 5.0) -> str: ...
```

Current desktop limitation:
```python
with mss.mss() as sct:
    monitor = sct.monitors[1]  # primary monitor
    logical_w = monitor["width"]
    logical_h = monitor["height"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add target-display-aware observe behavior to LocalDesktopBackend</name>
  <files>opengui/backends/desktop.py, tests/test_opengui_p4_desktop.py</files>
  <read_first>
    - opengui/backends/desktop.py
    - tests/test_opengui_p4_desktop.py
    - opengui/backends/virtual_display.py
    - .planning/phases/13-macos-background-execution/13-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: when no target display is configured, `observe()` still captures `mss.monitors[1]`.
    - Test 2: when `configure_target_display(DisplayInfo(... monitor_index=2 ...))` has been called, `observe()` captures `mss.monitors[2]`.
    - Test 3: HiDPI resizing still uses the selected monitor's logical width/height after the monitor switch.
  </behavior>
  <action>
Modify `opengui/backends/desktop.py` with these exact changes:

1. Add an instance attribute storing optional target-display metadata, initialized to `None`.
2. Add a narrow public method:
```python
def configure_target_display(self, display_info: DisplayInfo | None) -> None: ...
```
3. In `observe()`, select the monitor with:
   - `1` when no target display is configured
   - `self._target_display.monitor_index` when target display metadata exists
4. Keep the existing physical-to-logical downscale flow exactly as today; only the selected monitor changes.
5. Do not change `execute()` coordinate math in `LocalDesktopBackend`; offset translation remains the wrapper's responsibility.

Extend `tests/test_opengui_p4_desktop.py` with these exact green tests:
- `test_observe_uses_configured_monitor_index`
- `test_observe_defaults_to_primary_monitor_when_target_display_missing`

Update the mss test helper so it can provide at least two monitor entries without breaking existing tests.
  </action>
  <acceptance_criteria>
    - `rg -n "def configure_target_display" opengui/backends/desktop.py` returns one match.
    - `rg -n "monitor_index" opengui/backends/desktop.py` returns at least one match in `observe()`.
    - `rg -n "def test_observe_uses_configured_monitor_index" tests/test_opengui_p4_desktop.py` returns one match.
    - `rg -n "def test_observe_defaults_to_primary_monitor_when_target_display_missing" tests/test_opengui_p4_desktop.py` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p4_desktop.py::test_observe_uses_configured_monitor_index tests/test_opengui_p4_desktop.py::test_observe_defaults_to_primary_monitor_when_target_display_missing -q</automated>
  </verify>
  <done>The local desktop backend can observe the selected target display instead of always capturing the primary monitor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject DisplayInfo from the wrapper and prove observe/execute alignment</name>
  <files>opengui/backends/background.py, tests/test_opengui_p13_macos_display.py</files>
  <read_first>
    - opengui/backends/background.py
    - opengui/backends/desktop.py
    - tests/test_opengui_p10_background.py
    - tests/test_opengui_p13_macos_display.py
    - .planning/phases/13-macos-background-execution/13-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `BackgroundDesktopBackend.preflight()` calls `inner.configure_target_display(display_info)` before `inner.preflight()` when that method exists.
    - Test 2: absolute action coordinates still shift only once through `_apply_offset()`.
    - Test 3: a configured monitor index and wrapper offsets can coexist without mutating Linux/noop behavior.
  </behavior>
  <action>
Update `opengui/backends/background.py` with these concrete behaviors:

1. After `self._display_info = await self._display_manager.start()`, detect whether the inner backend has a callable `configure_target_display` attribute.
2. If present, call `inner.configure_target_display(self._display_info)` before `await self._inner.preflight()`.
3. On `shutdown()`, if the inner backend has `configure_target_display`, call `configure_target_display(None)` after display cleanup so later foreground runs do not inherit stale target metadata.
4. Leave `_apply_offset()` exactly wrapper-owned; do not move offset math into the inner desktop backend.

Extend `tests/test_opengui_p13_macos_display.py` with these exact green tests:
- `test_background_wrapper_configures_target_display_before_preflight`
- `test_macos_target_surface_routing_keeps_observe_and_execute_aligned`

In the routing-alignment test, use:
- a fake display manager returning `DisplayInfo(display_id="macos:42", width=1440, height=900, offset_x=200, offset_y=120, monitor_index=2)`
- an inner mock with `configure_target_display`, `preflight`, `observe`, and `execute`
- an absolute tap action `Action(action_type="tap", x=100.0, y=200.0)`

Assert that:
- `configure_target_display()` receives the same `DisplayInfo` instance returned by the manager
- `inner.execute()` receives `x=300.0`, `y=320.0` from wrapper offset translation
- the wrapper does not call `configure_target_display()` more than once during `preflight()`
  </action>
  <acceptance_criteria>
    - `rg -n "configure_target_display\\(self\\._display_info\\)" opengui/backends/background.py` returns at least one match.
    - `rg -n "configure_target_display\\(None\\)" opengui/backends/background.py` returns at least one match.
    - `rg -n "def test_background_wrapper_configures_target_display_before_preflight" tests/test_opengui_p13_macos_display.py` returns one match.
    - `rg -n "def test_macos_target_surface_routing_keeps_observe_and_execute_aligned" tests/test_opengui_p13_macos_display.py` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p13_macos_display.py::test_background_wrapper_configures_target_display_before_preflight tests/test_opengui_p13_macos_display.py::test_macos_target_surface_routing_keeps_observe_and_execute_aligned tests/test_opengui_p4_desktop.py -q</automated>
  </verify>
  <done>The wrapper and desktop backend now share one target-surface contract, satisfying the routing half of MAC-03.</done>
</task>

</tasks>

<verification>
Run the targeted desktop and Phase 13 unit tests together. Linux/noop behavior should continue to pass unchanged.
</verification>

<success_criteria>
Observation and input now target the same macOS display surface, with monitor selection handled in the desktop backend and global offsets handled only once in the wrapper.
</success_criteria>

<output>
After completion, create `.planning/phases/13-macos-background-execution/13-macos-background-execution-02-SUMMARY.md`
</output>
