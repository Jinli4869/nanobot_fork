---
phase: 20-web-app-integration-and-verification
plan: "03"
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - nanobot/tui/web/src/features/workspace-routes.test.tsx
  - nanobot/tui/web/src/app/shell.test.tsx
  - tests/test_tui_p20_static.py
  - tests/test_commands.py
  - README.md
  - .planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md
autonomous: true
requirements: [WEB-01, WEB-02, SHIP-01]
must_haves:
  truths:
    - "Automated regression proves the packaged web shell, backend routes, and existing CLI surface all still coexist after Phase 20."
    - "A user can follow one documented local workflow for development mode and one for packaged mode without guessing ports or build steps."
    - "Manual verification covers the actual browser behavior that automation cannot fully prove: opening the workspace, switching views, keeping context, and checking one unchanged CLI path."
  artifacts:
    - path: "tests/test_tui_p20_static.py"
      provides: "Phase 20 packaged startup and deep-link regression coverage"
      min_lines: 100
    - path: "tests/test_commands.py"
      provides: "CLI regression proving the existing nanobot entry point remains intact"
      contains: "nanobot"
    - path: ".planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md"
      provides: "Real-browser and CLI smoke path for milestone closeout"
      contains: "python -m nanobot.tui"
  key_links:
    - from: "README.md"
      to: "nanobot/tui/__main__.py"
      via: "documented startup commands for dev and packaged mode"
      pattern: "python -m nanobot.tui"
    - from: "tests/test_commands.py"
      to: "pyproject.toml"
      via: "existing CLI script preservation after adding nanobot-tui"
      pattern: "nanobot"
    - from: "tests/test_tui_p20_static.py"
      to: "nanobot/tui/app.py"
      via: "root-shell and deep-link serving regression checks"
      pattern: "/operations|/chat"
---

<objective>
Close Phase 20 with regression coverage and operator-facing startup guidance so the new web workspace is runnable, verifiable, and clearly bounded against the existing CLI-first nanobot workflows.

Purpose: Turn the Phase 20 implementation into a shippable slice by proving the integrated web surface works and documenting how to run it locally in both dev and packaged modes.
Output: Frontend/backend regression additions, CLI protection tests, README startup instructions, and a phase-local manual smoke checklist.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md
@.planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md
@pyproject.toml
@nanobot/tui/__main__.py
@nanobot/tui/app.py
@tests/test_tui_p17_config.py
@tests/test_commands.py
@nanobot/tui/web/package.json
@nanobot/tui/web/src/app/router.tsx
@nanobot/tui/web/src/app/shell.tsx
@nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx
@nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx

<interfaces>
Validation commands already defined for this plan:
```text
npm --prefix nanobot/tui/web run build
npm --prefix nanobot/tui/web run test -- --run
uv run --extra dev pytest tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py tests/test_tui_p18_chat.py tests/test_tui_p18_streaming.py tests/test_tui_p19_runtime.py tests/test_tui_p19_tasks.py tests/test_tui_p19_traces.py tests/test_tui_p20_static.py -q
```

Phase 20 closeout constraints:
```text
- keep implementation primarily under nanobot/tui
- do not broaden scope into auth, remote hosting, or core nanobot/OpenGUI rewrites
- preserve existing CLI-first behavior
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integrated regression coverage for workspace context, packaged deep links, and CLI safety</name>
  <files>nanobot/tui/web/src/features/workspace-routes.test.tsx, nanobot/tui/web/src/app/shell.test.tsx, tests/test_tui_p20_static.py, tests/test_commands.py</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, .planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, nanobot/tui/web/src/app/router.tsx, nanobot/tui/web/src/app/shell.tsx, nanobot/tui/web/src/features/chat/ChatWorkspaceRoute.tsx, nanobot/tui/web/src/features/operations/OperationsWorkspaceRoute.tsx, nanobot/tui/app.py, nanobot/tui/__main__.py, tests/test_tui_p20_static.py, tests/test_commands.py</read_first>
  <behavior>
    - Test 1: The frontend shell preserves `sessionId` and `runId` in the URL when navigating `Chat -> Operations -> Chat`.
    - Test 2: Packaged deep links continue to return the SPA shell after a frontend build.
    - Test 3: The existing `nanobot` CLI entry point remains callable and is not replaced by the new web startup script.
  </behavior>
  <action>Expand regression coverage rather than broadening runtime code. Strengthen `nanobot/tui/web/src/features/workspace-routes.test.tsx` and `nanobot/tui/web/src/app/shell.test.tsx` so they seed router state with an active `sessionId` and `runId`, navigate across the SPA, and assert that the same identifiers remain present in both the URL and visible UI. Extend `tests/test_tui_p20_static.py` with deep-link regressions for packaged `/chat/{session_id}` and `/operations?runId=...` behavior. Add a narrow `tests/test_commands.py` regression that proves the existing `nanobot` CLI surface still works after adding the web startup script and packaging metadata. Only touch production frontend code in the same task if these tests expose a real gap; do not reopen backend route design or CLI architecture.</action>
  <acceptance_criteria>
    - The frontend tests contain explicit assertions for both `sessionId` and `runId`.
    - `tests/test_tui_p20_static.py` asserts packaged `/chat/...` and `/operations?...` responses.
    - `tests/test_commands.py` contains a regression specifically protecting the existing `nanobot` CLI surface.
    - `npm --prefix nanobot/tui/web run test -- --run --reporter=dot` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run build && npm --prefix nanobot/tui/web run test -- --run --reporter=dot && uv run --extra dev pytest tests/test_tui_p20_static.py tests/test_commands.py -q</automated>
  </verify>
  <done>Integrated automated coverage now proves the SPA context model, packaged root/deep-link behavior, and CLI surface coexist cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Document the supported startup paths and real-browser smoke checklist</name>
  <files>README.md, .planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md</files>
  <read_first>.planning/phases/20-web-app-integration-and-verification/20-RESEARCH.md, .planning/phases/20-web-app-integration-and-verification/20-VALIDATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, nanobot/tui/__main__.py, pyproject.toml, nanobot/tui/web/package.json, tests/test_tui_p20_static.py</read_first>
  <action>Update `README.md` with exactly two supported startup workflows. Document development mode as `python -m nanobot.tui` for the backend on `127.0.0.1:18791` plus `npm --prefix nanobot/tui/web run dev` for the frontend proxy. Document packaged/local mode as `npm --prefix nanobot/tui/web run build` followed by `python -m nanobot.tui` or `nanobot-tui`. State explicitly that the existing `nanobot` CLI usage remains unchanged. Create `.planning/phases/20-web-app-integration-and-verification/20-MANUAL-SMOKE.md` with numbered steps that open the browser workspace, create or resume a chat session, switch to operations and back without losing context, inspect one run, and then rerun one representative CLI command after the browser check. Keep the docs local-first and bounded; do not add auth, remote hosting, or cloud deployment instructions.</action>
  <acceptance_criteria>
    - `README.md` contains the literal commands `python -m nanobot.tui`, `npm --prefix nanobot/tui/web run dev`, `npm --prefix nanobot/tui/web run build`, and `nanobot-tui`.
    - `README.md` states that existing `nanobot` CLI usage is unchanged.
    - `20-MANUAL-SMOKE.md` contains numbered browser and CLI verification steps.
    - The final Phase 20 validation command from `20-VALIDATION.md` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix nanobot/tui/web run build && npm --prefix nanobot/tui/web run test -- --run && uv run --extra dev pytest tests/test_tui_p17_runtime.py tests/test_tui_p17_config.py tests/test_tui_p18_chat.py tests/test_tui_p18_streaming.py tests/test_tui_p19_runtime.py tests/test_tui_p19_tasks.py tests/test_tui_p19_traces.py tests/test_tui_p20_static.py -q</automated>
  </verify>
  <done>The startup documentation is explicit, the smoke path is phase-local, and the full Phase 17-20 regression slice has a single closeout command.</done>
</task>

</tasks>

<verification>
Run the full `20-03` command from `20-VALIDATION.md`. If `uv run --extra dev pytest ...` is still blocked by the local optional-build issue documented in Phases 18-19, use the `.venv/bin/python -m pytest ...` fallback with the same test file list after the frontend build and frontend tests succeed.
</verification>

<success_criteria>
Phase 20 is shippable: the web workspace has integrated regression proof, local startup commands are documented for both dev and packaged modes, and a human smoke checklist covers the real browser behavior automation cannot fully sample.
</success_criteria>

<output>
After completion, create `.planning/phases/20-web-app-integration-and-verification/20-web-app-integration-and-verification-03-SUMMARY.md`
</output>
