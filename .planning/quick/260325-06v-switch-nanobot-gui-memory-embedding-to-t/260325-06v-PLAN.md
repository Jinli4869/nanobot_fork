---
phase: quick
plan: 260325-06v
type: execute
wave: 1
depends_on: []
files_modified:
  - nanobot/agent/tools/gui.py
  - tests/test_opengui_p6_wiring.py
autonomous: true
must_haves:
  truths:
    - "Nanobot GUI memory embedding uses the same OpenAI-compatible direct path as OpenGUI CLI when the provider exposes a direct client"
    - "LiteLLM embedding remains available as a fallback for providers that do not expose a direct OpenAI-compatible client"
    - "Direct embedding calls strip LiteLLM-style provider prefixes like dashscope/ before sending the model to the OpenAI-compatible endpoint"
  artifacts:
    - path: "nanobot/agent/tools/gui.py"
      provides: "Direct OpenAI-compatible embedding adapter path for GUI memory wiring"
      contains: "_normalize_direct_embedding_model"
    - path: "tests/test_opengui_p6_wiring.py"
      provides: "Regression coverage for custom/direct embedding path"
      contains: "test_gui_tool_uses_direct_openai_compatible_embedding_path_when_provider_exposes_client"
---

<objective>
Make Nanobot GUI memory retrieval use the same direct OpenAI-compatible embedding path as
OpenGUI CLI for custom providers, so DashScope-compatible endpoints behave the same in
Telegram/Nanobot as they do in standalone CLI runs.

Also document the required gui.embeddingModel configuration the user should keep in
~/.nanobot/config.json.
</objective>

<tasks>

<task type="auto">
  <name>Switch GUI embedding adapter to prefer direct OpenAI-compatible clients</name>
  <files>nanobot/agent/tools/gui.py</files>
  <action>
Update _build_embedding_adapter() so that when the active Nanobot provider exposes an
OpenAI-compatible direct client (for example CustomProvider), GUI memory embeddings reuse
that direct client instead of calling litellm.aembedding().

Keep the existing LiteLLM path as a fallback for providers that still rely on _resolve_model
and litellm-based routing.
  </action>
  <verify>
    <automated>Direct-path regression test passes and existing GUI wiring tests remain green</automated>
  </verify>
</task>

<task type="auto">
  <name>Add regression coverage and config guidance</name>
  <files>tests/test_opengui_p6_wiring.py</files>
  <action>
Add a focused test that proves a CustomProvider-like direct client uses embeddings.create()
with the provider prefix stripped from gui.embeddingModel, matching the behavior expected by
OpenGUI CLI's direct OpenAI-compatible embedding bridge.
  </action>
  <verify>
    <automated>uv run pytest tests/test_opengui_p6_wiring.py tests/test_opengui_p8_trajectory.py tests/test_opengui_p3_nanobot.py tests/test_opengui_p11_integration.py</automated>
  </verify>
</task>

</tasks>
