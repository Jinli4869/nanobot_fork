---
phase: 28-shortcut-extraction-productionization
plan: "02"
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - opengui/skills/shortcut.py
  - opengui/skills/shortcut_store.py
  - opengui/skills/shortcut_promotion.py
  - tests/test_opengui_p27_storage_search_agent.py
  - tests/test_opengui_p28_shortcut_productionization.py
autonomous: true
requirements: [SXTR-02, SXTR-03, SXTR-04]
must_haves:
  truths:
    - "Promoted shortcuts persist backward-compatible provenance and lineage metadata without breaking Phase 27 store/search round-trip behavior."
    - "The production promotion path rejects low-value candidates using explicit gates rather than Phase 26's always-pass defaults."
    - "Duplicate or near-duplicate promoted shortcuts are merged, versioned, or rejected deterministically instead of growing the store with repeated entries."
  artifacts:
    - path: "opengui/skills/shortcut.py"
      provides: "Shortcut schema fields for provenance and lineage"
      contains: "source_trace_path"
    - path: "opengui/skills/shortcut_store.py"
      provides: "Shortcut-layer merge/version lifecycle support"
      contains: "add_or_merge"
    - path: "tests/test_opengui_p28_shortcut_productionization.py"
      provides: "Phase 28 gating, provenance, and duplicate-handling coverage"
      contains: "test_promotion_pipeline_persists_full_shortcut_contract_after_store_reload"
  key_links:
    - from: "opengui/skills/shortcut_promotion.py"
      to: "opengui/skills/shortcut.py"
      via: "promotion enriches `ShortcutSkill` with persisted provenance fields"
      pattern: "source_trace_path|shortcut_version"
    - from: "opengui/skills/shortcut_store.py"
      to: "opengui/skills/library.py"
      via: "shortcut-layer merge heuristics adapt legacy conflict-detection patterns"
      pattern: "add_or_merge|_find_best_conflict|_merge"
    - from: "opengui/skills/shortcut_store.py"
      to: "tests/test_opengui_p27_storage_search_agent.py"
      via: "existing round-trip and search contracts stay green after metadata additions"
      pattern: "version|round_trip|search"
---

<objective>
Add provenance, quality gates, and merge/version behavior for promoted shortcuts.

Purpose: satisfy `SXTR-02`, `SXTR-03`, and `SXTR-04` by making promoted shortcuts self-describing, rejectable, and lifecycle-managed inside the shortcut-layer store rather than appended blindly.
Output: backward-compatible shortcut schema metadata, production gate logic in the promotion seam, shortcut-layer merge/version support, and regression tests covering the new lifecycle contract.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-shortcut-extraction-productionization/28-RESEARCH.md
@.planning/phases/28-shortcut-extraction-productionization/28-VALIDATION.md
@opengui/skills/shortcut.py
@opengui/skills/shortcut_store.py
@opengui/skills/shortcut_promotion.py
@opengui/skills/library.py
@tests/test_opengui_p27_storage_search_agent.py
@tests/test_opengui_p28_shortcut_productionization.py

<interfaces>
Add these concrete optional fields to `ShortcutSkill` in `opengui/skills/shortcut.py`:
```python
source_task: str | None = None
source_trace_path: str | None = None
source_run_id: str | None = None
source_step_indices: tuple[int, ...] = ()
promotion_version: int = 1
shortcut_version: int = 1
merged_from_ids: tuple[str, ...] = ()
promoted_at: float | None = None
```

Shortcut-layer merge/version target in `opengui/skills/shortcut_store.py`:
```text
- add `list_all(...)` and `update(...)` helpers for ShortcutSkill
- add async `add_or_merge(skill: ShortcutSkill) -> tuple[str, str | None]`
- conflict comparison must consider app/platform, action sequence, parameter slot names, state conditions, and provenance overlap
- merged shortcut keeps the canonical old `skill_id` and increments `shortcut_version`
```

Production gate target in `opengui/skills/shortcut_promotion.py`:
```text
- reject fewer than 2 promotable steps
- reject unsupported action types outside: tap, click, input_text, hotkey, scroll, swipe, press, wait
- reject candidates whose retained rows all have empty or whitespace-only `model_output`
- reject candidates whose derived `app` stays `unknown`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing provenance, gate, and merge/version contract tests</name>
  <files>tests/test_opengui_p28_shortcut_productionization.py, tests/test_opengui_p27_storage_search_agent.py</files>
  <read_first>.planning/phases/28-shortcut-extraction-productionization/28-RESEARCH.md, opengui/skills/shortcut.py, opengui/skills/shortcut_store.py, tests/test_opengui_p27_storage_search_agent.py, tests/test_opengui_p28_shortcut_productionization.py</read_first>
  <behavior>
    - Test 1: `test_promoted_shortcut_round_trips_with_provenance` creates a `ShortcutSkill` carrying `source_task`, `source_trace_path`, `source_run_id`, `source_step_indices`, `promotion_version`, `shortcut_version`, `merged_from_ids`, and `promoted_at`; assert `to_dict()` / `from_dict()` preserve them.
    - Test 2: `test_promotion_pipeline_persists_full_shortcut_contract_after_store_reload` runs the real promotion pipeline against a promotable trace, persists through `ShortcutSkillStore`, reloads the store, and asserts the promoted record still has normalized `app`, `platform`, `parameter_slots`, `preconditions`, `postconditions`, and provenance fields.
    - Test 3: `test_promotion_pipeline_rejects_low_value_candidates` configures traces or candidate rows that violate the explicit gate rules and asserts no store write occurs.
    - Test 4: `test_shortcut_store_add_or_merge_merges_duplicate_promotions` promotes two near-identical shortcuts for the same app/platform and asserts the store count remains 1 while `shortcut_version` increments and `merged_from_ids` captures the superseded ID.
    - Test 5: extend `tests/test_opengui_p27_storage_search_agent.py` so existing store round-trip/search tests continue to pass with the new optional metadata fields present.
  </behavior>
  <action>Extend `tests/test_opengui_p28_shortcut_productionization.py` with the exact test names `test_promoted_shortcut_round_trips_with_provenance`, `test_promotion_pipeline_persists_full_shortcut_contract_after_store_reload`, `test_promotion_pipeline_rejects_low_value_candidates`, and `test_shortcut_store_add_or_merge_merges_duplicate_promotions`. In the provenance test, assert the serialized dict contains the exact keys `source_trace_path`, `source_run_id`, `source_step_indices`, `promotion_version`, and `shortcut_version`. In the promotion/store-reload test, build a trace whose retained rows yield at least one parameter slot and non-empty pre/post conditions, run the real `ShortcutPromotionPipeline`, persist through `ShortcutSkillStore`, instantiate a second `ShortcutSkillStore` from the same directory, and assert the reloaded promoted shortcut preserves the exact fields `app`, `platform`, `parameter_slots`, `preconditions`, `postconditions`, `source_trace_path`, and `source_step_indices`. In the merge test, construct one old shortcut with `skill_id="shortcut-compose-v1"` and one new shortcut with `skill_id="shortcut-compose-v2"` for the same normalized app and matching step action signature, then assert the stored canonical record keeps `skill_id="shortcut-compose-v1"` and `shortcut_version == 2`. Update `tests/test_opengui_p27_storage_search_agent.py` so at least one round-trip test uses the new metadata-bearing shortcut shape.</action>
  <acceptance_criteria>
    - `tests/test_opengui_p28_shortcut_productionization.py` contains the exact test names `test_promoted_shortcut_round_trips_with_provenance`, `test_promotion_pipeline_persists_full_shortcut_contract_after_store_reload`, and `test_shortcut_store_add_or_merge_merges_duplicate_promotions`
    - `tests/test_opengui_p28_shortcut_productionization.py` contains the exact strings `source_trace_path`, `source_step_indices`, `parameter_slots`, `preconditions`, `postconditions`, `promotion_version`, and `shortcut_version`
    - `tests/test_opengui_p27_storage_search_agent.py` includes a round-trip assertion that reads the new optional shortcut metadata fields
    - `uv run pytest tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py -k "provenance or promotion_store_roundtrip or low_value or add_or_merge or round_trip"` exits non-zero before implementation and exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py -k "provenance or promotion_store_roundtrip or low_value or add_or_merge or round_trip"</automated>
  </verify>
  <done>The tests lock the exact persisted provenance fields, explicit gate rules, and shortcut-layer merge/version contract before implementation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement backward-compatible provenance fields, production gates, and shortcut-layer merge/version support</name>
  <files>opengui/skills/shortcut.py, opengui/skills/shortcut_store.py, opengui/skills/shortcut_promotion.py, tests/test_opengui_p27_storage_search_agent.py, tests/test_opengui_p28_shortcut_productionization.py</files>
  <read_first>.planning/phases/28-shortcut-extraction-productionization/28-RESEARCH.md, opengui/skills/shortcut.py, opengui/skills/shortcut_store.py, opengui/skills/library.py, opengui/skills/shortcut_promotion.py, tests/test_opengui_p28_shortcut_productionization.py</read_first>
  <behavior>
    - `ShortcutSkill.to_dict()` and `from_dict()` include the new optional provenance/lineage fields with safe defaults so older files still deserialize.
    - `ShortcutSkillStore.add_or_merge(...)` returns one of `ADD`, `MERGE`, `KEEP_OLD`, or `KEEP_NEW` and never creates repeated near-duplicate entries for the same app/platform/action signature.
    - Merged shortcuts preserve the canonical old ID, increment `shortcut_version`, union `merged_from_ids`, and keep existing search fields (`name`, `description`, `app`, `platform`, `parameter_slots`, `preconditions`, `postconditions`) intact.
    - `ShortcutPromotionPipeline` enforces the explicit low-value rejection gates before calling `store.add_or_merge(...)`.
  </behavior>
  <action>Modify `opengui/skills/shortcut.py` to add the exact optional fields listed in this plan's `<interfaces>` block and serialize them in `to_dict()` / `from_dict()` with defaults when keys are absent. Modify `opengui/skills/shortcut_store.py` to add `list_all(platform: str | None = None, app: str | None = None) -> list[ShortcutSkill]`, `update(skill_id: str, updated_skill: ShortcutSkill) -> bool`, `_find_best_conflict(incoming: ShortcutSkill) -> ShortcutSkill | None`, `_action_signature(skill: ShortcutSkill) -> tuple[str, ...]`, `_condition_signature(skill: ShortcutSkill) -> tuple[str, ...]`, `_merge_shortcuts(old: ShortcutSkill, new: ShortcutSkill) -> ShortcutSkill`, and `async add_or_merge(skill: ShortcutSkill) -> tuple[str, str | None]`. Use these concrete merge rules: match only when `platform` and normalized `app` are equal; treat exact `source_trace_path + source_step_indices` as same-origin conflict; otherwise require matching normalized name or action similarity >= 0.75; on merge, keep `old.skill_id`, set `shortcut_version = max(old.shortcut_version, new.shortcut_version) + 1`, set `merged_from_ids` to the sorted union of `old.merged_from_ids`, `new.merged_from_ids`, and `new.skill_id` when different, preserve `created_at=old.created_at`, and set `promoted_at` to the newer non-null timestamp. Update `opengui/skills/shortcut_promotion.py` so after `ExtractionSuccess` it enriches the candidate with concrete provenance values from the trace and calls `await store.add_or_merge(enriched)` instead of `store.add(...)`. Before any store write, enforce the gate rules from `<interfaces>` exactly, including rejecting `app == "unknown"` and unsupported `action_type` values.</action>
  <acceptance_criteria>
    - `opengui/skills/shortcut.py` contains the exact field names `source_trace_path`, `source_run_id`, `source_step_indices`, `promotion_version`, `shortcut_version`, `merged_from_ids`, and `promoted_at`
    - `opengui/skills/shortcut_store.py` contains `async def add_or_merge(self, skill: ShortcutSkill)` and `def list_all(`
    - `opengui/skills/shortcut_promotion.py` contains the exact supported-action gate values `tap`, `click`, `input_text`, `hotkey`, `scroll`, `swipe`, `press`, and `wait`
    - `opengui/skills/shortcut_promotion.py` calls `await store.add_or_merge(`
    - `uv run pytest tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py -k "provenance or promotion_store_roundtrip or low_value or add_or_merge or round_trip"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py -k "provenance or promotion_store_roundtrip or low_value or add_or_merge or round_trip"</automated>
  </verify>
  <done>Promoted shortcuts now persist stable provenance, reject brittle candidates up front, and lifecycle-manage duplicates inside the shortcut-layer store.</done>
</task>

</tasks>

<verification>
Run the storage/promotion slice from the validation contract: `uv run pytest tests/test_opengui_p27_storage_search_agent.py tests/test_opengui_p28_shortcut_productionization.py -k "provenance or promotion_store_roundtrip or low_value or add_or_merge or round_trip"`. Confirm older shortcut-store fixtures still load, promotion output survives store reload with `app`, `platform`, `parameter_slots`, `preconditions`, and `postconditions` intact, and duplicate promotions do not increase the stored shortcut count.
</verification>

<success_criteria>
`SXTR-02`, `SXTR-03`, and `SXTR-04` are satisfied when promoted shortcuts carry persisted provenance and lineage metadata, the promotion seam rejects low-value evidence before any write, and duplicate shortcut candidates are merged, versioned, or rejected deterministically in the shortcut-layer store.
</success_criteria>

<output>
After completion, create `.planning/phases/28-shortcut-extraction-productionization/28-02-SUMMARY.md`
</output>
