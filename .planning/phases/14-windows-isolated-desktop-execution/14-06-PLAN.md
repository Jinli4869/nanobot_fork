---
phase: 14-windows-isolated-desktop-execution
plan: "06"
type: execute
wave: 2
depends_on:
  - 14-05
files_modified:
  - opengui/cli.py
  - nanobot/agent/tools/gui.py
  - tests/test_opengui_p5_cli.py
  - tests/test_opengui_p11_integration.py
  - tests/test_opengui_p12_runtime_contracts.py
autonomous: true
requirements:
  - WIN-01
  - WIN-02
  - WIN-03
gap_closure: true

must_haves:
  truths:
    - "CLI and nanobot pass a real `target_app_class` signal into the Windows isolated probe before mode resolution."
    - "Unsupported Windows app classes block or warn before the first agent step instead of only existing as unreachable probe logic."
    - "The Phase 14 regression slice proves the repaired Windows worker path without regressing Linux or macOS runtime behavior."
  artifacts:
    - path: "opengui/cli.py"
      provides: "CLI app-class propagation into the shared runtime probe"
      contains: "target_app_class"
    - path: "nanobot/agent/tools/gui.py"
      provides: "Nanobot app-class propagation into the shared runtime probe"
      contains: "target_app_class"
    - path: "tests/test_opengui_p5_cli.py"
      provides: "CLI regression coverage for Windows app-class propagation and ordering"
      contains: "test_run_cli_passes_target_app_class_to_windows_probe"
    - path: "tests/test_opengui_p11_integration.py"
      provides: "Nanobot regression coverage for Windows app-class propagation and failure semantics"
      contains: "test_gui_tool_passes_target_app_class_to_windows_probe"
  key_links:
    - from: "opengui/cli.py::run_cli"
      to: "opengui/backends/background_runtime.py::probe_isolated_background_support"
      via: "CLI forwards `target_app_class` into the shared Windows probe"
      pattern: "target_app_class="
    - from: "nanobot/agent/tools/gui.py::execute"
      to: "opengui/backends/background_runtime.py::probe_isolated_background_support"
      via: "Nanobot forwards `target_app_class` into the shared Windows probe"
      pattern: "target_app_class="
    - from: "tests/test_opengui_p5_cli.py"
      to: "tests/test_opengui_p11_integration.py"
      via: "both host-entry suites assert app-class blocking happens before agent execution starts"
      pattern: "windows_app_class_unsupported"
---

<objective>
Finish the Phase 14 gap closure by wiring real Windows app-class input through both host entry points and rerunning the cross-slice regression that protects Linux and macOS.

Purpose: The Windows probe already understands unsupported app classes, but the verifier showed CLI and nanobot never pass that input in real runs. This plan makes the probe reachable and closes the regression loop.
Output: CLI and nanobot app-class propagation, host-entry regression coverage, and a green phase slice after the repaired worker/backend path lands.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-windows-isolated-desktop-execution/14-RESEARCH.md
@.planning/phases/14-windows-isolated-desktop-execution/14-VERIFICATION.md
@.planning/phases/14-windows-isolated-desktop-execution/14-05-PLAN.md
@.planning/phases/13-macos-background-execution/13-macos-background-execution-03-SUMMARY.md
@opengui/backends/background_runtime.py
@opengui/cli.py
@nanobot/agent/tools/gui.py
@tests/test_opengui_p5_cli.py
@tests/test_opengui_p11_integration.py
@tests/test_opengui_p12_runtime_contracts.py

<interfaces>
Shared probe signature already supports Windows app-class input:
```python
def probe_isolated_background_support(
    *,
    sys_platform: str | None = None,
    xvfb_binary: str = "Xvfb",
    target_app_class: str | None = None,
) -> IsolationProbeResult:
```

Existing host-entry seam:
```python
probe = probe_fn(sys_platform=sys.platform)
decision = resolve_fn(...)
if decision.mode == "isolated":
    mgr = _build_isolated_display_manager(...)
```

Gap-closing host contract:
- CLI accepts an explicit Windows app-class input and forwards it to the probe before `resolve_run_mode()`.
- Nanobot exposes the same input on the GUI tool schema and forwards it to the probe before `resolve_run_mode()`.
- Linux and macOS calls remain behaviorally unchanged; the new argument only changes Windows isolated probing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire explicit Windows app-class propagation through CLI and nanobot</name>
  <files>opengui/cli.py, nanobot/agent/tools/gui.py, tests/test_opengui_p5_cli.py, tests/test_opengui_p11_integration.py</files>
  <read_first>
    - opengui/cli.py
    - nanobot/agent/tools/gui.py
    - opengui/backends/background_runtime.py
    - tests/test_opengui_p5_cli.py
    - tests/test_opengui_p11_integration.py
    - .planning/phases/14-windows-isolated-desktop-execution/14-VERIFICATION.md
    - .planning/phases/13-macos-background-execution/13-macos-background-execution-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: CLI passes `target_app_class` into `probe_isolated_background_support()` before mode resolution and blocks unsupported Windows app classes before the first agent step.
    - Test 2: Nanobot forwards the same `target_app_class` value through `GuiSubagentTool.execute()` and preserves its existing JSON failure shape.
    - Test 3: When no explicit value is supplied, both host entry points default Windows isolated probes to `classic-win32` so the shared Windows probe path is still reachable in real runs.
  </behavior>
  <action>
Implement explicit app-class propagation instead of heuristics:

1. In `opengui/cli.py`:
   - add a new argument `--target-app-class` with choices `classic-win32`, `uwp`, `directx`, `gpu-heavy`, and `electron-gpu`
   - add a helper that resolves the probe value: on Windows background local runs default to `classic-win32` when the flag is omitted; on other hosts return `None`
   - call `probe_isolated_background_support(sys_platform=sys.platform, target_app_class=resolved_target_app_class)` before `resolve_run_mode()`

2. In `nanobot/agent/tools/gui.py`:
   - add `"target_app_class"` to the tool schema with the same allowed values
   - extend `GuiSubagentTool.execute()` signature to accept `target_app_class: str | None = None`
   - default Windows background local runs to `classic-win32` when the caller omits the field
   - call `probe_isolated_background_support(sys_platform=sys.platform, target_app_class=resolved_target_app_class)` before `resolve_run_mode()`

3. Update host-entry tests with these exact additions:
   - `test_run_cli_passes_target_app_class_to_windows_probe`
   - `test_gui_tool_passes_target_app_class_to_windows_probe`

The propagation tests must capture the actual kwargs passed to the probe and assert:
- `target_app_class == "uwp"` when the explicit value is provided
- `target_app_class == "classic-win32"` when Windows isolated mode is requested without an explicit override

Do not add raw `sys.platform == "win32"` dispatch after probe resolution. Backend selection remains keyed on `probe.backend_name`, consistent with Phase 13.
  </action>
  <acceptance_criteria>
    - `rg -n -- "--target-app-class|target_app_class" opengui/cli.py nanobot/agent/tools/gui.py` returns at least six total matches.
    - `rg -n "def test_run_cli_passes_target_app_class_to_windows_probe" tests/test_opengui_p5_cli.py` returns one match.
    - `rg -n "def test_gui_tool_passes_target_app_class_to_windows_probe" tests/test_opengui_p11_integration.py` returns one match.
    - `rg -n "classic-win32|windows_app_class_unsupported" tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py` returns at least four total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p5_cli.py -k "passes_target_app_class_to_windows_probe or warns_for_windows_unsupported_app_class_before_agent_start" tests/test_opengui_p11_integration.py -k "passes_target_app_class_to_windows_probe or blocks_windows_non_interactive_isolation_request" -q</automated>
  </verify>
  <done>Both host entry points can now reach the Windows app-class probe logic in real runs and preserve their existing warning/block behavior shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Run and stabilize the Phase 14 gap-closure regression slice</name>
  <files>tests/test_opengui_p5_cli.py, tests/test_opengui_p11_integration.py, tests/test_opengui_p12_runtime_contracts.py</files>
  <read_first>
    - tests/test_opengui_p14_windows_desktop.py
    - tests/test_opengui_p5_cli.py
    - tests/test_opengui_p11_integration.py
    - tests/test_opengui_p12_runtime_contracts.py
    - opengui/backends/background_runtime.py
    - opengui/cli.py
    - nanobot/agent/tools/gui.py
    - .planning/phases/14-windows-isolated-desktop-execution/14-VERIFICATION.md
  </read_first>
  <action>
Run the full cross-slice gap-closure regression command exactly as written:

`uv run pytest tests/test_opengui_p14_windows_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q`

If any test fails, fix it in this same plan with these constraints:
- keep Linux Xvfb and macOS CGVirtualDisplay behavior unchanged
- do not weaken assertions that guard `windows_app_class_unsupported`, `windows_non_interactive_session`, `backend_name=windows_isolated_desktop`, or `cleanup_reason=...`
- prefer adjusting stale tests over broad runtime behavior changes when the failure is only in expected tokens or ordering

Only touch `tests/test_opengui_p12_runtime_contracts.py` if the repaired Windows probe path exposes a real shared-runtime regression. Do not add new planning docs in this plan.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_opengui_p14_windows_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q` exits 0.
    - `rg -n "windows_isolated_desktop|windows_app_class_unsupported|cleanup_reason=|classic-win32" tests/test_opengui_p14_windows_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py` returns at least eight total matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_opengui_p14_windows_desktop.py tests/test_opengui_p5_cli.py tests/test_opengui_p11_integration.py tests/test_opengui_p12_runtime_contracts.py -q</automated>
  </verify>
  <done>The host-entry app-class path is proven end-to-end and the repaired Windows isolated seam passes the phase regression slice without regressing Linux or macOS.</done>
</task>

</tasks>

<verification>
Do not mark the gap closure complete until the full four-file regression command passes. The targeted propagation tests are necessary but not sufficient because the verifier failures span runtime, backend, CLI, and nanobot behavior together.
</verification>

<success_criteria>
The Windows app-class block/warn path is reachable from both host entry points, and the Phase 14 regression slice passes with real worker-routed Windows execution while Linux and macOS behavior remain stable.
</success_criteria>

<output>
After completion, create `.planning/phases/14-windows-isolated-desktop-execution/14-windows-isolated-desktop-execution-06-SUMMARY.md`
</output>
