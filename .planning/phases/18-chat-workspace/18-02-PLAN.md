---
phase: 18-chat-workspace
plan: "02"
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - nanobot/tui/contracts.py
  - nanobot/tui/dependencies.py
  - nanobot/tui/routes/chat.py
  - nanobot/tui/schemas/__init__.py
  - nanobot/tui/schemas/chat.py
  - nanobot/tui/services/__init__.py
  - nanobot/tui/services/chat.py
  - nanobot/tui/services/event_stream.py
  - tests/test_tui_p18_streaming.py
autonomous: true
requirements:
  - CHAT-02
must_haves:
  truths:
    - "Assistant progress updates stream incrementally to the browser while a reply is running."
    - "The browser receives a final assistant event without polling for completion."
    - "The first streaming transport is browser-native and reconnect-friendly."
  artifacts:
    - path: "nanobot/tui/services/event_stream.py"
      provides: "In-process event broker for live chat events"
    - path: "nanobot/tui/routes/chat.py"
      provides: "SSE endpoint for chat session events"
    - path: "tests/test_tui_p18_streaming.py"
      provides: "Coverage for event ordering and streamed progress/final reply behavior"
  key_links:
    - from: "nanobot/tui/services/chat.py"
      to: "nanobot/tui/services/event_stream.py"
      via: "progress callback publishing typed events"
      pattern: "publish|emit"
    - from: "nanobot/tui/routes/chat.py"
      to: "fastapi.sse.EventSourceResponse"
      via: "GET session event stream"
      pattern: "EventSourceResponse"
    - from: "nanobot.agent.loop.AgentLoop.process_direct"
      to: "nanobot/tui/services/chat.py"
      via: "on_progress callback forwarding"
      pattern: "on_progress"
---

<objective>
Layer live browser streaming onto the chat workspace without changing the CLI path or widening the provider/runtime surface.

Purpose: satisfy `CHAT-02` by translating `AgentLoop.process_direct(..., on_progress=...)` updates into SSE events the browser can consume incrementally.
Output: a typed event broker, SSE event schema, and chat-streaming coverage.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-chat-workspace/18-RESEARCH.md
@.planning/phases/18-chat-workspace/18-VALIDATION.md
@.planning/phases/18-chat-workspace/18-01-PLAN.md
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/tui/routes/chat.py
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/tui/dependencies.py
@/Users/jinli/Documents/Personal/nanobot_fork/nanobot/agent/loop.py

<interfaces>
From `nanobot/agent/loop.py`:
```python
async def process_direct(..., on_progress: Callable[[str], Awaitable[None]] | None = None) -> str: ...
```

Required event envelope from research:
```python
class ChatEvent(BaseModel):
    id: str
    type: Literal["message.accepted", "progress", "assistant.final", "error", "complete"]
    session_id: str
    run_id: str | None = None
    payload: dict[str, Any]
```

Transport decision:
```text
POST /chat/sessions/{session_id}/messages
GET  /chat/sessions/{session_id}/events
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add streaming tests and typed SSE event models</name>
  <files>nanobot/tui/contracts.py, nanobot/tui/schemas/__init__.py, nanobot/tui/schemas/chat.py, tests/test_tui_p18_streaming.py</files>
  <behavior>
    - Test 1: the SSE stream emits `message.accepted`, one or more `progress` events, `assistant.final`, then `complete` in order for a successful run.
    - Test 2: progress-only callbacks and the final assistant reply remain distinct event types so the browser does not need to parse CLI-formatted text.
    - Test 3: the stream contract stays GET + SSE rather than POST streaming or Typer-mediated transport.
  </behavior>
  <action>Create `tests/test_tui_p18_streaming.py` first and lock the transport contract around SSE with typed event envelopes. Extend chat schemas/contracts only as needed to represent stream events and broker subscriptions. Keep the scope at backend transport only; do not add polling fallbacks, WebSockets, or frontend consumers in this phase.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p18_streaming.py::test_sse_stream_emits_progress_and_final_reply_events tests/test_tui_p18_streaming.py::test_stream_transport_preserves_event_order_for_progress_and_final_message -q</automated>
  </verify>
  <done>Streaming tests exist, the SSE event envelope is typed, and the plan locks Phase 18 to a browser-native GET stream transport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the in-memory event broker and SSE chat stream</name>
  <files>nanobot/tui/dependencies.py, nanobot/tui/routes/chat.py, nanobot/tui/services/__init__.py, nanobot/tui/services/chat.py, nanobot/tui/services/event_stream.py, tests/test_tui_p18_streaming.py</files>
  <behavior>
    - Test 1: subscribing to `/chat/sessions/{session_id}/events` yields SSE events as the matching message submission runs.
    - Test 2: `ChatWorkspaceService` publishes progress updates from the `on_progress` callback and emits a final assistant event when `process_direct()` returns.
    - Test 3: disconnect handling unsubscribes cleanly and does not require booting CLI channel runtime.
  </behavior>
  <action>Implement a small in-process event broker dedicated to `nanobot/tui`, then inject it through the dependency layer into the chat service and router. Use `EventSourceResponse` for `GET /chat/sessions/{session_id}/events`, keep the POST message endpoint as the mutation trigger, and publish typed events from the existing `on_progress` callback and final `process_direct()` return. Do not route browser chat through Typer commands, do not add provider-level token streaming, and do not let the transient broker become the durable source of truth.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_tui_p18_streaming.py::test_sse_stream_emits_progress_and_final_reply_events tests/test_tui_p18_streaming.py::test_stream_transport_preserves_event_order_for_progress_and_final_message tests/test_tui_p18_chat.py::test_chat_routes_create_and_reuse_session_backed_conversations -q</automated>
  </verify>
  <done>The browser can attach to an SSE stream, receive incremental progress and final assistant events for a chat run, and the event transport stays isolated inside `nanobot/tui`.</done>
</task>

</tasks>

<verification>
Run the dedicated streaming tests plus the core chat route test to prove the streaming layer composes on top of the Phase 18 foundation instead of replacing it.
</verification>

<success_criteria>
`CHAT-02` is satisfied when assistant progress and final completion arrive over SSE in stable event order, without polling and without any CLI command-path changes.
</success_criteria>

<output>
After completion, create `.planning/phases/18-chat-workspace/18-chat-workspace-02-SUMMARY.md`
</output>
