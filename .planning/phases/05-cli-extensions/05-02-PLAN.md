---
phase: 05-cli-extensions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ADAPTERS.md
  - opengui/interfaces.py
  - tests/test_opengui_p5_adapters.py
autonomous: true
requirements:
  - EXT-01
must_haves:
  truths:
    - "A developer can open `ADAPTERS.md` and understand how to adapt another host to `LLMProvider` and `DeviceBackend` without reading nanobot internals first."
    - "The repo contains a short in-code pointer that sends adapter authors from `opengui/interfaces.py` to the full adapter documentation."
    - "The documentation references a real production example (`NanobotLLMAdapter`) and a copy-paste starter skeleton."
  artifacts:
    - path: "ADAPTERS.md"
      provides: "Repo-root adapter guide for other claw hosts"
      contains:
        - "# OpenGUI Adapter Patterns"
        - "## LLMProvider"
        - "## DeviceBackend"
        - "class ExampleHostLLMAdapter"
        - "NanobotLLMAdapter"
    - path: "opengui/interfaces.py"
      provides: "Short code-level pointer from the protocol definitions to the adapter guide"
      contains:
        - "For host-agent adapter examples, see repo-root ADAPTERS.md and nanobot/agent/gui_adapter.py."
    - path: "tests/test_opengui_p5_adapters.py"
      provides: "Automated docs regression checks"
      contains:
        - "test_adapters_doc_contains_required_sections"
        - "test_adapter_pointer_exists_in_code"
  key_links:
    - from: "opengui/interfaces.py"
      to: "ADAPTERS.md"
      via: "module docstring pointer"
      pattern: "ADAPTERS\\.md"
    - from: "ADAPTERS.md"
      to: "nanobot/agent/gui_adapter.py"
      via: "real reference example"
      pattern: "nanobot/agent/gui_adapter\\.py"
---

<objective>
Document the adapter pattern for other claw hosts and place a code-level pointer at the protocol boundary.

Purpose: Satisfy `EXT-01` and the roadmap success criterion without coupling `opengui` to nanobot runtime code.
Output: `ADAPTERS.md`, a short adapter pointer in `opengui/interfaces.py`, and automated docs checks in `tests/test_opengui_p5_adapters.py`.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-extensions/05-CONTEXT.md
@.planning/phases/05-cli-extensions/05-RESEARCH.md
@.planning/phases/05-cli-extensions/05-VALIDATION.md
@.planning/phases/03-nanobot-subagent/03-01-SUMMARY.md
@opengui/interfaces.py
@nanobot/agent/gui_adapter.py

<interfaces>
From `opengui/interfaces.py`:
```python
class LLMProvider(typing.Protocol):
    async def chat(...) -> LLMResponse: ...

class DeviceBackend(typing.Protocol):
    async def observe(...) -> Observation: ...
    async def execute(...) -> str: ...
    async def preflight(...) -> None: ...
    @property
    def platform(self) -> str: ...
```

From `nanobot/agent/gui_adapter.py`:
```python
class NanobotLLMAdapter:
    """Wrap a nanobot LLM provider with opengui's chat interface."""

    async def chat(...) -> OpenGuiLLMResponse: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docs regression tests for the adapter guide and in-code pointer</name>
  <files>tests/test_opengui_p5_adapters.py</files>
  <read_first>
tests/test_opengui_p5_adapters.py
tests/test_opengui_p3_nanobot.py
opengui/interfaces.py
nanobot/agent/gui_adapter.py
.planning/phases/05-cli-extensions/05-CONTEXT.md
.planning/phases/05-cli-extensions/05-RESEARCH.md
.planning/phases/05-cli-extensions/05-VALIDATION.md
  </read_first>
  <action>
Create `tests/test_opengui_p5_adapters.py` with file-content assertions for repo-root `ADAPTERS.md` and `opengui/interfaces.py`.
Cover the required documentation contract: the guide must mention both `LLMProvider` and `DeviceBackend`, include a copy-paste skeleton adapter class named `ExampleHostLLMAdapter`, reference `NanobotLLMAdapter` plus `nanobot/agent/gui_adapter.py`, and verify the exact pointer sentence in `opengui/interfaces.py`.
Use direct text assertions only; this test file must not import nanobot runtime modules or execute CLI code.
If you need temporary RED scaffolding, mark the new tests `@pytest.mark.xfail(strict=False, reason="Phase 5 adapter docs pending")` in this task and remove those markers in Task 2.
  </action>
  <acceptance_criteria>
`rg -n "def test_adapters_doc_contains_required_sections" tests/test_opengui_p5_adapters.py` returns 1 match.
`rg -n "def test_adapter_pointer_exists_in_code" tests/test_opengui_p5_adapters.py` returns 1 match.
`rg -n "ExampleHostLLMAdapter" tests/test_opengui_p5_adapters.py` returns at least 1 match.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_opengui_p5_adapters.py -x -q</automated>
  </verify>
  <done>The docs regression file exists and encodes every `EXT-01` content requirement the documentation task must satisfy.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADAPTERS.md and add the protocol-level pointer in interfaces.py</name>
  <files>ADAPTERS.md, opengui/interfaces.py, tests/test_opengui_p5_adapters.py</files>
  <read_first>
ADAPTERS.md
opengui/interfaces.py
tests/test_opengui_p5_adapters.py
nanobot/agent/gui_adapter.py
.planning/phases/05-cli-extensions/05-CONTEXT.md
.planning/phases/05-cli-extensions/05-RESEARCH.md
.planning/phases/03-nanobot-subagent/03-01-SUMMARY.md
  </read_first>
  <action>
Create repo-root `ADAPTERS.md` with the exact top heading `# OpenGUI Adapter Patterns`.
Organize the guide with explicit sections for `LLMProvider`, `DeviceBackend`, and the wiring pattern that keeps `opengui` independent while host runtimes adapt into those protocols.
Include a copy-paste starter skeleton named `ExampleHostLLMAdapter` that shows a host LLM being wrapped into `LLMResponse` and `ToolCall`, plus a short note that hosts may reuse `AdbBackend` / `LocalDesktopBackend` or implement their own `DeviceBackend`.
Reference `NanobotLLMAdapter` and `nanobot/agent/gui_adapter.py` as the real production example, but state explicitly that nanobot files are reference-only and must not be imported into `opengui` runtime code.
Add the exact sentence `For host-agent adapter examples, see repo-root ADAPTERS.md and nanobot/agent/gui_adapter.py.` to the top-level module docstring in `opengui/interfaces.py`.
Promote the Phase 5 adapter docs tests from Task 1 to passing and remove any temporary xfail markers before finishing.
  </action>
  <acceptance_criteria>
`rg -n "^# OpenGUI Adapter Patterns$" ADAPTERS.md` returns 1 match.
`rg -n "^## LLMProvider$" ADAPTERS.md` returns 1 match.
`rg -n "^## DeviceBackend$" ADAPTERS.md` returns 1 match.
`rg -n "class ExampleHostLLMAdapter" ADAPTERS.md` returns at least 1 match.
`rg -n "NanobotLLMAdapter" ADAPTERS.md` returns at least 1 match.
`rg -n "nanobot/agent/gui_adapter.py" ADAPTERS.md` returns at least 1 match.
`rg -n "For host-agent adapter examples, see repo-root ADAPTERS.md and nanobot/agent/gui_adapter.py." opengui/interfaces.py` returns 1 match.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_opengui_p5_adapters.py -x -q</automated>
  </verify>
  <done>`EXT-01` is complete: the repo ships adapter documentation, the protocols module points to it, and the documentation is regression-tested.</done>
</task>

</tasks>

<verification>
Run `./.venv/bin/python -m pytest tests/test_opengui_p5_adapters.py -x -q`.
Run `rg -n "ADAPTERS.md|nanobot/agent/gui_adapter.py" opengui/interfaces.py ADAPTERS.md` to confirm the pointer and reference link both exist.
</verification>

<success_criteria>
`ADAPTERS.md` exists at the repo root and documents how another claw host adapts into `LLMProvider` and `DeviceBackend`.
`opengui/interfaces.py` contains the required short pointer sentence to the adapter guide and production example.
The docs regression tests pass without importing nanobot runtime code into `opengui`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-extensions/05-02-SUMMARY.md`
</output>
